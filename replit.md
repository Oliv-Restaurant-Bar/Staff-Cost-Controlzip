# Personalkostentracker
An internal reporting tool for revenue, personnel costs, schedules, and KPIs, supporting multiple restaurant tenants.

## Run & Operate
- **Run:** `npm run dev`
- **Build:** `npm run build`
- **Required Env Vars:** Supabase URL and Anon Key (see `src/integrations/supabase/client.ts`)

## Stack
- **Frameworks:** React, TypeScript, Shadcn UI, Tailwind CSS
- **Runtime:** Node.js (via Vite)
- **Database/ORM:** Supabase (Auth, Postgres DB, KV Store)
- **Date Handling:** `date-fns`
- **Charting:** `recharts`

## Where things live
- **Core Application:** `src/App.tsx`
- **Tenant Management:** `src/contexts/TenantContext.tsx`, `src/components/TenantSwitcher.tsx`, `src/lib/tenant-utils.ts`
- **Supabase Integration:** `src/integrations/supabase/client.ts`, `src/lib/supabase-db.ts`, `src/lib/supabase-kv.ts`
- **Database Schema:** `supabase/setup_new_project.sql` (and migration scripts in `supabase/migrations/`)
- **Authentication & Permissions:** `src/contexts/AuthContext.tsx`, `src/hooks/usePermissions.ts`
- **Küchen-Manager Personalstamm-Zugriff (kueche_manager, eingeschränkt):** `usePermissions.ts` lässt `kueche_manager` `canAccessModule('personalstamm')` + `canEditEmployees` (Mitarbeiter anlegen/bearbeiten), aber die Lohn-Gates (`canSeeHourlyWages`/`canToggleCostView`/`canAccessSettings`) bleiben UNVERÄNDERT → Küchen-Manager sieht NIE Löhne. In `Personalstamm.tsx` ist die Liste über `allowedDepartment` bereits auf Küche gefiltert; `handleNew()` setzt die Abteilung neuer Mitarbeiter auf `allowedDepartment` und der Abteilungs-`Select` ist gesperrt, wenn `allowedDepartment !== 'all'` (Küchen-Manager kann nur Küchen-Mitarbeiter anlegen). **Wichtig (PII/Lohn-Leak vermieden):** `canEditEmployees` war historisch = Admin+GF Beaulieu und gated noch immer vertrauliche Flächen; daher gibt es das lokale Gate `canManageAllEmployees = isAdmin || isBeaulieuManager` (= alte canEditEmployees-Semantik), das ALLE Admin-/Vertraulich-Flächen abschirmt und NICHT für den Küchen-Manager gilt: Duplikat-Warnung (zeigt Löhne), „Ausstehende Anmeldungen"/pending_review (Onboarding-PII), Abschnitt „Persönliche Daten" (inkl. AHV/IBAN), „Vertragliche Grundlagen", „Arbeitsvertrag". Lohnfelder selbst bleiben unter `canEditWages`. Küchen-Manager bearbeitet nur Stammdaten (Name/Abteilung[Küche]/Anstellungsart/Wochenstunden/aktiv/Zeiterfassungs-Flag) + Notizen.
- **Routing:** Defined in `src/App.tsx`
- **Navigation (Menüstruktur):** `src/components/AppNav.tsx` — `DASHBOARD_ITEM` + `NAV_GROUPS` (7 groups: Verkauf, Umsatz, Personal, Warenkosten, Foratable, Admin, Import) drive desktop sidebar, mobile bottom bar AND mobile sheet. Active highlighting uses the longest-path matcher `activeNavPath(pathname)`. `/reporting` is intentionally unlinked (route kept in `App.tsx`, reachable only by URL).
- **UI Components:** `src/components/`
- **Pages:** `src/pages/`
- **Reporting & Financials:** `src/types/reporting.ts`, `src/lib/reporting-store.ts`, `src/types/pl.ts`, `src/lib/pl-engine.ts`, `src/pages/PLView.tsx`
- **P&L Vorjahres-Diagnose (nur Sichtbarkeit, KEINE Berechnung):** Pure `src/lib/pl-prior-year-diagnostics.ts` (`computePriorYearDiagnostics(priorYear, prevYearRecords, vjDailyData)` → `PriorYearDiagnostics`; NO supabase/DOM, only `import type` from `reporting`/`vj-daily-supabase`). Prüft die in `PLView.tsx` BEREITS geladenen Vorjahresdaten (`prevYearRecords = loadYear(year-1)` aus `reporting_v1` + `vjDailyData = loadVjDailyYear(year-1)`, beide via authentifizierte Supabase-Session) auf: `hasRevenue` (`revenueActual>0`), `hasExpenses` (`expenseCategories[].amount≠0`), `hasPersonnel` (`personnelCostActual>0`), `hasDailyRevenue` (vj_daily keys `${priorYear}-` ODER `rec.year===priorYear`, `actualRevenue>0`). Abgeleitet: `hasAnyRevenue`/`hasAnyCosts`/`hasAnyData`; `status` `'none'|'revenue_only'|'ok'` + dt. `message` (Jahr nur in `'none'`). `none`→amber Banner „Keine Vorjahresdaten für {Jahr} vorhanden. Bitte Vorjahres-Kontoblätter oder Vorjahreswerte importieren."; `revenue_only`→blaues Banner „Vorjahresumsatz vorhanden, aber keine Vorjahreskosten vorhanden."; `ok`→kein Banner. Wiring in `PLView.tsx`: `priorYearDiag` useMemo nach `prevYearRecords`, Banner ganz oben im Content (nach `<StichtagBanner/>`, in allen Modi sichtbar). Verändert KEINE Berechnungen/Spalten. Tests: `src/lib/__tests__/pl-prior-year-diagnostics.test.ts` (node env, 10 synthetische — none/revenue-only/voll, daily-only, gemischte Kosten, Fremdjahr/0-Werte ignoriert).
- **Budgeting:** `src/types/budget.ts`, `src/lib/budget-store.ts`, `src/pages/Budget.tsx`
- **Product & Inventory:** `src/pages/Artikel.tsx`, `src/lib/artikel-store.ts`, `src/pages/ProduktStamm.tsx`, `src/lib/produkt-stamm-db.ts`, `src/pages/WesAnalyse.tsx`, `src/pages/ArtikelTracking.tsx`, `src/lib/artikel-tracking-store.ts`
- **Produkt-Analyse (Rangliste + Drill-down):** `src/pages/ProduktAnalyse.tsx` (`/produkt-analyse`, ranking) → clickable rows open `src/pages/ProduktDetail.tsx` (`/produkt-analyse/produkt`, single product). Pure logic in `src/lib/product-analytics.ts` (NO supabase/DOM, only `import type` from `sales-db`): `PeriodSelection` discriminated union **Day/Week/Month/Year** (replaces the old "Mehrere Monate"/"Kumuliert" modes; room for range/comparison later), ISO-week helpers (`isoWeekInfo`/`isoWeekStart`/`isoWeeksInYear` — handles 52/53-week cross-year, `shiftIsoWeek(year,week,delta)` for prev/next week jumps that roll across year/53-week boundaries), `periodBounds`/`periodLabel`/`weekRangeLabel(year,week)` (compact nav label "KW 26 · 22.06.–28.06.2026"; shows the start-year only on cross-year weeks), `categoryOf`/`matchesCategory` (`SOURCE_CATEGORY` food/beverage), `filterRows`, `aggregateProducts` (ranking), `aggregateProductsByWeekday(rows,year,week)` (per-product Mo–So buckets + totals for the weekly ranking; only rows whose `sale_date` falls in the 7 ISO-week days; emits `ProductWeekAggregate{days:WeekdayBucket[7],total_revenue,total_qty}`), `buildBreakdown` (single product → month=row/day, week=Mon–So, year=Jan–Dez, day=per-source entries [product_sales has no hourly data]; returns rows w/ `share`+`avgPrice`, `totalRevenue`/`totalQty`/`salesCount`/`avgRevenuePerSale`), and `filtersToParams`/`filtersFromParams` for URL round-trip of `{period,category,metric}`. Ranking mirrors filters to the URL (`useSearchParams`) so the detail page is refresh-safe and the back button restores context (incl. metric); keeps sort/Top10-20-All/Flop20/category/hide + **inline product search & column sort only (min/max Umsatz/Anzahl inputs removed)**. **Week selection is clamped via `Math.min(week, isoWeeksInYear(year))`** so an invalid KW53 never reaches the filter/URL. **Weekly view UX:** the KW dropdown is replaced by prev/next arrows + the exact `weekRangeLabel` range + "Heute"/"Diese Woche" quick buttons (both jump to the current ISO week; year dropdown kept). In week mode the ranking renders `WeekRankTable` (weekday columns Mo–So with date sub-labels + Total; each `WeekCell` shows revenue on top and qty ("… Stk.") smaller underneath, emphasizing qty when sorting by Anzahl; tfoot column totals + "–" for empty days); other periods keep the original `RankTable`. The page's `weekAgg`/`weekDayDates` memos derive from the SAME `filterRows(...)` scope as the ranking so per-product totals match the weekday-bucket sums and header dates align with `agg.days`. Detail shows KPIs (Gesamtumsatz/Gesamtanzahl/Ø Umsatz pro Verkauf) + breakdown table (Datum/Periode · Anzahl · Umsatz · % vom Produkt-Total · Ø Preis) with a Umsatz/Anzahl toggle (drives the %-column). Loads via existing `loadProductSalesRows` (`product_sales` has no `restaurant_id` — tenant posture is boundary-faithful pure functions only; no schema/migration change). Tests: `src/lib/__tests__/product-analytics.test.ts` (node env, 37 synthetic tests — monthly/weekly/yearly/day, cross-year ISO weeks, empty data, URL round-trip, scope isolation, `weekRangeLabel`/`shiftIsoWeek` rollovers, `aggregateProductsByWeekday` bucket placement/totals/out-of-week exclusion).
- **Schedule Planner:** `src/pages/SchedulePlanner.tsx`, `src/lib/pattern-warnings.ts`
- **Überstundenkosten Festangestellte (PersonalFix `/personal-fix`):** Pure `src/lib/overtime-analysis.ts` (KEIN React/supabase/DOM; nur `import type` von `personnel` + `getEffectiveHourlyRate` aus `employee-rate`; date-fns `getISOWeek`/`getISOWeekYear`). Überstunden NUR für Festangestellte (`isFixedSalaryEmployee` = `employmentType` `vollzeit|teilzeit`, BEWUSST NICHT an `monthlySalary>0` gekoppelt → fehlender Satz zeigt Stunden, Kosten=null). Schwellen `DAILY_OVERTIME_THRESHOLD=8.4`/`WEEKLY_OVERTIME_THRESHOLD=42`. `computeOvertimeAnalysis({employees,entries,departmentFilter?})`: gruppiert produktive Ist-Stunden pro (MA, ISO-Woche, Tag); pro Woche `dailyOvertimeSum=Σ max(0,dayH-8.4)`, `weeklyOvertime=max(0,weekH-42)`, **`effectiveOvertime=max(dailyOvertimeSum,weeklyOvertime)` (KEINE Doppelzählung)**; `overtimeHours=Σ Wochen`, `cost=effectiveOvertime×rate` (null wenn Satz fehlt). **Abwesenheiten ausgeschlossen:** Einträge mit `OvertimeHoursEntry.absenceType` (FE/K/U/…) werden übersprungen — sonst drücken Ferien-/Kranktage über die 42h-Woche und erzeugen falsche Überstunden (gleiche Semantik wie Grid: produktiv = `hours>0 && !absenceType`). `round2` überall; Stündliche (minijob/aushilfe) ignoriert; Abteilungsfilter respektiert. `computeOvertimeTotals(regularCost,overtimeCost,netRevenue,includeOvertime)` → regulär/ÜS/Total exkl./inkl. + lokal neu berechnete PKQ (`safeQuote` null bei Umsatz≤0). **Rate-Extraktion:** `getEffectiveHourlyRate`+`getWageLabel` wohnen jetzt in pure `src/lib/employee-rate.ts` (socialCostFactor-Literal 1.13 beibehalten); `ActualHoursGrid.tsx` importiert + re-exportiert sie (bestehende Importe von PersonalFix/IstDayDetailDialog bleiben gültig). **UI:** eigenständige `src/components/personal-fix/OvertimeCostCard.tsx` (Card/Switch/Table/Badge; Toggle „Überstundenkosten einbeziehen" steuert Total+PKQ; aufklappbare Wochen-/Tages-Aufschlüsselung). **Berechtigungen (defense-in-depth, obwohl PersonalFix nur admin/beaulieu_manager):** individuelle Kosten/Sätze nur bei `canSeeHourlyWages`, aggregierte Kosten/PKQ nur bei `canSeePersonnelCostTotals`; Überstunden-STUNDEN sind keine Lohndaten → immer sichtbar. **Wiring in `PersonalFix.tsx`:** `overtimeAnalysis` useMemo baut `entries` aus `supabaseActualHours` des gewählten Monats (überspringt `absenceType` + `hours≤0`), Kandidaten = `isEmployeeActiveInMonth`, `departmentFilter:'all'`; Karte nach dem KPI-Grid; `regularCost={pfix.active.istTotal}`, `netRevenue={effectiveRevenue}`. **SELBSTSTÄNDIGE Karte — fädelt ÜS NICHT in die bestehenden Seiten-Totale ein** (lokaler PKQ-Recompute). **Bekannte, dokumentierte Grenzen (Card-Fussnote):** (1) Wochen am Monatsrand nur mit geladenen Tagen; (2) Überschneidung möglich mit manuell markierten Zusatzkosten (`isAdditionalCost`), da `pfix.active.istTotal` `zusatzIstCHF` bereits enthält — ÜS sind eine separate kalkulatorische Auswertung. KEINE Migration/DB-/Import-Änderung. Tests: `src/lib/__tests__/overtime-analysis.test.ts` (node env, 15 — Tages-/Wochen-ÜS, max-keine-Doppelzählung, Abwesenheiten ignoriert [Tag+Woche], stündlich ausgeschlossen, fehlender Satz, Abteilungsfilter, Toggle inkl./exkl., ISO-Cross-Year, Rundung).
- **Küchen-Manager Tages-Header-Totale (lohn-sicher):** Pure `src/lib/schedule-daily-totals.ts` (KEIN React/supabase/DOM) liefert die Per-Tag-Spaltenkopf-Totale für den `kueche_manager` im Wochen-Dienstplan (`ModernScheduleGrid`-Header). `dailyTotalsVisibility('full'|'manager')` → `{showHours,showRevenue,showRatio,showTargetIndicator,showCostChf,showIndividualWages}`; **'full' (admin/service/beaulieu) = nur `showHours`** → Verhalten UNVERÄNDERT; 'manager' = Stunden+Umsatz+Quote+Zielindikator, `showCostChf`/`showIndividualWages` IMMER false. `computeDailyLaborRatio(cost,revenue,thresholdPercent)` → `{ratio: cost/revenue*100 (null bei revenue≤0), isOverTarget: ratio>threshold}` (gleiche Einheiten wie die Wochen-/Monats-PKQ-Karten). `computeDailyKitchenTotals(emps:EmployeeDayInput[],revenue,thresholdPercent,daysInMonth)` → `DailyKitchenTotals{plannedHours,personnelCost(CHF),revenue,laborCostRatio,isOverTarget}`; Kostenmodell spiegelt `weeklyPlannedLaborCost`: Festangestellte (vollzeit/teilzeit mit `monthlySalary`>0) → `monthlySalary/daysInMonth`, Stündliche → `(dayHours+dayAbsenceHours)*hourlyWage`. **`toDailyTotalsDisplay()` strippt `personnelCost`** → grid-seitige `DailyTotalsDisplay{plannedHours,revenue,laborCostRatio,isOverTarget}`, d.h. **kein CHF-Lohnbetrag erreicht je die Grid-Props/DOM**. Verdrahtung: `SchedulePlanner.tsx` `dailyManagerTotals` useMemo (gibt `{}` zurück außer `isKuecheManager`; iteriert `displayDays`×`visibleEmployees` — gleiche kitchen-scoped Basis wie die PKQ-Karten, NICHT das row-gefilterte `displayEmployees`; `revenue=dailyBudgets[date].plannedRevenue`, `threshold=laborCostThreshold`) → Props `managerSafeTotals={isKuecheManager}`+`dailyManagerTotals` an `ModernScheduleGrid`. `ModernScheduleGrid.tsx` Header-Block: `managerSafeTotals` → Manager-Block (Netto-Stunden, „CHF {revenue}", Quote% emerald/rot + `AlertTriangle` bei over-target, „–" wenn `ratio==null`), sonst die UNVERÄNDERTE Brutto-`dayTotals`-Zeile. Nur `kueche_manager` betroffen (admin+service unverändert), keine Migration/DB-Änderung. **Bekannte Grenze (nicht im Scope):** Mitarbeiter-Objekte mit `hourlyWage`/`monthlySalary` erreichen den Grid bereits für alle Rollen (Pre-existing Gap, kein NEUER Leak hier; echte Lohn-Vertraulichkeit braucht später eine Data-Layer/Projektion). Tests: `src/lib/__tests__/schedule-daily-totals.test.ts` (node env, 8 — Stunden/Umsatz sichtbar, keine Löhne/CHF [Display ohne `personnelCost`], nur Küchen-Mitarbeiter via `getVisibleEmployeesForRole`, admin 'full' unverändert, Quote-Edgecases revenue=0/over-target/Festangestellten-Proration).
- **Dienstplan-Export Abteilungsfilter (abteilungstreu):** Pure `src/lib/schedule-export-department.ts` (`selectEmployeesForDepartment(employees, dept)`, KEIN supabase/DOM; re-exportiert von `schedule-export-import.ts`, alle Export-Pfade routen darüber) filtert Mitarbeiter strikt nach Abteilung (`'all'`=alle, sonst exakte Abteilung) — behebt den Bug, dass der Küchen-Export auch Service enthielt. `ExportOptionsDialog` nimmt `activeDepartment` (setzt die Abteilung beim Öffnen auf die aktive) + `lockDepartment`; `SchedulePlanner` übergibt `lockDepartment={!canSwitchDepartment}`, sodass eingeschränkte Manager (Küche/Service) die Abteilung im Export nicht wechseln können. Tests: `src/lib/__tests__/schedule-export-department.test.ts` (node env, 5 Tests — Küche/Service/all + Leerfälle).
- **Manager-Mitarbeiter-Sichtbarkeit (zentrale Quelle der Wahrheit):** Pure `src/lib/employee-visibility.ts` (KEIN supabase/DOM; delegiert an `selectEmployeesForDepartment`) ist die EINZIGE Stelle, die entscheidet, welche Mitarbeiter eine Rolle sehen/auswählen/exportieren/zählen darf. Exporte: `departmentForRole(role)` (`kueche_manager`→`'küche'`, `service_manager`→`'service'`, sonst `null`), `effectiveEmployeeDepartmentScope(role, allowedDepartment)` (eingeschränkte Rolle GEWINNT über `allowedDepartment` = defense-in-depth; admin/beaulieu folgen `allowedDepartment`='all'), `getVisibleEmployeesForRole(role, allowedDepartment, employees)`. **Invariante:** admin/`beaulieu_manager` (`allowedDepartment`='all') → unveränderte Liste (No-op). Verdrahtung in `SchedulePlanner.tsx`: `roleScopedEmployees = useMemo(getVisibleEmployeesForRole(...))` direkt nach dem `employees`-State; `activeEmployees`-Basis + ALLE Anzeige-/Auswahl-/Zähl-/Export-Pfade (paFixCost, varHoursExceeded, detectPatternWarnings, PDF/Template-Export, publish `targetEmps`+`publishDiffAll`, StaffingStatusBar, ActualHoursImportButton, EmployeeHoursSummary, kosten-gegatete „Mitarbeiter (N)"-Tabelle, „Alle (N)"-Zähler, Publish-Personenauswahl, sämtliche Dialoge) lesen `roleScopedEmployees`; `activeDepartment`-Init + Rollen-Coercion-Effekt nutzen `effectiveEmployeeDepartmentScope`. In `Personalstamm.tsx` routet `visibleBase` durch `getVisibleEmployeesForRole` (pending_review bleibt separat ausgeblendet); Neuanlage-Default-Abteilung + gesperrter Abteilungs-Select unverändert. **Bewusst RAW (`employees`) gelassen:** localStorage Laden/Speichern/Merge, Mitarbeiter-CRUD, und der Import-MATCH (`importScheduleFromExcelV2`/`matchEmployeeByName`) — Matching MUSS die volle Liste sehen, sonst werden Dubletten angelegt. **Bekannte Grenzen (nicht im Scope dieses Fixes):** (1) `AuthContext` startet `role='admin'` und stuft asynchron auf `kueche_manager` herab → kurzer Render-Frame, in dem die volle Liste sichtbar ist (dokumentierte Auth-Bootstrap-Entscheidung); (2) der Import-PREVIEW/-Write-Pfad ist abteilungsübergreifend (Match läuft bewusst gegen die volle Liste). Tests: `src/lib/__tests__/employee-visibility.test.ts` (node env, 12 synthetische — küche/service/admin/beaulieu, Rolle gewinnt bei `allowedDepartment`='all', Export-Scope nur Küche, keine Mutation).
- **Schicht-Schnellauswahl Farbe ändern (ShiftLegend):** In `src/components/schedule-planner/ShiftLegend.tsx` hat jede Schichtzeile (Arbeits- UND Abwesenheits-Schichten) im Hover-Overlay einen Paletten-Button, der ein `SIDEBAR_COLORS`-Swatch-Panel umschaltet; Auswahl ruft `updateShifts` mit neuer `color` und geleerten `excelColor`/`textColor` auf, die via `getExcelColorsFromTailwind(color)` in `useShiftConfig.ts` neu abgeleitet werden.
- **CSV/PDF Import:** `src/lib/csv-import-engine.ts`, `src/lib/pdf-import-engine.ts`, `src/pages/CSVImport.tsx`
- **Gastronovi Produkt-CSV-Import (Anzahl+Umsatz → `product_sales`):** `src/pages/SalesUpload.tsx` + pure `src/lib/gastronovi-csv-parser.ts`. `matchAnzahlUmsatz` aggregates BOTH the Anzahl and Umsatz files **per normalized product name, summing day values per date** (`aggregateRowsByName`), emitting exactly ONE record per `(product, sale_date)` — fixes the old `Map.set` last-write-wins (dropped revenue) + per-line duplicate emission (inflated qty / double-counted revenue). `MatchResult.duplicateProducts` lists merged products (blue info banner in the preview). Same-name-different-price articles get summed (flat CSV has no article number — inherent limit). Tests: `src/lib/__tests__/gastronovi-csv-parser.test.ts` (node env, synthetic).
- **Supplier Documents:** `src/types/supplier-documents.ts`, `src/lib/supplier-documents-store.ts`, `src/pages/SupplierDocuments.tsx`, `src/pages/SupplierComparison.tsx`
- **Onboarding Flow:** `src/pages/OnboardingPage.tsx`, `src/components/onboarding/`
- **Reservations Import (Foratable):** `src/pages/ReservationenImportPage.tsx` (embeddable tab of Foratable Import — accepts `embedded?:boolean`; `/reservationen-import` redirects to `/foratable-import`), `src/lib/reservation-import-parser.ts`, `src/lib/reservation-analytics.ts`, `src/lib/reservation-import-db.ts`, `supabase/migrations/20260621_reservations.sql` (tables `reservation_imports`, `guest_profiles`, `reservation_records`). Separate from Gastronovi (`gn_*`) imports. The stats core is the pure `computeReservationStats(rows: ReservationStatsInput[])` (extracted in the parser); `computeStats(ParsedReservation[])` delegates to it. The summary UI (Kacheln + Hinweise + 4 Cards) lives in the shared `src/components/reservations/ReservationSummary.tsx` (exports `ReservationSummary`, `Tile`, `NUM0`/`NUM1`, `fdate`, `STATUS_LABEL`/`STATUS_CLASS`), reused by both the import preview and the Foratable Report. **Idempotency:** the import is keyed on `Res.Nr.` via the UNIQUE constraint `reservation_records_restaurant_extid_uq (restaurant_id, external_reservation_id)` + `.upsert(..., { onConflict })`, so re-importing updates instead of duplicating; in-CSV duplicate Res.Nr. are deduped last-row-wins (`dedupeReservationsByExternalId`). `saveReservationImport` returns a split stat — `inserted`/`updated` (computed via a read-before-write of existing conflict keys, since upsert can't report it), `duplicateKeyMerged`, `skippedRows` (rows without Res.Nr., tagged `kind:'skipped'` by the parser). Defensive standalone constraint migration: `supabase/migrations/20260623_reservation_records_unique_constraint.sql` (idempotent; dedups then `ADD CONSTRAINT IF NOT EXISTS`). DB-layer idempotency tests: `src/lib/__tests__/reservation-import-db.test.ts` (in-memory Supabase mock).
- **Foratable Report:** `src/pages/ForatableReportPage.tsx` (admin-only `/foratable-report`, gated `isAdmin && !isGuest`; nav item "Foratable Report" in the Foratable group). Read-only KPI report over ALREADY-imported `reservation_records` for a Von/Bis range (+ quick ranges current/last month, current/last year). Pure `src/lib/foratable-report.ts` (`ReportRange`, `ForatableReport`, `splitNewReturningGuests`, `buildForatableReport`, `quickRange(kind,today)`) reuses `computeReservationStats`; read-only `src/lib/foratable-report-db.ts` (`fetchReservationReportRows`/`fetchPriorGuestKeys`/`loadForatableReport` — paginated, tenant-filtered via `restaurant_id`, throws on error). New/returning = guest had any reservation strictly before `from` (`fetchPriorGuestKeys`). Renders the shared `ReservationSummary`; optional jsPDF+autoTable export + `window.print()`. The fetch effect/callback are guarded so no DB read happens before the `Navigate` redirect for non-admin/guest sessions. No import-logic change, no migration, Gästeexport Import untouched. Tests: `src/lib/__tests__/foratable-report.test.ts` (node env, synthetic).
- **Foratable Report „Beste Reservationszeiten":** Pure `src/lib/reservation-time-analysis.ts` (NO supabase/DOM, only `import type` from `reservation-import-parser`) groups reservations by EXACT start time. `analyzeReservationTimes(rows)` → `ReservationTimeAnalysis{slots:TimeSlotStat[],totalReservations,totalPersons,ignoredNoTime,excludedByStatus}`; each `TimeSlotStat{time,count,persons,shareCount,sharePersons}` (shares 0..1 vs the FILTERED time-bearing totals → sum ~100%), default sorted count desc / tie-break time asc. **Status policy:** `MEANINGFUL_TIME_STATUSES`=`['completed','confirmed','pending','unknown']`, `EXCLUDED_TIME_STATUSES`=`['cancelled','noshow']` (`isMeaningfulTimeStatus`) — this DELIBERATELY differs from `computeReservationStats.topTimes` (which counts ALL statuses); „Beste Reservationszeiten" = realized demand, so Stornos/No-Shows are dropped. Rows without a time are excluded from `slots` but counted in `ignoredNoTime`. Helpers `sortTimeSlots(slots,'count'|'persons')` (`TimeSortKey`, non-mutating) + `topTimeSlots(slots,limit)` (limit≤0→all). Wired by adding a required `timeAnalysis:ReservationTimeAnalysis` to `ForatableReport`, computed in `buildForatableReport` (DB layer untouched; rows already tenant-scoped). UI lives in `ForatableReportPage.tsx`: a „Beste Reservationszeiten" `<section>` (sort toggle Reservationen/Personen, Top10/„Alle Zeiten" toggle shown only when >10 slots, table Zeit|Reservationen|Personen|%Res.|%Pers., per-row bar viz relative to the max of the active metric, strongest row highlighted with `bg-primary/5`+„Spitze" badge, note „ohne Stornos & No-Shows" + `ignoredNoTime`). `displayedTimeSlots`/`maxTimeMetric` `useMemo` sits BEFORE the non-admin `Navigate` early return (no conditional-hook bug); `showAllTimes` resets to false on each new report load. The shared `ReservationSummary` gained an additive `hideTopTimes?:boolean` (default false → import preview unchanged) and the report passes it to suppress the duplicate „Häufigste Zeiten" card. PDF „Top-Zeiten" table now uses `timeAnalysis` (top10, with %-columns) instead of the legacy all-status `stats.topTimes`. Tenant isolation N/A in the pure layer (rows pre-scoped by `restaurant_id` in `fetchReservationReportRows`). Tests: `src/lib/__tests__/reservation-time-analysis.test.ts` (node env, 13 synthetic — grouping, sort by count/persons, status exclusion, empty, shares sum, null-time ignored, topN) + a `buildForatableReport` `timeAnalysis` assertion in `foratable-report.test.ts`.
- **Gästeexport Import (Foratable → CRM-Anreicherung):** `src/pages/GaesteImportPage.tsx` (admin-only, embeddable `embedded?:boolean` tab of Foratable Import at `/foratable-import?tab=gaeste`; `/gaeste-import` redirects there; wizard upload→preview→commit; gated `isAdmin && !isGuest`), pure libs `src/lib/foratable-guest-import-parser.ts` (header-based parse, Ja/Nein→bool, Geburtsdatum→yyyy-MM-dd, normalized email/phone/name + aggregate stats) and `src/lib/foratable-guest-import.ts` (`buildGuestIndex`/`matchGuestRow` email→phone→name with ambiguity flagging, `rowToIncomingCrm`, `mergeCrmProfile` fill-empty-only, `planGuestImport`), orchestration `src/lib/foratable-guest-import-db.ts` (`previewForatableGuestImport`/`commitForatableGuestImport` — AUTHORITATIVE: fresh `fetchGuestProfilesStrict`+re-plan each call, tenant-filtered bulk upsert ONLY into `guest_crm_profiles`, throws on write error). **Scope guard:** only ENRICHES empty manual CRM fields of existing guests (VIP→vipManual, Newsletter→newsletterOptIn, Blacklist→blockedGuest [positive token only], Geburtsdatum→birthday, Firma→company, Sitzplatz→favoriteTable, Gästeinfo+Speisen+Labels→crmNotes); never overwrites non-empty values (differ→keep+count conflict); never touches `reservation_records`/`guest_profiles`/`guest_statistics`/visit counts/segments. No migration. Tests: `src/lib/__tests__/foratable-guest-import-parser.test.ts` + `foratable-guest-import.test.ts` (node env, synthetic, no PII).
- **Import-Center (zentrale Karten-Übersicht aller Importe):** `/import` = die bestehende `src/pages/ImportHub.tsx`. **Zugriff:** erreichbar für admin + `beaulieu_manager` + `beaulieu_viewer` (Gäste ausgeschlossen); Gate ist Single Source of Truth `if (visibleCategories({isAdmin,isGuest,isBeaulieuManager,isBeaulieuViewer}).length===0) return <Navigate to="/personal" replace/>` (GLEICHE Funktion wie das Grid → Nav/Seite/Karten immer kohärent). Die Inline-Sektionen unter dem Grid sind admin-only und in `{showAdminSections && (…)}` (`showAdminSections=isAdmin&&!isGuest`) gewrappt → Nicht-Admins (Beaulieu) sehen NUR das Karten-Grid. Ganz oben im `<main>` rendert sie `<ImportCenterGrid/>` (aus `src/components/import-center/ImportCenterGrid.tsx`); alle bestehenden Inline-Sektionen + `DatenstandCard` bleiben darunter erhalten. Das Grid zeigt eine Karte pro Import-Kategorie (Name/Beschreibung/Status-Badge/„Zuletzt: …"-Zeitstempel/Checklist-Häkchen/Aktions-Button). **Launcher-Modell, KEIN Embedding/Rewrite:** Karten mit `kind:'route'` navigieren via `<Link>` zur bestehenden Route, Karten mit `kind:'inline'` (Mitarbeitende→Anchor `ist-stunden`, Vorjahreswerte→Anchor `umsatz-vorjahr`) scrollen per `scrollIntoView` zu der bereits existierenden `Section` auf DERSELBEN Seite. Pure Logik in `src/lib/import-center.ts` (KEIN React/supabase/DOM): `IMPORT_CATEGORIES` (9 Deskriptoren in fixer Reihenfolge: foratable→`/foratable-import`, gastronovi→`/gastronovi-import`, produktumsaetze (Label „Verkaufsdaten Upload", `/sales-upload` = die EINZIGE Verkaufsdaten-Upload-UI; Beschreibung „Verwendet für Verkaufsdashboard, Tagesansicht, Produktanalyse, Forecast und Umsatzauswertungen.")→`/sales-upload`, warenrechnungen→`/warenrechnungen`, wes→`/wes-analyse`, erfolgsrechnung→`/csv-import`, budget→`/budget`, mitarbeitende→inline, vorjahreswerte→inline), `ImportAccessContext{isAdmin,isGuest,isBeaulieuManager,isBeaulieuViewer}` + pro Karte `canAccess` (spiegelt die ECHTEN Route-Guards: ALLE Prädikate beginnen mit `!isGuest` [Gäste = 0 Karten]; danach adminOnly / adminOrBeaulieu / warenAccess[+viewer]). Sichtbarkeit: admin=9, `beaulieu_manager`=3 (produktumsaetze/warenrechnungen/wes), `beaulieu_viewer`=1 (warenrechnungen), Gast=0, none=0. Die 2 Inline-Karten (mitarbeitende/vorjahreswerte) sind admin-only, weil ihre Ziel-Sektionen für Beaulieu ausgeblendet sind, `visibleCategories`/`getCategory`, sowie die Status-State-Machine `deriveStatusFromRun`/`deriveStatusFromTimestamp`/`pickLatestRun` + `EMPTY_STATUS`/`UNKNOWN_STATUS`/`STATUS_LABEL`/`STATUS_BADGE_CLASS` (States `none`=„Nicht gestartet" / `imported`=„Importiert" / `attention`=„Achtung" / `unknown`=„Kein Protokoll"). **Invariante: niemals einen Zeitstempel erfinden** — ohne echtes Signal `lastImportedAt:null`. Read-only Status-Aggregator `src/lib/import-center-db.ts` (`loadImportCenterStatuses(restaurantId, allowedSources: ReadonlySet<ImportStatusSource>)` — liest NUR die Status-Quellen der SICHTBAREN Karten; eine Rolle ohne Foratable/Gastronovi/Produktumsatz-Karte triggert deren Read NICHT [Least-Privilege]. Das Grid leitet `allowedSources` aus den sichtbaren `cats` ab, mit stabilem `sourcesKey`-Effect-Dep gegen Refetch-Loop): `Promise.allSettled` über `fetchLatestImportRuns` (Foratable, neuester von reservations+guest_export), `loadGnImports` (Gastronovi, max `imported_at`) und `fetchImportBatches` (Produktumsätze, [0].imported_at) — pro Kategorie degradiert ein Fehler isoliert zu `unknown`. Kategorien mit `statusSource:'none'` (Warenrechnungen/WES/Erfolgsrechnung/Budget/Mitarbeitende/Vorjahreswerte) haben kein zentrales Protokoll → „Kein Protokoll". Operator-Anzeige (`created_by`) wird NUR gezeigt, wenn sie nicht UUID-förmig ist (UUIDs = Rauschen → ausgeblendet). **Nav-Konsolidierung (`AppNav.tsx`):** EIN Item „Import"→`/import` in der Import-Gruppe (`adminOnly`+`beaulieuAllowed`+`beaulieuViewerAllowed`+`hideForGuest`; `NavItem` bekam die Flags `beaulieuViewerAllowed`/`hideForGuest`, BEIDE `isItemVisible`-Funktionen blenden `hideForGuest&&isGuest` aus und admittieren `beaulieu_viewer` via `beaulieuViewerAllowed`; Gruppen-`adminOnly` ist nur Metadaten — Filterung läuft rein über `isItemVisible`); die verstreuten Links „Foratable Import", „Gastronovi Z-Bericht" und „Verkaufsdaten Upload" wurden ENTFERNT — alle Routen existieren weiter (Karten-Ziele), keine neuen Redirects. Tests: `src/lib/__tests__/import-center.test.ts` (node env, 18 — admin=9/beaulieu_manager=3/beaulieu_viewer=1/Gast=0/none=0 Sichtbarkeit, route-vs-inline-Klassifikation, Routen = echte App-Pfade, Status-Ableitung inkl. failed→attention, „kein erfundener Zeitstempel"-Invariante, `pickLatestRun`, Label/Badge-Vollständigkeit) + `src/lib/__tests__/import-center-db.test.ts` (node env, 6 — `loadImportCenterStatuses` liest NUR erlaubte Quellen: none/leer→0 Reads, manager→nur `product_batches`, admin→alle, tenant-id-Weitergabe, Key-Scoping; Supabase via `vi.mock`).
- **Foratable Import (Sammelseite):** `src/pages/ForatableImportPage.tsx` (admin-only `/foratable-import`, gated `!isAdmin||isGuest`→`Navigate '/'`; single nav item "Foratable Import" in the Foratable group, icon `Upload`, replaces the former "Reservationen" + "Gästeexport Import" items). Two tabs reuse the existing pages via the `embedded` prop (suppresses each page's own header/back-link/outer max-width wrapper; banners + wizard bodies kept). Active tab is controlled by `?tab=` (`reservationen` default | `gaeste`); both `TabsContent` use `forceMount` + `hidden` so each page's upload/wizard state survives tab switches. Old routes redirect (`/reservationen-import`→`/foratable-import`, `/gaeste-import`→`/foratable-import?tab=gaeste`); the Import-Zentrale (`ImportHub.tsx`) and Foratable-Report links repoint accordingly. **Foratable Report** stays a separate route/nav item. The page also shows two **„Letzter Import"-Karten** (Reservationen / Gästeexport, DB-Zeitstempel) and a third tab **„Import-Verlauf"** (`?tab=verlauf`) listing recent runs newest-first with an Alle/Reservationen/Gästeexport filter.
- **Import-Historie (gemeinsam, beide Foratable-Importe):** Both import flows log every run into ONE shared table `import_runs` (`supabase/migrations/20260623_import_runs.sql` — RLS authenticated-only/anon-revoked, self-test, mirrors `20260621_reservations.sql`; run manually in SQL editor). Columns: `restaurant_id`, `import_type` (`reservations`|`guest_export`), `file_name`, `status` (`success`|`failed`), `record_count`, `period_from`/`period_to`, `error_message`, `stats_json` (jsonb), `created_by` (operator **UUID only — no email/PII**), `started_at`, `finished_at` (**DB `DEFAULT now()` — authoritative timestamp, never browser time**), `created_at`. Pure logic in `src/lib/import-runs.ts` (`ImportRunType`/`ImportRunStatus`/`ImportRunStats`/`ImportRunRow`, `buildReservationRunStats`, `buildGuestRunStats` [skipped=unassignable+errorRows], `latestRunByType`, `rowToImportRun`, `runStatChips`, `IMPORT_TYPE_LABEL`; NO supabase import). DB layer `src/lib/import-runs-db.ts` (`logImportRun` — **best-effort, NEVER throws**, resolves operator via `supabase.auth.getSession()`; `fetchImportRuns({type?,limit?})`, `fetchLatestImportRuns` [two type-filtered `limit:1` queries], `checkImportRunsTableExist`). Wired at the page call sites ONLY — `saveReservationImport`/`commitForatableGuestImport` are UNCHANGED; pages capture `startedAt`, `await logImportRun` on success then refresh, log a `failed` run in the error/catch path. Both embedded pages gained an optional `onImported?: () => void` prop so the parent refreshes its indicators. No guest PII ever stored (only aggregate counts + filename). Tests: `src/lib/__tests__/import-runs.test.ts` (node env, synthetic).
- **Gäste-CRM:** `src/pages/GaesteCrmPage.tsx` (list+search+segments), `src/pages/GaesteDetailPage.tsx` (full per-guest Kundenakte — see "Gäste Detailseite" below), `src/lib/reservation-crm.ts` (segment/metric logic + `guestListMetricsFromDetail` adapter, single source of truth; also exports the **separate** `simpleGuestStatus(visits)` → `SimpleGuestStatus` `new`/`returning`/`regular`/`vip` with `SIMPLE_STATUS_*` consts + `SIMPLE_STATUS_LABEL` — Neu=1 / Wiederkehrend=2–3 / Regelmässig=4–9 / VIP≥10, null at 0; **purely visit-count based, never fed into `classifySegment`/`computeCrmScore`** — revenue-based tiers are impossible since `reservation_records` has no monetary column), `src/lib/reservation-crm-db.ts` (read-only CRM reads; `fetchGuestReservations` is paginated; tenant-isolation tests in `src/lib/__tests__/reservation-crm-db.test.ts`). Metrics/segments are computed live in TS over `guest_profiles`/`reservation_records` (no dedicated tables). Optional read-only SQL view `guest_statistics` in `supabase/migrations/20260622_guest_statistics_view.sql` mirrors the exact `reservation-crm.ts` semantics for direct Supabase querying.
- **Manuelle CRM-Daten in der Gäste-Liste (Phase 1.2):** Manual `guest_crm_profiles` data is surfaced in `/gaeste` and fed into the Kampagnen WITHOUT touching the auto-computed segment/score. Plumbing: `GuestListMetrics` carries an optional `crm?: GuestCrmProfile | null` (set via `guestListMetrics(..., crm)` 5th param; defaults to `null`) — purely additive, never read by `classifySegment`/`computeCrmScore`. Bulk read `fetchGuestCrmProfilesByIds(guestIds)` in `src/lib/guest-crm-profile-db.ts` (chunked `.in`, throws on error; tenant-safe only because callers pass tenant-scoped IDs from `fetchGuestProfiles`). Pages (`GaesteCrmPage`, `CrmAuswertungPage`) load it in a try/catch → on failure show an amber warning banner and fall back to an empty map (manual filters/badges/campaigns simply stay empty). Filter/sort logic in `src/lib/guest-list-filters.ts`: `BoolFilter` (`alle`/`ja`/`nein`), 8 bool fields (`vipManual`/`stammgastManual`/`companyCustomer`/`newsletter`/`blocked`/`hasAllergies`/`hasBirthday`/`hasCrmNote`) via `matchBool`+`crmFlags`, shared `BOOL_FILTER_OPTIONS`, plus sort keys `birthday`/`company`. Defensive: missing CRM → all flags false / dates+text null.
- **Gäste-CRM Smart-Segmente (dynamisch, mehrfach-zuordenbar):** Pure module `src/lib/guest-smart-segments.ts` (NO supabase/DOM) derives 7 LIVE, MULTI-membership segments straight from the already-loaded `GuestListMetrics` (+ optional `m.crm`) — **strictly additive**, never fed into `classifySegment`/`computeCrmScore`, no new query, no migration. A guest can be in several at once (e.g. VIP+Gefährdet+Verloren). Segments: `vip` (visits≥`SIMPLE_STATUS_VIP_MIN`=10), `regular` (≥`SIMPLE_STATUS_REGULAR_MIN`=4), `new` (visits≥1 & first visit ≤`NEW_GUEST_DAYS`=30d), `upcoming_birthday` (`m.crm.birthday` within next 30d, **year-agnostic** w/ wraparound, Feb29→Mar1 rollover), `never_returned` (visits===1), `at_risk` (visits≥1 & `daysSinceLastVisit`≥90), `lost` (visits≥1 & ≥180 — **overlaps `at_risk` by design**). Exports: `SmartSegment` type, `SMART_SEGMENT_ORDER`/`_LABEL`/`_DESCRIPTION`/`_SLUG`, threshold consts (`SMART_VIP_MIN`/`_REGULAR_MIN`/`_NEW_DAYS`/`_AT_RISK_DAYS`=90/`_LOST_DAYS`=180/`_BIRTHDAY_WINDOW_DAYS`=30), `daysUntilBirthday`/`isUpcomingBirthday` (UTC day-math), `SMART_SEGMENT_PREDICATES`, `hasSmartSegment`, `guestSmartSegments(m,today)` (in ORDER), `filterBySmartSegment` (filter+sort visits desc then name, non-mutating), `summarizeSmartSegments`, `SmartSegmentFilter`/`SMART_SEGMENT_FILTER_OPTIONS`, `smartSegmentExportTable(seg,rows)` (reuses tested `CSV_HEADERS`+`campaignRowToCells` from `reservation-campaigns.ts`; filename `crm-smart-segment-<slug>`). Shared badge UI `src/components/crm/SmartSegmentBadges.tsx` (`SmartSegmentBadge`+`SmartSegmentBadges`, icons Crown/Star/Sparkles/Cake/UserMinus/AlertTriangle/Ghost, `SMART_SEGMENT_ICON`). Wired in `GaesteCrmPage` (badges under each name cell; page-local `smartSegment` state + "Smart-Segment" `FilterSelect`; extra filter step on top of `searchAndFilterGuests` — `applyGuestFilters` deliberately UNCHANGED to avoid `today`-threading churn; included in `filtersActive`+reset; per-segment CSV export button when a segment is selected via `smartSegmentExportTable`+`downloadCsv`) and in `GaesteDetailPage` header (badges via `guestSmartSegments({...guestListMetricsFromDetail(profile,metrics), crm: crmSaved}, today)`). Tenant isolation inherited from the tenant-scoped `fetchGuestProfiles` (asserted in `reservation-crm-db.test.ts`). Tests: `src/lib/__tests__/guest-smart-segments.test.ts` (node env, synthetic, no PII — every rule at its boundaries, birthday wrap/Feb29, multi-membership, filter/summarize/export).
- **Gäste Detailseite (Kundenakte):** `src/pages/GaesteDetailPage.tsx` (admin-only `/gaeste/:guestId`) backed by the pure helper `src/lib/reservation-guest-profile.ts`. Six live-computed sections over `reservation_records` (read-only, single scrollable page of cards, mobile+desktop): **CRM-Score** (0–100), **Besuchsverhalten** (incl. a **Gast-Status** tile from `simpleGuestStatus` — separate from the computed segment), **Präferenzen** (incl. **Lieblingsmonat**), **Risiko**, **Besuchstrend (90d)** and a filterbare **Reservierungshistorie**. Helper exports: `computeGuestPreferences` (favorite area via `normalizeArea` → Restaurant/Terrasse/Bar buckets w/ raw fallback, favorite weekday/**month** (`favoriteMonth` via `monthIndex`+`MONTH_LABELS`)/time/party-size — completed visits only), `computeVisitTrend` (`TREND_STABLE_TOLERANCE=1`, non-overlapping inclusive windows [today−89,today] vs [today−179,today−90], completed-only), `filterReservationHistory` (`HistoryFilter` = alle/besuche/storniert/noshow), `computeCrmScore` (+`visitsScore`/`recencyScore`/`regularityScore`/`partySizeScore`/`scoreTier`, `CRM_SCORE_WEIGHTS`, constants), `totalPersonsOnVisits`, `mostFrequent`, `weekdayIndex`. **CRM-Score weighting** (documented in the helper): Besuchsanzahl 40% (full ≥20 visits) / Aktualität 25% (full ≤30d, 0 ≥180d) / Regelmässigkeit 20% (full Ø≤30d, 0 ≥180d, 0 if <2 visits) / Gruppengrösse 15% (full Ø≥6). Tiers: 0-39 niedrig / 40-69 mittel / 70-89 hoch / 90-100 VIP-Potenzial. Risk reuses `reservation-dashboard.ts` (`overdueByDays`/`isOverdue`/`isAtRiskTier`) via the `guestListMetricsFromDetail` adapter. Tests: `src/lib/__tests__/reservation-guest-profile.test.ts` (node env, synthetic, no PII). No migration/import/`guest_statistics`/Gastronovi changes. The page is now wrapped in a Tabs layout: **Übersicht** (these six read-only sections) and **CRM** (the editable manual profile — see "Gäste CRM-Profil (manuell, editierbar)" below).
- **Gäste CRM-Profil (manuell, editierbar):** New table `guest_crm_profiles` (`supabase/migrations/20260622_guest_crm_profiles.sql`), 1:1 with `guest_profiles` via `guest_id` UUID PK + FK `ON DELETE CASCADE`, **no** `restaurant_id` (tenant derived from the parent guest). RLS mirrors `20260621_reservations.sql` (authenticated full CRUD, anon REVOKE ALL, service_role full; self-test uses a temp `guest_profiles` row with `restaurant_id='__rls_selftest__'` then cascade-cleans up). Pure mapping/validation in `src/lib/guest-crm-profile.ts` (NO supabase import — `GuestCrmProfile`/`GuestCrmProfileRow`, `EMPTY_CRM_PROFILE`, `rowToCrmProfile`, `crmProfileToRow`, `normalizeCrmProfile` trims text→null & validates `birthday` yyyy-MM-dd, `isCrmProfileDirty`, plus `manualCrmBadges(profile)` → `ManualBadge[]` (`kind` `'vip'|'stammgast'`) driven SOLELY by `vipManual`/`stammgastManual`). Tenant-checked CRUD in `src/lib/guest-crm-profile-db.ts` (`fetchGuestCrmProfile`/`upsertGuestCrmProfile` both verify the guest belongs to the tenant via `fetchGuestById` first; write throws on error — no silent fallback). Editable **CRM** tab lives in `src/pages/GaesteDetailPage.tsx` (module-scope `FieldText`/`FieldArea`/`FieldToggle`/`ManualCrmBadge`/`CrmSummaryField` helpers; `crmSaved` vs `crmForm` dirty tracking; `canEditCrm = isAdmin && !isGuest`). Footer has TWO buttons: **Speichern** (saves all fields, disabled unless dirty) and **Änderungen verwerfen** (resets form to `crmSaved`, no DB write, disabled unless dirty). Manual VIP/Stammgast badges (`manualCrmBadges(crmSaved)`) render in the page header next to the computed `SegmentBadge` AND drive a read-only **CRM-Profil** summary card at the top of the Übersicht tab (VIP/Stammgast badges, Geburtstag, Firma, Allergien, Notizen) — independent of the computed segment. Manual fields (`vip_manual`/`stammgast_manual`/`company_customer`/`newsletter_opt_in`/`blocked_guest`, `birthday`, `company`, `language`, `allergies`, `dietary_notes`, `favorite_table`/`favorite_area`/`favorite_wine`/`favorite_dish`, `crm_notes`) are **strictly separate** from the auto-computed segment/score/campaigns — they are never fed into `classifySegment`/`computeCrmScore`/campaign predicates. UI labels: `blocked_guest`="Sperrliste", `favorite_table`="Lieblingsplatz", `dietary_notes`="Unverträglichkeiten". Tests: `src/lib/__tests__/guest-crm-profile.test.ts` (pure: mapping/normalize/dirty/manualCrmBadges/type) and `src/lib/__tests__/guest-crm-profile-db.test.ts` (CRUD with mocked supabase + `fetchGuestById`) — both node env, synthetic, no PII.
- **CRM Auswertung (Dashboard):** `src/pages/CrmAuswertungPage.tsx` (admin-only `/gaeste/auswertung`), `src/lib/reservation-dashboard.ts` (pure KPI logic — status predicates, `countInRange`, `futureReservationKpis`, `guestDashboardKpis`; no DB/DOM). Read-only aggregate fetches added to `src/lib/reservation-crm-db.ts` (`fetchNoShowCountsByGuest`, `fetchFutureReservations`, `fetchReservationsInRange`). Computes guest-overview KPIs, future-reservation buckets (current/next month, next 30/60/90 days, open/unanswered separate), and a custom date-range count. Active = `confirmed|completed`, open = `pending|unknown`, excluded = `cancelled|noshow`. No new SQL view/migration; existing tables only. The page uses a tab layout (`@/components/ui/tabs`): **Übersicht** (the three sections above), **Rückkehrpotenzial** (return-potential / at-risk guests) and **Kampagnen** (predefined campaign lists + CSV export). The 6 "Zukünftige Reservationen" tiles and the 4 "Individueller Zeitraum" tiles are clickable → a drill-down `src/components/crm/ReservationDetailList.tsx` (Datum/Uhrzeit/Gast/Personen/Status/Raum·Bereich/Tel·E-Mail/optional Kommentar; header `N Reservationen · M Personen`). Counts AND lists come from the SAME `ReservationDetailRow[]`: pure helpers `ReservationDetailRow`, `FutureSelectionKey`, `futureSelectionBounds`, `maxFutureBoundary`, `filterReservationDetails` (sort date asc / time asc nulls-last / id asc), `futureReservationList`, `rangeReservationList` in `reservation-dashboard.ts` mirror the `countInRange` status predicates so list length === tile count and persons-sum === tile persons; `fetchReservationDetailRows`/`fetchFutureReservations`/`fetchReservationsInRange` now return `ReservationDetailRow[]` (incl. PII for the list, tenant-filtered). **Guest names in the drill-down list are clickable → guest detail page (`/gaeste/:guestId`):** `ReservationDetailRow` carries `guestId` (from the `reservation_records.guest_id` FK, null-safe — null = unmatched reservation renders as plain text, no dead link); the generic list helpers preserve it; `ReservationDetailList` takes an optional `onSelectGuest?(guestId)`. The Rückkehrpotenzial + Kampagnen rows already navigate on row click. Tests: `src/lib/__tests__/reservation-detail-list.test.ts` (incl. guestId survives the future/range list helpers). **Einheitliche Gäste-Navigation + Rückkehr-Kontext:** every guest name in CRM Auswertung (drill-down list + Rückkehrpotenzial/Kampagnen tables) is a shared, link-styled `src/components/crm/GuestProfileLink.tsx` (`button` when `onSelect`+`guestId` else `span`; `stopPropagation` prop so a name click inside a row-clickable `<tr>` doesn't double-navigate; class `text-primary underline-offset-2 hover:underline`). `ReservationDetailList`'s former local `GuestName` was extracted to this shared component. The whole CRM Auswertung view state (tab / future-tile / individueller Zeitraum + Auswahl / geöffnete Kampagne) is mirrored to the URL via the pure `src/lib/crm-auswertung-url.ts` (NO supabase/DOM): `CrmViewState`, `parseCrmViewState`/`crmViewStateToParams` (omit defaults; range emitted ONLY when `rsel`+both dates valid), `crmReturnUrl`, `buildGuestHref(id, fromUrl)` (encodes `from`), and the **open-redirect-safe** `parseFromParam` (validates `from` against a synthetic base origin + EXACT `/gaeste/auswertung` pathname → returns only `pathname+search`; rejects `//evil`, absolute URLs, subpaths, backslashes/control chars). `CrmAuswertungPage`: one-shot `useRef` init from `searchParams`; controlled `Tabs` (`value`/`onValueChange` from URL on first render — no uncontrolled→controlled flip); a URL-sync `useEffect` with a string-equality guard (`next.toString() !== searchParams.toString()`) + `setSearchParams(next,{replace:true})` (loop-safe); a one-shot `useEffect` that re-runs `runRange()` then restores `rangeSel` when a valid range was restored; `goToGuest = navigate(buildGuestHref(id, crmReturnUrl(viewState)))` wired to the drill-down list AND the overdue/campaign rows. `GaesteDetailPage` reads `from` via `parseFromParam`: the back button is context-aware (`fromUrl ? 'Zurück zur CRM-Auswertung' → fromUrl : 'Zurück zur Gästeliste' → /gaeste`), and the missing-profile block is a friendly message with a "Gästesuche öffnen" (`/gaeste`) escape hatch + a conditional "Zurück zur CRM-Auswertung" button. NO DB/schema/migration change; reuses existing routes/components. Tests: `src/lib/__tests__/crm-auswertung-url.test.ts` (node env, 30 synthetic — parse/serialize/round-trip, default omission, range completeness, `parseFromParam` open-redirect abwehr).
- **Rückkehrpotenzial & gefährdete Stammgäste:** Pure logic in `src/lib/reservation-dashboard.ts` (`OVERDUE_INTERVAL_FACTOR=1.5`, `overdueByDays`, `isOverdue`, `isAtRiskTier`, `returnPotentialKpis`, `buildReturnPotentialList`, `ReturnPotentialRow`). Überfällig = `daysSinceLastVisit > avgDaysBetweenVisits × 1.5` (needs ≥2 visits & positive interval). Gefährdet = visit-count tier (`baseSegmentByVisits` in `src/lib/reservation-crm.ts`, **not** the live segment which flips to `inaktiv` after 90d) is `stammgast`/`vip` AND last visit >90d. Tests: `src/lib/__tests__/reservation-return-potential.test.ts`. Shared segment badge extracted to `src/components/crm/SegmentBadge.tsx` (`SegmentBadge`, `SEGMENT_ICON`, `SEGMENT_CLASS`), reused by `GaesteCrmPage`.
- **CRM Kampagnen (Kampagnenlisten & Export):** Pure logic in `src/lib/reservation-campaigns.ts` — 18 `CAMPAIGNS` defs (`id`/`label`/`description`/`slug`/`predicate`/`sort`), `CAMPAIGN_BY_ID`, `filterCampaign`, `summarizeCampaigns`, `buildCampaignCsv`, `campaignRowToCsvValues`, `campaignCsvFilename`, plus `CSV_HEADERS`, `INACTIVE_SHORT_DAYS=90`/`INACTIVE_LONG_DAYS=180`. Rendered as the third **Kampagnen** tab in `CrmAuswertungPage` (overview cards → detail table → CSV download). Segment-based lists (Wiederkehrende/Stammgäste/VIP/Ohne Besuch) use the **live** `m.segment`; Inaktiv >90/>180 are day-based (`daysSinceLastVisit`); Überfällig/Gefährdet/Neukunden-30/No-Show reuse `reservation-dashboard.ts` (`isOverdue`, `isAtRiskTier`, `NEW_GUEST_DAYS`, `NO_SHOW_RISK_MIN`). **Phase 1.2 added 7 manual-CRM campaigns** (`manuelle-vip`, `manuelle-stammgaeste`, `firmenkunden`, `newsletter`, `mit-allergien`, `mit-geburtstag`, `sperrliste`) whose predicates read ONLY `m.crm` flags (never the computed segment); they need a loaded CRM profile to match. CSV = UTF-8 **with BOM** (BOM prepended in the page at download, NOT in `buildCampaignCsv`), `;`-separated, CRLF, German headers, empty for missing, German `dd.MM.yyyy` dates, rounded intervals, filename `crm-kampagne-<slug>.csv`. CSV is unified: 8 manual-CRM columns (VIP/Stammgast manuell, Firmenkunde, Newsletter, Sperrliste, Geburtstag, Firma, Allergien — bool→`'Ja'`/`''`) are appended to EVERY row at indices 9–16, regardless of campaign. The page passes `noShowCount` AND the CRM profile into `guestListMetrics`. Tests: `src/lib/__tests__/reservation-campaigns.test.ts` (node env, synthetic, no PII). No migration / import / `guest_statistics` changes.
- **Gäste-Duplikate (Erkennung & Zusammenführung):** `src/pages/GaesteDuplikatePage.tsx` (admin-only `/gaeste/duplikate`, gated `isAdmin && !isGuest`, fetch guarded before `Navigate`; nav item "Gäste Duplikate" `GitMerge` in the Foratable group; route registered BEFORE `/gaeste/:guestId`). Detects duplicate guests by **normalized phone**; lists each group (≥2 guests sharing a phone key) side-by-side (Name, Telefon, E-Mail, Reservationen, letzte Reservation, angelegt am), master radio (default = most reservations) + AlertDialog confirmation, reloads after merge. Pure logic `src/lib/guest-duplicates.ts` (NO supabase: `normalizePhoneKey` reuses `normalizeMobile` w/ stored fallback, `DuplicateGuest`/`DuplicateGroup`, `groupDuplicatesByPhone`, `guestDisplayName`, `mergeCrmProfiles` [bool OR, fill-empty text, idempotent line-union for allergies/dietaryNotes/crmNotes], `fillEmptyIdentity` [fills empty master identity from dups, NEVER `match_key`]). DB layer `src/lib/guest-duplicates-db.ts` (`fetchGuestsForDuplicateScan`/`fetchDuplicateGroups`; `mergeGuests(tenant,masterId,dupIds)` — **no DB transaction (same as imports)**; PREFLIGHT verifies unique dupIds, master∉dups, all profiles loaded tenant-filtered & count==expected, aborts before any write on a missing/cross-tenant ID; ORDERED recoverable steps: move `reservation_records.guest_id`→master FIRST (FK `ON DELETE SET NULL`) → merge dup CRM into master → fill-empty master identity → recompute master aggregates from `reservation_records` (private `recomputeGuestAggregate`, copied from import, **never sums**) → POSTCONDITION verify no reservation still points at a dup → DELETE dup `guest_profiles` (CASCADE) → best-effort `logGuestMerge`; returns `{error,reservationsMoved,crmMerged,mergedCount}`; `checkGuestMergeLogTableExist`). Every read/write is `.eq('restaurant_id', tenant)`-scoped. Audit table `import`-style migration `supabase/migrations/20260623_guest_merge_log.sql` (RLS authenticated-only/anon-revoked/self-test; cols `restaurant_id`, `master_guest_id`, `merged_guest_ids` jsonb, `merged_count`, `reservations_moved`, `crm_merged`, `created_by` uuid — **IDs/counts only, no guest PII**, `created_at` DEFAULT now(); run manually in SQL editor). Import modules untouched. Tests: `src/lib/__tests__/guest-duplicates.test.ts` (pure) + `src/lib/__tests__/guest-duplicates-db.test.ts` (in-memory Supabase mock exercising move-first/recompute/delete-gate/cross-tenant-abort/PII-free log) — node env, synthetic, no PII.
- **CRM Segment-Aktionen (intern, mandantengesichert):** Auf jeder Smart-Segment-Ansicht in `GaesteCrmPage` (nur wenn ein Smart-Segment aktiv & nicht Top/Flop) gibt es 5 INTERNE, manuell + bestätigungsbasierte Aktionen (KEINE E-Mails/WhatsApp/externe Integrationen): (1) CSV-Export, (2) Kontaktliste erstellen, (3) Als kontaktiert markieren, (4) Notiz hinzufügen, (5) Folgeaufgabe erstellen. UI `src/components/crm/SegmentActionBar.tsx` (5 Buttons + 4 Bestätigungsdialoge; `actionsAvailable`-Prop deaktiviert die 4 Schreibaktionen + zeigt Migrations-Hinweis, CSV bleibt aktiv). Zwei DB-Tabellen via `supabase/migrations/20260624_crm_activities.sql` (manuell im SQL-Editor ausführen, RLS authenticated-only/anon-revoke/self-test, pgcrypto): `guest_crm_activities` (Typ-Diskriminator `contacted`|`note`|`follow_up` mit **per-Typ CHECK-Constraints**, `batch_id` gruppiert Sammelaktionen) und `guest_crm_contact_lists` (JSONB `guest_ids`-Snapshot + `member_count`). Beide haben `restaurant_id TEXT NOT NULL`, `created_by` = **Operator-UUID, keine PII**. Pure Logik `src/lib/crm-activities.ts` (KEIN supabase: `CrmActivity`/`Row`+`rowToCrmActivity`, `ContactList`/`Row`+`rowToContactList`, `ACTIVITY_TYPE_LABEL`/`FOLLOW_UP_STATUS_LABEL`, `cleanText`, `uniqueGuestIds`, `buildActivityInsertRows` [Fan-out: eine Zeile pro Gast, typ-fremde Felder null, follow_up-Default-Status `open`], `sortContacted`/`sortNotes`/`sortFollowUps`, `splitActivitiesByType`→`{contacted,notes,followUps}`). DB-Schicht `src/lib/crm-activities-db.ts`: **Mandantentrennung Pflicht** — `verifyGuestsBelongToTenant` (count-match über `guest_profiles.in(ids).eq(restaurant_id)`) läuft vor JEDEM Schreibvorgang, ein einziger fremder Gast bricht VOR jedem Write ab; jeder Read/Write `.eq('restaurant_id')`. `markGuestsContacted`/`addGuestNote`/`createFollowUpTask` (Fan-out, `batch_id` bei >1 Gast, werfen bei Schreibfehler), `setFollowUpStatus` (Gate `id`+`restaurant_id`+`activity_type='follow_up'`, wirft bei kein-Treffer), `fetchGuestActivities` (Tenant-Gate via `fetchGuestById` + Filter; best-effort `[]` NUR bei fehlender Tabelle, echte Fehler werfen), `createContactList`/`fetchContactLists` (gleicher best-effort-Vertrag), `checkCrmActivitiesTableExist`. `GaesteCrmPage`-Verdrahtung: `selectedIds`-Set + Checkbox-Spalte (Header select-all, per-Zeile `stopPropagation`), Aktionsziel = explizite Auswahl sonst die ANGEZEIGTEN (gefiltert+gesuchten) Zeilen — CSV-Default deckt sich mit den Schreibaktionen; nach Erfolg Auswahl leeren **und** neu laden (`handleActionDone`). `GaesteDetailPage` hat einen dritten Tab **„Aktivität"** (Icon `BellRing`, Count-Badge) mit Abschnitten Folgeaufgaben (Switch „Erledigt" → `setFollowUpStatus`, reload), Kontaktverlauf, Notizen (via `splitActivitiesByType`). Tests: `src/lib/__tests__/crm-activities.test.ts` (pure: Mapping/Fan-out/Sortierung/Split) + `crm-activities-db.test.ts` (in-memory Supabase-Mock: Mandantentrennung, kontaktiert/Notiz/Folgeaufgabe, Status-Gate, Anzeige der Gast-Aktivität) — node env, synthetic, no PII.

## Architecture decisions
- **Multi-tenancy:** Implemented via ID prefixes (`b-` for Beaulieu) in Supabase `employees` and `app_settings` keys, avoiding a dedicated `restaurant_id` column for simplicity and backwards compatibility.
- **Data Persistence Strategy:** LocalStorage is used as the primary, fast data store, with Supabase `app_settings` serving as a secondary, persistent backup, especially for financial and budget data. A sync hook (`useSyncStore`) handles this.
- **Authorization:** A centralized `usePermissions` hook enforces role-based access control, defined in `src/hooks/usePermissions.ts`, for granular control over features and data visibility.
- **Onboarding Flow:** Supports both existing employees completing details via a tokenized link and new self-registrations creating `pending_review` employee records for admin approval.
- **Time-based Reporting:** A global "Stichtag" (cut-off date) filter (`StichtagContext`) allows historical reporting and period closing, impacting various financial views.

## Product
- **Dashboard:** Overview of KPIs, revenue, costs, pro-rata personnel costs.
- **Schedule Planning:** Plan and actual shift schedules.
- **Target/Actual Analysis:** Comparison of planned vs. actual performance.
- **Personnel Master Data:** Employee data management (admin only).
- **Reporting/Financial Module:** Comprehensive financial data including prior year comparisons (P&L, budget).
- **Fixed Personnel Costs:** Overview of monthly salaries including 13th month salary by department.
- **Multi-Tenant Support:** Manage data for multiple restaurants (Oliv, Beaulieu) with tenant-specific data isolation.
- **Role-Based Access:** Different user roles (admin, service_manager, kueche_manager) have varying data access and permissions.
- **Onboarding System:** Facilitates employee data collection and self-registration.
- **Guest Link System:** Allows admins to generate temporary, password-protected read-only links for sharing sessions.
- **Pattern Warnings:** Identifies operational risks in schedules (e.g., consecutive days, short recovery times).
- **Product Master Data:** Centralized database for food/beverage articles, WES (cost of goods sold) per product, and tracking.
- **WES Analysis:** Compares WES from recipes, suppliers, and accounting for cost control.
- **Reservations Import:** Imports Foratable reservation-tool CSV exports (admin only) with upload→preview→save wizard and per-file history; recognizes returning guests for guest/behavior analytics. Existing-guest reuse before creating a new profile matches **only by normalized phone → email (case-insensitive), tenant-scoped** — name is NOT used for matching (avoids merging different people who share a name). Name-only bookings (no phone/email) still de-duplicate via the `(restaurant_id, match_key)` upsert.
- **Gästeexport Import (CRM-Anreicherung):** Admin-only import (`/foratable-import?tab=gaeste`, also linked in the Import-Zentrale) that enriches existing guests' manual CRM profiles from a Foratable *guest-list* export. Matches each CSV row to an existing guest by normalized email→phone→name (ambiguous matches are reported as "nicht zugeordnet"), then fills ONLY empty CRM fields (VIP, Newsletter, Sperrliste, Geburtstag, Firma, Lieblingsplatz, Notizen) — already-maintained values are kept and divergences counted as conflicts. Preview shows matched/created/updated/conflicts/unassignable before committing. Reservations, visit counts and segments are never touched; CSV-only, no email/WhatsApp.
- **Gäste-CRM:** Admin-only guest list with search (name/email/phone), visit count, first/last visit, average visit interval, and auto-segmentation (VIP ≥20 / Stammgast 8–19 / Wiederkehrend 3–7 / Neukunde 1–2 / Inaktiv >90d / Ohne Besuch). The list also surfaces the **manual CRM profile**: VIP/Stammgast-manuell, Firmenkunde, Newsletter and Sperrliste show as badges next to each guest; eight Ja/Nein/Alle filters (VIP manuell, Stammgast manuell, Firmenkunde, Newsletter, Sperrliste, Allergien vorhanden, Geburtstag hinterlegt, CRM-Notiz vorhanden) plus sortable Firma- and Geburtstag-columns let admins slice by manual data. Manual data is shown read-only here and never changes the auto-computed segment. The per-guest detail page is a full CRM customer record (Kundenakte) with six sections: a weighted CRM-Score 0–100 (niedrig/mittel/hoch/VIP-Potenzial), Besuchsverhalten (incl. a simple visit-count-based Gast-Status: Neu/Wiederkehrend/Regelmässig/VIP — separate from the segment), Präferenzen (favorite area/weekday/month/time/party-size), Risiko (no-shows, cancellations, overdue, at-risk), Besuchstrend (last 90d vs prior 90d → steigend/stabil/rückläufig), and a filterable reservation history (Alle/Nur Besuche/Nur Storniert/Nur No-Show). A visit = a completed reservation. Optional `guest_statistics` SQL view exposes the same KPIs directly in Supabase. The detail page now has two tabs: **Übersicht** (the read-only analytics above) and **CRM** (editable manual profile, see below).
- **Gäste CRM-Profil (Bearbeitung):** Admin-only editable manual CRM record per guest on the detail page's **CRM** tab — status flags (VIP / Stammgast manuell, Firmenkunde, Newsletter, Sperrliste), Stammdaten (Geburtstag, Firma, Sprache), Präferenzen (Lieblingsplatz/-bereich/-wein/-gericht), Allergien & Unverträglichkeiten, and internal notes. Two buttons: **Speichern** saves all fields at once and **Änderungen verwerfen** resets the form to the last-saved state (no DB write); both are disabled while there are no unsaved edits. If no record exists yet the form starts empty and the first save creates the row (upsert). Manual VIP/Stammgast flags additionally show as badges in the guest header and in a read-only **CRM-Profil** summary on the Übersicht tab (VIP, Stammgast, Geburtstag, Firma, Allergien, Notizen), shown independently of the auto-computed segment. Manual data is kept strictly separate and never changes the auto-computed segment/score/campaigns. Read-only guest links (guest sessions) cannot edit. Stored in the dedicated `guest_crm_profiles` table; computed live metrics remain untouched.
- **CRM Auswertung:** Admin-only dashboard (`/gaeste/auswertung`) with three tabs. **Übersicht:** guest-overview KPI cards (total/active/inactive/new/repeat-rate/VIP/Stammgäste/ohne Besuch/No-Show-Risiko/Rückkehrpotenzial), future-reservation analysis (current & next month, next 30/60/90 days, plus open/unanswered shown separately), and a custom date-range filter (active vs. open reservations + persons). All future-reservation tiles and the custom-range result tiles are clickable and open a detailed list of the underlying reservations (date, time, guest, persons, status, room/area, contact). **Rückkehrpotenzial:** KPI cards for overdue guests, at-risk Stammgäste and at-risk VIP, plus a list of overdue guests (most-overdue first) linking to each guest's detail page. **Kampagnen:** 18 predefined campaign lists — the 11 behavioural ones (überfällige/gefährdete Stammgäste & VIP, inaktiv >90/>180 Tage, 2+ No-Shows, Wiederkehrende, Stammgäste, VIP, Neukunden letzte 30 Tage, ohne Besuch) plus 7 manual-CRM ones (VIP manuell, Stammgäste manuell, Firmenkunden, Newsletter, mit Allergien, mit Geburtstag, Sperrliste). Each shows count + description with "Anzeigen" (detail table: Name, E-Mail, Telefon, Segment, Besuche, letzter Besuch, Tage seit letztem, Ø Intervall, No Shows) and "CSV exportieren" (only the filtered list, UTF-8 with BOM, semicolon-separated, German headers, `crm-kampagne-<slug>.csv`; the CSV always includes 8 manual-CRM columns). Admin-only, CSV download only — no email/WhatsApp. Computed live in TS from existing reservation tables; no new migration.
- **Rückkehrpotenzial (gefährdete Stammgäste) — Gästeliste:** Flags regulars slipping away — `returnRiskRatio` = daysSinceLastVisit ÷ personal avgDaysBetweenVisits; `isAtReturnRisk` = ratio ≥ `RETURN_RISK_FACTOR` (2) AND visits ≥ `RETURN_RISK_MIN_VISITS` (3). Logic in `src/lib/reservation-crm.ts`; filter (`returnRisk`) + sort key (`returnRisk`) in `src/lib/guest-list-filters.ts`; KPI count `returnRiskGuests` in `src/lib/reservation-dashboard.ts`. Surfaced on `/gaeste` (filter dropdown, quick-filter chip, "Rückkehr-Risiko" column showing the overdue ratio) and as the `Rückkehrpotenzial` KPI card in the `/gaeste/auswertung` Übersicht tab. This 2.0×-factor metric is distinct from the Rückkehrpotenzial **tab's** 1.5×-factor `OVERDUE_INTERVAL_FACTOR` logic (`reservation-dashboard.ts`); both coexist. Independent of the static "Inaktiv (>90d)" segment so it catches guests early. Admin-only, computed live in TS, no migration.

## User preferences

### Datenintegrität — verbindliche Entwicklervorgabe
- **Umsatzdaten dürfen niemals per localStorage-Overwrite oder naivem Blob-Write gespeichert werden.**
- Jeder Schreibpfad für `dailyBudgets` / `beaulieu:dailyBudgets` muss `safeUpsertDailyBudgets()` verwenden.
- `safeUpsertDailyBudgets` liest immer zuerst den aktuellen Supabase-KV-Stand, mergt, schreibt zurück.
- Bei KV-Schreibfehler erscheint eine sichtbare Fehlermeldung — kein stilles Fallback auf localStorage.
- Die `daily_revenues`-Tabelle (Roadmap in diesem File) wird erst aktiviert, wenn die aktuelle Lösung fachlich fertig getestet ist. Bis dahin keine Migration ausführen.

## Technische Roadmap — daily_revenues Migration

### Kontext
Tagesumsätze werden aktuell als grosser KV-Blob in `app_settings` gespeichert
(Schlüssel `dailyBudgets` / `beaulieu:dailyBudgets`). Als Sofortschutz gegen
Datenverlust wurde `safeUpsertDailyBudgets` eingeführt (liest KV → mergt →
schreibt zurück). Diese Lösung ist stabil, hat aber strukturelle Grenzen:
kein Audit-Log, kein atomarer UPSERT pro Tag, Merge-Konflikte bei Parallelzugriff.

### Zielarchitektur
Normalisierte Tabelle `daily_revenues` in Supabase Postgres:
```sql
-- Vorbereitet in supabase/migrations/20260507_daily_revenues.sql
CREATE TABLE daily_revenues (
  restaurant_id TEXT    NOT NULL,  -- 'oliv' | 'beaulieu'
  date          DATE    NOT NULL,
  actual_revenue        NUMERIC,
  planned_revenue       NUMERIC,
  actual_food           NUMERIC,
  actual_beverage       NUMERIC,
  actual_labor_cost     NUMERIC,
  previous_year_revenue NUMERIC,
  created_at    TIMESTAMPTZ DEFAULT now(),
  updated_at    TIMESTAMPTZ DEFAULT now(),
  updated_by    TEXT,              -- user email für Audit-Trail
  PRIMARY KEY (restaurant_id, date)
);
```
Jeder Tag hat einen eindeutigen Datensatz — kein Blob, kein Merge nötig.

### Migrationsschritte (in dieser Reihenfolge)

**Schritt 1 — SQL-Migration ausführen**
- Script `supabase/migrations/20260507_daily_revenues.sql` im Supabase SQL-Editor
  ausführen (noch nicht aktiv).
- Zählt bestehende KV-Daten vor und nach der Migration (Abweichung = 0 prüfen).

**Schritt 2 — Datenmigration KV → Tabelle**
- Einmalige Server-Side-Migration: alle Einträge aus `app_settings` wo
  `key IN ('dailyBudgets', 'beaulieu:dailyBudgets')` als normalisierte Zeilen
  in `daily_revenues` einfügen.
- Validierung: `SELECT count(*) FROM daily_revenues` muss ≥ bisherige Blob-Tage.

**Schritt 3 — Neue Datenbankschicht**
- Neue Datei `src/lib/daily-revenues-db.ts` mit:
  - `upsertDailyRevenue(restaurantId, date, fields)` → atomarer UPSERT inkl.
    `updated_by` und `updated_at`
  - `getDailyRevenues(restaurantId, fromDate, toDate)` → Rückgabe als Map
  - `getDailyRevenueMonth(restaurantId, year, month)` → 31-Tage-Array
- Kein localStorage mehr für Tagesumsätze — direkt aus Supabase Postgres.

**Schritt 4 — Lese- und Schreibpfade umstellen**
Betroffene Komponenten (alle nutzen aktuell `safeUpsertDailyBudgets`):
1. `GastronoviImportSection` (handleSave, commitImport, handleImportClick)
2. `TagesansichtPage` (saveManualIst)
3. `TagesControllingPage` (commitUmsatzEdit)
4. `Dashboard` (saveRevenue)
5. `useVj2025Import` (Vorjahres-Seed)
6. `usePersonnelData` (Lohnkosten-Schreibpfad)
7. `SchedulePlanner` (Lohnkosten-Tagesblock)

**Schritt 5 — Audit-Log**
- Tabelle `daily_revenues` enthält `updated_by` (Email des einloggten Benutzers)
  und `updated_at` (Timestamp).
- Optional: separate `daily_revenues_audit`-Tabelle mit Trigger für vollständigen
  Change-History.

**Schritt 6 — Fallback entfernen**
- `safeUpsertDailyBudgets` auf Read-Only-Fallback reduzieren (nur noch lesen,
  nicht mehr schreiben).
- `useSyncStore` `SYNC_KEYS`-Liste: `dailyBudgets` entfernen.
- localStorage-Einträge `dailyBudgets` / `beaulieu:dailyBudgets` nach Migration
  löschen (Cleanup-Script).

**Schritt 7 — Integrationstests**
Alle vier Views müssen für jeden Testtag identische Werte zeigen:
- Tagesansicht (`TagesansichtPage`)
- Dashboard (`Index.tsx`)
- Tages-Controlling (`TagesControllingPage`)
- Erfolgsrechnung / P&L (`PLView.tsx`)

Testscript: `src/lib/__tests__/daily-revenues-integration.test.ts` (noch zu erstellen).

### Status
- [x] SQL-Migration-Script vorbereitet (`supabase/migrations/20260507_daily_revenues.sql`)
- [ ] SQL-Script im Supabase SQL-Editor ausführen
- [ ] Datenmigration KV → Tabelle
- [ ] `daily-revenues-db.ts` implementieren
- [ ] Schreibpfade umstellen
- [ ] Audit-Log aktivieren
- [ ] Fallback entfernen
- [ ] Integrationstests

## Gotchas
- **Supabase Migrations:** All SQL migration scripts in `supabase/migrations/` **must** be executed sequentially in the Supabase SQL Editor.
- **Tenant Filtering:** Data for Beaulieu is filtered by `id LIKE 'b-%'` and for Oliv by `id NOT LIKE 'b-%'`; there is no explicit `restaurant_id` column.
- **Mirus Name Mapping:** Manual mapping might be required for Mirus imports to correctly link employee names.
- **Default Budget Seed:** The 2026 budget is seeded automatically upon first load if no `plLineItems` exist for that year.

## Pointers
- **Supabase Documentation:** [https://supabase.com/docs](https://supabase.com/docs)
- **React Documentation:** [https://react.dev/](https://react.dev/)
- **Tailwind CSS Documentation:** [https://tailwindcss.com/docs](https://tailwindcss.com/docs)
- **Shadcn UI Documentation:** [https://ui.shadcn.com/docs](https://ui.shadcn.com/docs)
- **Vite Documentation:** [https://vitejs.dev/guide/](https://vitejs.dev/guide/)