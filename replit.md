# Personalkostentracker

Internes Reporting-Tool für Umsatz, Personalkosten, Dienstpläne und KPIs — mandantenfähig (Oliv, Beaulieu).

## Run & Operate
- **Run:** `npm run dev` · **Build:** `npm run build` · **Tests:** `npx vitest run`
- **Env Vars:** Supabase URL + Anon Key (`src/integrations/supabase/client.ts`)
- **Große Builds:** bei OOM (Exit -1 ohne Output) mit `NODE_OPTIONS=--max-old-space-size=8192 npx vite build` erneut ausführen.
- **Type-Check:** `NODE_OPTIONS=--max-old-space-size=8192 npx tsc -p tsconfig.app.json --noEmit` — das nackte `npx tsc --noEmit` prüft NICHTS (Root-tsconfig hat `files: []`, solution-style).

## Stack
- React + TypeScript + Vite, Shadcn UI, Tailwind CSS
- Supabase (Auth, Postgres, KV in `app_settings`), `date-fns`, `recharts`

## Architektur & Konventionen
- **Multi-Tenancy:** über ID-Präfixe (`b-` = Beaulieu) in `employees`/`app_settings` — **KEINE `restaurant_id`-Spalte** auf `employees`/`app_settings` (Filter `id LIKE 'b-%'` bzw. `NOT LIKE`). Reservations-/CRM-Tabellen haben `restaurant_id`; jeder Read/Write ist tenant-gefiltert.
- **Persistenz:** localStorage als schneller Primärspeicher + Supabase `app_settings` (KV) als persistentes Backup (`useSyncStore`). Finanz-/Budgetdaten NIE per naivem Blob-Write speichern (s. User preferences).
- **Auth/Rollen:** zentral in `src/hooks/usePermissions.ts` (+ `src/contexts/AuthContext.tsx`). Rollen admin / service_manager / kueche_manager / beaulieu_* / Gast. Admin-only-Seiten gaten `isAdmin && !isGuest` auf Route-Guard UND Lade-Effekt (Fetch feuert vor `Navigate`).
- **Stichtag:** globaler Cut-off-Filter (`StichtagContext`) für historisches Reporting/Periodenabschluss.
- **Reine Logik:** Analyse-/Berechnungs-Libs (`src/lib/*-utils.ts`, `*-analytics.ts`) sind DOM-/Supabase-frei (nur `import type`) und synthetisch getestet. Test-Dateien für Node-Only-Logik brauchen `// @vitest-environment node` in Zeile 1 (sonst jsdom/libuuid-Crash); React-Komponententests brauchen `// @vitest-environment happy-dom` (jsdom lädt das native `canvas`-Paket → libuuid-DLOPEN-Crash, Alias/vi.mock greifen dort nicht).
- **Wo Dinge leben:** `src/pages/` (Seiten), `src/components/` (UI), `src/lib/` (Logik + DB-Layer), `src/types/` (Modelle), `src/contexts/`, `src/hooks/`, `supabase/migrations/` (SQL). Kern: `src/App.tsx` (Routing), `src/components/AppNav.tsx` (Navigation: `DASHBOARD_ITEM` + `NAV_GROUPS`, longest-path-Highlighting; NavItem-Flag `secondary: true` = Menüpunkt liegt pro Gruppe hinter „Mehr"-Toggle in Desktop-Sidebar UND Mobile-Sheet, klappt auto-auf bei aktiver Route — sekundär: Kennzahlen Bericht, Datenintegrität MA, CRM Auswertung, Reservations Analyse, Gäste Duplikate, Foratable Report; `/reporting` bewusst nur per URL erreichbar; `/import-cockpit` hat KEINEN Nav-Eintrag mehr — Route+Guard bestehen, erreichbar über Header-Link im Import-Center). Tenant: `src/contexts/TenantContext.tsx`, `src/lib/tenant-utils.ts`. Supabase: `src/lib/supabase-db.ts`, `supabase-kv.ts`. Schema: `supabase/setup_new_project.sql`.

## Designsystem & UX-Leitlinien (Phase 3, ab Juli 2026)
- **Bausteine (für ALLE neuen/migrierten Seiten verwenden, keine Eigenbauten):** `src/components/ui/tones.ts` (Ton-Typ `good|warn|critical|info|neutral` + Farbmaps TONE_DOT/TEXT/PILL/BOX — Grün=gut, Orange=Achtung, Rot=kritisch, Blau=Info, Grau=neutral), `status-pill.tsx` (StatusPill), `kpi-card.tsx` (KpiCard mit EXPLIZITEN trend-Props ohne Fach-Semantik + KpiGrid + MoreKpis-Collapsible mit optionalem `storageKey`), `info-tip.tsx` (InfoTip — Erklärungen als Tooltip statt Textabsatz; TooltipProvider ist app-weit in App.tsx), `hint-box.tsx` (HintBox), `page-states.tsx` (LoadingState/EmptyState), `table-style.ts` (Klassen-Konstanten TABLE/TH/TH_NUM/TH_STICKY/TD/TD_NUM/ROW_CLICKABLE — bewusst KEINE DataTable-Abstraktion), `dialog-size.ts` (DIALOG_SM/MD/LG, Anwendung ab Phase 3.3), `src/components/layout/PageShell.tsx` (Breiten narrow=4xl/default=7xl/wide=1800px — kein Einheitszwang) + `PageHeader.tsx` (sticky, opak, icon/title/info/meta/Toolbar-children/actions).
- **Seiten-Muster:** kompakte Toolbar → Warnung nur wenn nötig (HintBox) → max. 4 KPI-Karten (KpiGrid), weitere hinter MoreKpis (KPIs nie löschen, nur verlagern) → Hauptinhalt ohne langes Scrollen (TABLE_SCROLL max-h + Sticky-Kopf) → Details per Klick/Tooltip/Collapsible. Zahlen IMMER rechtsbündig + tabular-nums. Sticky-Flächen brauchen OPAKE Hintergründe (bg-card/bg-muted, keine /40-Alpha).
- **Pilot:** KennzahlenBerichtPage vollständig migriert (Referenz-Implementierung). Phasenplan + Dashboard-Rollen-Audit: `docs/ux-phase3-plan.md`. Tests: `src/components/__tests__/design-system.test.tsx`, `src/pages/__tests__/KennzahlenBerichtPage.test.tsx` (happy-dom).
- **Phase 3.2 (Juli 2026):** ReservationAnalysePage vollständig migriert (reine UI, keine Logik-/Datenänderung): PageShell/PageHeader (Kennzahl-Umschalter als Header-children, Gäste-CRM-Back als action), 1-Zeilen-Toolbar (Jahr/Von–Bis + Presets + Status, aria-labels), Zustände via HintBox/EmptyState/LoadingState, KpiGrid mit exakt 4 KpiCards (KpiTile-Wrapper, TREND_TONE up=good/down=critical), ALLE Tabellen inkl. der 3 Dialog-Tabellen auf table-style-Konstanten (nur MonthYoyTable sticky in TABLE_SCROLL), Erklärtexte → InfoTip (OCCURRENCE_NOTE im Header-InfoTip + Karten-InfoTips). Lokale Helfer FieldLabel/AmberNote gelöscht. Test: `src/pages/__tests__/ReservationAnalysePage.test.tsx` (happy-dom, Daten-Layer gemockt; vitest braucht `NODE_OPTIONS=--max-old-space-size=8192` — 4096 wird still ge-OOM-killt).
- **Hinweis:** Das geplante OP-Listen-Tool (und weitere neue Module) nutzt später DIESELBEN Bausteine — bei neuen Flächen nichts Eigenes erfinden.

## Module

### Startseite & Dashboard
- **Startseite (vereinfacht, Admin):** Route „/" = `isAdmin ? StartOverview : (canAccessModule('dashboard') ? Dashboard : /personal)` — Manager/beaulieu_manager behalten das alte Dashboard-Verhalten; das ausführliche Dashboard bleibt vollständig unter `/dashboard` (Nav-Item „Dashboard" in Gruppe Verkauf, adminOnly; `DASHBOARD_ITEM` heisst jetzt „Start"). Seite `src/pages/StartOverview.tsx` (3 Bereiche: Heute-Statuskarten Umsatzimport/Reservationen/Dienstplan/Tagesabschluss, Warnungen = NUR Status `action`, Schnellaktionen als Links; Gäste sehen keine Schreib-/PII-Aktionen). Rein `src/lib/start-overview-utils.ts` (`buildStartOverview`; Frische-Regeln 1:1 via `computeSourceStatus` aus dem Import-Cockpit — KEINE eigenen Schwellen; Dienstplan-Regel: geplant bis ≥ heute+7 = ok, < heute = action; Tagesabschluss = gestriger Tag im `adyenAbstimmung_v1`-Blob bestätigt, null = nicht eingerichtet → unknown ohne Warnung). Hook `src/hooks/useStartOverview.ts` (read-only: `fetchCockpitSignals` + Direkt-Read des Adyen-Blobs aus localStorage, NIE Save-Schicht; Zustände loading/error/ready). Tests: `start-overview-utils.test.ts` (node) + `StartOverview.test.tsx` (happy-dom, Hook gemockt).
- **„Heute wichtig"-Banner (Dashboard, UX Phase 2):** `src/components/HeuteWichtigBanner.tsx`, oben in `src/pages/Dashboard.tsx` gerendert. STRIKT read-only/additiv: nutzt `useStartOverview` (dieselbe Quelle wie Startseite/Import-Cockpit, KEINE eigenen Frische-Regeln), zeigt NUR Status `action` + 4 Schnellaktionen (Import/Tagesabschluss/Dienstplan/Reservationen; Gäste ohne Schreib-/PII-Aktionen); nur Admin (`!isAdmin → null`). Test: `HeuteWichtigBanner.test.tsx` (happy-dom, Hook gemockt).

### Reporting & Finanzen
- **Erfolgsrechnung / P&L:** `src/pages/PLView.tsx`, `src/lib/pl-engine.ts`, `src/types/pl.ts`, `src/lib/reporting-store.ts`, `src/types/reporting.ts`. Vorjahres-Diagnose-Banner (nur Sichtbarkeit, keine Berechnung): rein `src/lib/pl-prior-year-diagnostics.ts`.
- **Budget:** `src/pages/Budget.tsx`, `src/lib/budget-store.ts`, `src/types/budget.ts`. 2026-Seed automatisch beim ersten Laden ohne `plLineItems`.
- **Tagesumsätze:** KV-Blob `dailyBudgets` / `beaulieu:dailyBudgets`; Schreiben NUR via `safeUpsertDailyBudgets` (read → merge → write, sichtbarer Fehler statt stillem Fallback). Roadmap: Normalisierung nach `daily_revenues` (s. u.).
- **OP-Liste Kreditoren (Phase 1):** `/op-liste` (`src/pages/OpListe.tsx`, admin-only via BlockedRoute + `!isAdmin → Navigate`, Guard auch VOR Lade-Effekt; Gäste read-only ohne Import-Button; Nav in Gruppe Umsatz nach Tagesabschlüsse). PDF-Import (Sage-OP-Liste) im Dialog `src/components/op-liste/OpImportDialog.tsx` mit Vorschau (KPIs/Top10/Warnungen) VOR dem Speichern; Parser rein `src/lib/op-liste-parser.ts` (X-Positions-Kalibrierung aus Kopfzeile für Alters-Buckets, Beträge NUR mit 2 Nachkommastellen, debug+failureReason auf allen Pfaden — gegen SYNTHETISCHES Fixture gebaut, vor Verlass auf Zahlen mit echter OP-PDF validieren); nutzt `extractPdfTextLines` aus `pdf-import-engine.ts`. Vergleich/Behörden rein `src/lib/op-liste-compare.ts` (`normalizeSupplierName`, `compareSnapshots` mit Status neu/erledigt/gestiegen/gesunken/unverändert, `buildAuthoritySummary` MWST/QST/AHV/BVG per Regex-Matching). DB `src/lib/op-liste-db.ts` + Migration `supabase/migrations/20260708_creditor_op.sql` (`creditor_op_imports`/`creditor_op_items`, RLS authenticated-only + REVOKE anon, partieller Unique-Index auf `(restaurant_id, snapshot_date)` WHERE status='active'); gleicher Stichtag wird NIE still überschrieben — Lifecycle processing→active/replaced (+Rollback-Kompensation), Ersetzen nur mit explizitem Hinweis im Dialog. Typen `src/types/op-liste.ts`. Tests: `op-liste-parser.test.ts`, `op-liste-compare.test.ts` (node) + `OpListe.test.tsx` (happy-dom, DB/Dialog gemockt).

### Personal
- **Personalaufwand = Total Arbeitgeberkosten (AG-Umbau, Juli 2026):** ALLE Kostenanzeigen/-exporte rechnen mit effektivem AG-Aufwand, NIE mit rohem `hourlyWage`. Kern: `src/lib/social-costs.ts` (`SocialCostRates` + `socialCostFactorFromRates`, zentrale Sätze via Settings-Card/KV, CH-Defaults synchron ab erstem Render — nie null), `src/lib/employee-rate.ts` (`getEmployerCostRate`/`getEffectiveHourlyRate` = Brutto inkl. anteil. 13. × AG-Faktor; `getWageLabel`), Hooks `useSocialCostRates` + `useEmployerRateMap` (`{rateById, rates}` — bevorzugt in Komponenten). Festangestellte: `(monthlySalaryWith13th ?? monthlySalary) × Faktor` (Dashboard `agMonthly`, MobileDayView pro Kalendertag, DayDetailDialog pro Stunde ÷182h — beides AG-basiert). Libs bekommen `rates` als PFLICHT-Param (pdf-export, comprehensive-report, schedule-export-import `ExportOptionsV2`/`TemplateExportOptions`, schedule-print-export `PrintExportOptions`, correctionSuggestions) → tsc erzwingt Call-Sites. FIBU-Begriffe: `EMPLOYER_COST_LABELS(_SHORT)`/`EMPLOYER_COST_INFO`/`EmployerCostInfoTip`. **Regeln:** pkIst aus FIBU-5xxx-Konten ist BEREITS AG-Aufwand — NIE mit Faktor multiplizieren. `dailyBudgets.plannedLaborCost`/`actualLaborCost` = Legacy-0-Felder (nicht mehr befüllt). Default-Import-Wage 25.00 in schedule-export-import bleibt bewusst roher Lohn (Import-Vorschlag, kein Kostenausweis). **Tote Komponenten NICHT migriert** (0 Importer, bei Wiederbelebung zuerst auf rates umstellen oder löschen): KPIDashboard (hardcoded ×1.22!), UnifiedCostOverview, MonthlyOvertimeOverview, WeeklyOvertimeOverview, TimeEntriesEditor, PlannedLaborCostCalculator.
- **Personalstamm:** `src/pages/Personalstamm.tsx` (admin + eingeschränkt `kueche_manager`). Lohn-Gating: Löhne unter `canSeeHourlyWages`/`canEditWages`; vertrauliche Flächen (Persönliche Daten inkl. AHV/IBAN, Verträge, Duplikat-Warnung, pending_review) unter lokalem Gate `canManageAllEmployees = isAdmin || isBeaulieuManager`. `kueche_manager` sieht NIE Löhne/PII, bearbeitet nur Küchen-Stammdaten (Abteilung auf Küche gesperrt).
- **Positionen/Stationen:** `src/pages/Positionen.tsx` (admin), rein `src/lib/position-utils.ts`, `src/lib/positions-db.ts`, `src/hooks/usePositions.ts`, Migration `supabase/migrations/20260625_positions.sql` (+ `employees.primary_station`/`secondary_stations`). Stabile **Keys (Slugs)**, nie Anzeigenamen, in `employee.primaryStation`/`secondaryStations` → Umbenennen bricht Zuordnungen nicht. Hierarchie Abteilung → Bereich → Position; `applyDefaultPositions` deaktiviert (löscht nicht) übrige Positionen. Qualifikationen auch im Personalstamm editierbar (Cross-Training).
- **Personalbedarf (SOLL-Besetzung):** `src/pages/Personalbedarf.tsx` (admin), rein `src/lib/staffing-requirements-utils.ts`, `-db.ts`, `src/hooks/useStaffingRequirements.ts`, Migration `20260625_staffing_requirements.sql`. Pro (Saison × Wochentag) je Position ≥1 Schicht; vollständig getrennt von der Dienstplanung (kein FK). Erweiterbar über neue `scope_type`-Werte + `meta` jsonb OHNE künftige Migration. Orphan-Schutz: Schichten inaktiver Positionen bleiben beim Speichern erhalten.
- **Personalbedarf-Abgleich (SOLL/Ist, nur ANZEIGE):** rein `src/lib/staffing-comparison-utils.ts` + `src/components/schedule-planner/StaffingComparisonPanel.tsx`, `DayStaffingBadge.tsx`, `StaffingScheduleCheckCard.tsx`; geteilte Darstellungsbausteine (Status-Stile, Badge, Differenz-Tooltip, KPI-Kacheln) in `staffing-status-ui.tsx`. **2-Farben-Warnung** (`ComparisonStatus` = `optimal`|`overstaffed`|`understaffed`): exakt = optimal/grün, JEDE Abweichung (auch ±1) = rot; Richtung nur über Label (Überbesetzt/Unterbesetzt) + Differenztext (`formatStaffingDiffPersons`, U+2212-Minus). KPI-Kacheln via `summarizeStaffingKpis` (Optimal/Überbesetzt/Unterbesetzt in Bedarfs-Schichten + Überstunden-Potenzial = Σ fehlende Personen × Schichtdauer in Personen-Stunden). Tabellen: Benötigt/Geplant/Differenz(Tooltip)/Status-Badge. Tooltip (`staffingTooltipLines`) zeigt Umsatz/Produktivität nur, wenn Werte übergeben werden — dieses Modul führt KEINE Umsatzquelle. Matching NUR über Hauptposition; rollen-scoped (`departments`-Filter → kein Cross-Department-Leak). Ändert KEINE Plan-/Kosten-/Overtime-Daten, keine neue Tabelle. **Nachfrage-Kontext (Reservationen):** rein `src/lib/staffing-demand-context.ts` (`buildDayDemandContext`: Tages-Personen/Res. gebucht + Ø der letzten `LOOKBACK_OCCURRENCES=8` Vorkommen desselben Wochentags, Ø über ALLE 8 inkl. 0-Tage, `pctDiff` null bei Ø=0, `kind` expected/past via `today`-Param) + Hook `src/hooks/useDayDemandContext.ts` (EIN `fetchReservationsInRange`, PII SOFORT auf `{date,partySize,status}` projiziert, Status hidden/loading/unavailable/error/ready, Tabellen-Check gecacht) + `StaffingDemandContext.tsx` (Gating `isAdmin && !isGuest` INTERN — Manager sehen nichts; sichtbarer Fehlerhinweis statt stiller 0). Eingebaut in StaffingComparisonPanel + StaffingScheduleCheckCard (Voll-Variante) und im DayStaffingBadge-Popover (`compact`-Prop, Ein-Zeilen-Variante; Fetch erst beim Öffnen — PopoverContent mountet lazy, `date` kommt als `dateStr` aus ModernScheduleGrid). Geteilte reine Anzeige-Helfer `formatDemandPct`/`demandHeadlineLabel`/`demandComparisonLabel` in `staffing-demand-context.ts`. Tooltip-Anschlussstelle bewusst nicht genutzt. Saison ohne Einfluss. Tests: `staffing-demand-context.test.ts` (node) + `StaffingDemandContext.test.tsx` (happy-dom, Hook gemockt).
- **Dienstplan:** `src/pages/SchedulePlanner.tsx`, `src/components/schedule-planner/*`, `src/lib/pattern-warnings.ts`. Manager-Sichtbarkeit zentral über `src/lib/employee-visibility.ts` (eingeschränkte Rolle GEWINNT; RAW-`employees` nur für Import-Match/CRUD/localStorage). Küchen-Manager-Tagesheader lohn-sicher (`src/lib/schedule-daily-totals.ts` — kein CHF-Betrag erreicht das DOM). Export abteilungstreu (`src/lib/schedule-export-department.ts`). Schicht-Farbe/Legende: `src/components/schedule-planner/ShiftLegend.tsx`.
- **Überstundenkosten Festangestellte:** `/personal-fix` (`src/pages/PersonalFix.tsx`) + rein `src/lib/overtime-analysis.ts`. Monatslogik: `overtime = max(0, produktive Ist − Monatssoll)`, Soll = `42h × (Tage/7) × Pensum`. Nur Festangestellte (`vollzeit|teilzeit` UND `monthlySalary>0`); Absenzen zählen nie als produktiv; Pro-MA-Deaktivierung möglich.

### Produkte & Warenkosten
- **Stammdaten/Analyse:** `src/pages/Artikel.tsx`, `ProduktStamm.tsx`, `WesAnalyse.tsx`, `ArtikelTracking.tsx` + zugehörige Stores in `src/lib/`.
- **Produkt-Analyse (Rangliste + Drill-down):** `src/pages/ProduktAnalyse.tsx` → `ProduktDetail.tsx`, rein `src/lib/product-analytics.ts`. Perioden Tag/Woche/Monat/Jahr (ISO-Wochen mit 52/53-Rollover), Filter URL-gespiegelt (refresh-/back-fest). `product_sales` hat keine `restaurant_id` — nur boundary-treue reine Funktionen, keine Schema-Änderung.
- **Lieferantendokumente:** `src/pages/SupplierDocuments.tsx`, `SupplierComparison.tsx` + `src/lib/supplier-documents-store.ts`.

### Importe
- **CSV/PDF:** `src/lib/csv-import-engine.ts`, `pdf-import-engine.ts`, `src/pages/CSVImport.tsx`.
- **Gastronovi Produkt-CSV:** `src/pages/SalesUpload.tsx` + rein `src/lib/gastronovi-csv-parser.ts` → `product_sales`. `matchAnzahlUmsatz` summiert je (Produkt, Datum) zu genau EINEM Record; gleiche Namen/verschiedene Preise werden unvermeidlich summiert (flaches CSV, keine Artikelnr.).
- **Umsatzabstimmung vs. Tagesabschlüsse (ZWEI Seiten):** `/umsatzabstimmung` (`UmsatzAbstimmungPage.tsx`, Titel „Umsatzabstimmung") zeigt NUR noch die Monatsabstimmung (`UmsatzAbstimmung`, Monats-Card „Monatsabstimmung Umsatz"). `/tagesabschluesse` (`TagesabschluessePage.tsx`, Titel „Tagesabschlüsse", KEIN Redirect mehr) trägt `TagesabschlussSection` + `AdyenAbgleichSection` (eigener Jahres-Selector, gleiche Guards: BlockedRoute für beaulieu_manager, `!isAdmin → Navigate`). Nav: BEIDE Einträge in Gruppe „Umsatz" (`Scale`/`ClipboardCheck`); `NAV_GROUPS` ist exportiert (Test). Bereichs-Label `umsatz` bleibt „Umsatz / Tagesabschluss". Seiten-Split-Test: `src/pages/__tests__/umsatz-tagesabschluss-split.test.tsx` (happy-dom). **UX-Verdichtung /tagesabschluesse (Juli 2026, reine UI — keine neue Logik/DB):** Erklärtexte nur noch als Info-Tooltips (`title` auf `ta-page-info`/`ta-section-info`); kompakte Monats-Toolbar mit „Heute"-Button (`ta-heute`, disabled im aktuellen Monat) + „+ Tagesabschluss" (`ta-add-abschluss`, öffnet Tagesdetail heute→erster offener Tag→erste Zeile); Anfangsbestand-Warnung 1-zeilig mit „Erfassen"-Button (`ta-anfangsbestand-erfassen`, klappt Eingabe auf); KPIs = 4 Chips (`ta-kpi-confirmed/-open/-diff/-saldo-ende`) + Collapsible „Weitere Kennzahlen" (`ta-kpi-more-toggle`/`ta-kpi-more` mit den übrigen KPI-Karten inkl. `ta-anfangsbestand-edit`); Legende in `<details>` (`ta-legend-toggle`); Tabelle: Scroll-Container `max-h-[70vh] overflow-auto` mit sticky 2-zeiligem Kopf (Gruppenzeile `top-0 z-20 h-6`, Spaltenzeile `top-6 z-10`) — Header-Farben dafür OPAK (dark: `*-900`/`*-950`, kein `/40`-Alpha, sonst scheinen Zeilen durch). UX-Test: `TagesabschlussSection.test.tsx` (happy-dom, DB/Dialoge gemockt).
- **Adyen-Abgleich (Tagesabschlüsse, täglich):** Section auf `/tagesabschluesse` (`src/components/umsatzabstimmung/` = `AdyenAbgleichSection.tsx`/`AdyenDayTable.tsx`/`adyen-ui.tsx`; Page bleibt dünn). Parser rein `src/lib/adyen-csv-parser.ts` („Received Payment Details"-CSV: Beträge vorzeichenbehaftet summiert, Typen mit /refund|chargeback/ werden bei positivem Betrag negiert, NUR CHF, Payout-/Settlement-CSV wird mit klarem Fehler abgelehnt, Debug+failureReason auf allen Pfaden; vor Verlass auf die Zahlen mit einer ECHTEN Adyen-Datei validieren — Fixture ist synthetisch). Abgleich rein `src/lib/adyen-abstimmung.ts`: Blob `adyenAbstimmung_v1` {days, methodLabels, overrides, comments, confirmations}, Persistenz `adyen-abstimmung-db.ts` = Settings-Infra (localStorage primär + KV-Backup, KEINE Migration); `mergeAdyenImport` ersetzt NUR importierte Tage und erhält Overrides/Kommentare/Bestätigungen. Z-Bericht-Seite read-only via `loadGnPaymentMethodsForMonth` (NUR Tagesimporte `period_from===period_to`); Mapping `normalizeGnPaymentName` auf Adyen-Keys (mastercard/visa/amex/twint/…) mit ZWEI Flags: `isCard` = Adyen-Vergleichsbasis (nur über Adyen abgewickelte Arten), `isKkCard` = KK-Total/Export-Karten (alle isCard PLUS PostCard/Lunch-Check/Stripe, die NICHT über Adyen laufen — dürfen NIE in isCard, sonst falsche rote Adyen-Diffs); unklassifizierte Arten (z. B. KD Tisch 5000) beides false. Nicht-Karten (Bar/Gutschein/…) nur kommentier-/übersteuerbar. Overrides `{originalValue, correctedValue, correctedByManualOverride, comment?, updatedAt}` bidirektional (zbericht|adyen, FieldKey `date:source:methodKey`), schreiben NIE in `gn_*` zurück; Erst-Original bleibt bei Mehrfach-Override verankert. Diff-Ampel `ADYEN_DIFF_THRESHOLDS` (grün ≤0.05 / orange ≤5 / rot). Tagesbestätigung `canConfirmDay`: Z-Bericht vorhanden + jede nicht-grüne Differenz übersteuert ODER kommentiert + Barbestand bestätigt. Gäste (isGuest) rein lesend. Tests: `adyen-csv-parser.test.ts`, `adyen-abstimmung.test.ts` (node), `AdyenDayTable.test.tsx` (happy-dom).
- **Tagesabschluss-Monatsübersicht (analog Excel „UMSATZ Oliv"):** Section auf `/tagesabschluesse` (`src/components/umsatzabstimmung/` = `TagesabschlussSection.tsx` [Orchestrierung] / `TagesabschlussTable.tsx` [**14 Spalten in 5 Gruppen** (`TAGESABSCHLUSS_COLUMN_GROUPS` exportiert, 2-zeiliger Header mit Gruppen-Tint + Trennlinien); **kompakte Standardansicht** = 10 Spalten via `detailOnly`-Flag (kk/einzahlungBank/cashIst/cashDiff ausgeblendet), reine Helper `visibleColumns(group, showAll)` exportiert; Prop `showAllColumns` (default false), Section-Toggle `ta-columns-toggle` („Alle Spalten anzeigen"): Umsatz (Datum, Umsatz) | Kartenzahlungen (KK = Karten **inkl. TWINT** laut Z-Bericht; **KK- und KK-Adyen-Zellen sind klickbare Popover-Buttons** (`ta-kk-btn-DATE`/`ta-adyen-btn-DATE` → `ta-kk-popover-DATE`/`ta-adyen-popover-DATE`, kein Tooltip mehr): KK-Popover = volle Zusammensetzung aller `isKkCard`-Arten (auto-Werte, feste Reihenfolge `KK_BREAKDOWN_ORDER` MC/Visa/TWINT/Amex/PostCard/LunchCheck/Stripe), KK-Adyen-Popover = NUR Adyen-Arten (effektive Werte inkl. Overrides aus `buildDayComparison`) + `ADYEN_HINWEIS`-Text + Sektion „Nicht über Adyen" (`isKkCard && !isCard`); gemeinsamer Renderer `ZahlungsartenBreakdown.tsx` (Total fett, negativ rot, auto-Posten „Korrektur (manuell)" wenn |Total−Summe|≥0.005) — DERSELBE auch im Tagesdetail; KK zählt ALLE `isKkCard`-Arten inkl. Amex/PostCard/Lunch-Check/Stripe; unklassifizierte Zahlarten wie KD Tisch 5000 zählen NIRGENDS mit; TWINT/Netto/MWST OHNE eigene Spalte; KK Adyen = farbig nach Diff-Status, Diff in Klammern in derselben Zelle — KEINE separate Adyen-Differenz-Spalte mehr, Total-Zeile behält `ta-total-adyen-diff` mit Vorzeichen-Aufhebungs-Hinweis) | Kasse (**Bargeld Soll** = berechnet/read-only mit Formel-Tooltip, Einzahlung Bank, **Kassensaldo Soll** = fortlaufend/read-only („—" + Hinweis-Tooltip solange Anfangsbestand unbekannt), **Cash Ist** = manuell inline (blau), **Cash Diff** = Ist − Kassensaldo Soll farbig nach Ampel, „—" solange Ist/Saldo fehlt; bei nicht-grüner Diff Badge „Begründet" (teal, Tooltip = Gründe + Notiz, `ta-diff-begruendet-DATE`) bzw. Button „Begründen" (`ta-diff-begruenden-DATE`) → `onReasonsClick` öffnet `TagesabschlussReasonDialog.tsx`) | Weitere Zahlungsarten (Debitoren=Rechnung nur als Tabellen-Label, VerkG/EingG) | Ausgaben (Barausgaben) + Status. Statusfarben (Badge + Zeilen-Tint-Präzedenz): bestätigt-grün > bestätigt_mit_differenz-GELB > Diff-rot/orange > offen-ROT > Zebra+Hover, Heute-Zeile `data-today` + Primär-Border + fettes Datum, negative Beträge rot; Bemerkung NUR als Icon am Datum (title) + im Dialog, KEINE Bemerkung-Spalte/-Inline-Edit mehr; **Dialog öffnet NUR über die link-gestylte Datum-Zelle** (Button `ta-date-…`, KEIN Zeilen-Klick); **Inline-Edit direkt in der Zeile**: Cash Ist/Einzahlung Bank (manuelle Felder) UND Debitoren (`onCorrectRechnung` = Korrektur-Override auf `rechnung`, Wert==Original/leer entfernt Korrektur, Override-Kommentar bleibt erhalten) via `InlineAmountInput` (parseAmountInput) — Commit bei Blur/Enter, Escape verwirft, Save nur bei echter Änderung, Leereingabe löscht (null), manuell = blau, korrigiert = gelb (`corrected`-Prop); **VerkG/EingG-Zellen sind Buttons** (`ta-gutschein-verkauft/-eingeloest-DATE`) → `TagesabschlussVoucherDialog` (Betrag = Override auf gutscheinVerkauft/-Eingeloest mit verankertem Original, Nummern kommagetrennt → `upsertManualDay`-Patch, Feld-Kommentar → `setTagesabschlussComment`; Save = `handleVoucherSave` in Section: EIN Blob-Zwischenstand durch alle drei Mutationen, EIN persist); **Barausgaben-Zelle ist Button** (`ta-expenses-DATE`, zeigt weiter das Total) → eigener `TagesabschlussExpenseDialog` (KEIN Tagesdetail); **Override-Popup für Umsatz/KK:** Edit-Pencils `ta-override-umsatz-DATE`/`ta-override-karten-DATE` (nur mit `onOverrideClick` + Z-Bericht, nicht bei readOnly/gesperrt) → `TagesabschlussOverrideDialog.tsx` (generisch; UI-Labels `OVERRIDE_UI_LABEL` „Umsatz Ist"/„KK Adyen Ist" — Export-Labels `TAGESABSCHLUSS_FIELD_LABEL` bewusst UNVERÄNDERT; Override auf Felder `umsatz`/`karten` mit verankertem Erst-Original, leeres Feld oder Original-Wert entfernt die Korrektur, Kommentar → `setTagesabschlussComment`, Save = EIN persist in `handleOverrideSave`; karten-Dialog zeigt Orientierungsblock `ta-override-context` mit TWINT + Adyen-Import — karten-Override selbst OHNE TWINT; testids `ta-override-original/-betrag/-kommentar/-save`); KK-Adyen-Zelle zeigt bei karten-Override den EFFEKTIVEN Z-KK-Wert (karten+TWINT) amber (`ta-adyen-effektiv-DATE`) statt des Adyen-Popovers — Adyen-Vergleichswerte (`buildDayComparison`) bleiben unberührt; Gruppen-Tints kräftiger (bg-*-100), `border-l-2`-Gruppentrenner, focus-within-Tint auf Edit-Zellen; ohne die neuen Callbacks rendern diese Zellen statisch (rückwärtskompatibel); Bestätigungs-Checkboxen (Barbestand/Tag) in der Status-Spalte nur für Tage mit Z-Bericht, identische Semantik wie im Dialog, gemeinsamer Adyen-Store; ohne `onSaveManual`/bei `readOnly` (Gast) rein statisch] / `TagesabschlussDayDialog.tsx` [manuelle Werte inkl. **Gutscheinnummern verkauft/eingelöst** (kommagetrennt → `string[]`, NUR im Dialog sichtbar, NIE in der Übersicht; Korrektur-Labels bewusst weiter `TAGESABSCHLUSS_FIELD_LABEL` = „Kreditkarten / Adyen / SIX"/„TWINT"/„Rechnung" wegen Export), Korrekturen, Barausgaben, Bestätigung + read-only Cash-Block (Formelbestandteile + Soll/Ist/Diff) + read-only Adyen-Block (Z/Adyen/Diff, Verweis auf Adyen-Abgleich; rendert zusätzlich `ZahlungsartenBreakdown` `ta-dialog-adyen-breakdown` mit Hinweis + „Nicht über Adyen") + Sektion „Zahlungsarten" = KK-Zusammensetzung via `ZahlungsartenBreakdown` (`ta-dialog-kk-breakdown`, erscheint sobald KK-Arten existieren) + aufklappbares „Weitere Zahlungsarten" NUR bei `weitereZahlungsarten.length>0` (default ZU, Reset bei Tageswechsel, testids `ta-dialog-zahlungsarten`/`ta-weitere-zahlungsarten-toggle`/`-list`; Quelle rein `collectWeitereZahlungsarten` = `RARE_KK_KEYS` (amex/postcard/lunch_check/stripe, „im KK-Total enthalten") + unklassifizierte Arten („in keiner Berechnung enthalten"), aggregiert je Key, label-sortiert; `TagesabschlussRow.weitereZahlungsarten` Pflichtfeld; Popover-Daten als Row-Pflichtfelder `kkZusammensetzung`/`adyenZusammensetzung`/`nichtAdyenKk` aus rein `collectKkBreakdown`/`collectNichtAdyenKk` (`ZahlungsartPosten`) — Export UNVERÄNDERT)] / `TagesabschlussVoucherDialog.tsx` [kleiner Gutschein-Dialog aus der Tabelle] / `TagesabschlussOverrideDialog.tsx` [Override-Popup Umsatz/KK Adyen aus der Tabelle] / `TagesabschlussExpenseDialog.tsx` + `TagesabschlussExpenseEditor.tsx` [Barausgaben-Dialog; Editor (Liste+Formular, testids `ta-exp-*`/`ta-expense-*`) wird von Barausgaben-Dialog UND Tagesdetail gemeinsam genutzt] / `TagesabschlussExportDialog.tsx` [Konto-Mapping + CSV]). Reine Logik `src/lib/tagesabschluss.ts`: Blob `tagesabschluss_v1` {days, expenses, overrides, comments, exportSettings}; 10 Auto-Felder aus Z-Bericht (`deriveAutoValues` via `normalizeGnPaymentName`; verkaufte Gutscheine aus `accountingLines` /gutschein/ ohne /einge/) + 2 manuelle (Cash Ist = Bestand Kasse, Einzahlung Bank) + Bemerkung + Gutscheinnummern verkauft/eingelöst (`string[]`, getrimmt, leer→Feld weg); Zell-Provenienz `DayCell.source` auto/manual/corrected/missing (UI: manuell = blau, korrigiert = gelb, Kommentar-Icon); Barumsatz = Umsatz − Karten − TWINT − Rechnung − eingelöste Gutscheine (Karten = `isKkCard`-Summe; unklassifizierte Arten bleiben implizit im Barumsatz); Export bucht jede `isKkCard`-Art separat je Konto-Mapping (ohne Mapping → KK-Sammel 1110), unklassifizierte Zahlarten (KD Tisch & Co.) auf eigenes Konto-Mapping (Pflicht, sonst Export-Fehler); **Kassenabschluss-Logik (fortlaufender Saldo):** Bargeld Soll = Umsatz − KK − Rechnung − Barausgaben − eingelöste + verkaufte Gutscheine (nur Tage mit Z-Bericht); Kassensaldo Soll = Saldo Vortag + Bargeld Soll − Einzahlung Bank; Startwert via `resolveKassensaldoStart` — **lückenlos über ALLE Monate/Jahre** (expliziter `anfangsbestand['YYYY-MM']` des Monats gewinnt ohne Load; sonst jüngster FRÜHERER Anker synchron aus den Blob-Schlüsseln + Vorwärts-Kette über `computeMonthEndSaldo`, Leermonate/Jahreswechsel laufen als 0-Beitrag durch, `KASSENSALDO_MAX_CHAIN_MONTHS`=120 nur Schutzkappe; ohne früheren Anker bzw. Monat VOR dem ältesten Anker → null — **NIE stille 0**; Saldo/Diff zeigen dann „—", Banner `ta-anfangsbestand-*` über der Tabelle erfasst den Anfangsbestand via `setAnfangsbestand`; Anfangsbestand EINMALIG erfassen + später via „ändern" an KPI `ta-kpi-saldo-anfang` bearbeitbar (`ta-anfangsbestand-edit/-cancel`, öffnet Banner vorbefüllt, Speichern setzt expliziten Monats-Anker und übersteuert die Kette; Alt-Tag-Änderungen rechnen alle Folgesalden automatisch neu — keine Persistenz von Zwischenständen)); Cash Ist = manuell gezählter Bestand; Cash Diff = Ist − Kassensaldo Soll (null solange Ist/Saldo fehlt), Ampel = `ADYEN_DIFF_THRESHOLDS`; Differenzgründe je Tag `setCashDiffReasons` (getrimmt/dedupliziert, Katalog `CASH_DIFF_REASONS`/`cashDiffReasonLabel`, freie Notiz; leer+leer löscht) → Status **`bestaetigt_mit_differenz`** = bestätigt + nicht-grüne Cash-Diff + (Grund ODER Notiz); Blob-Felder `cashDiffReasons` {date:{reasons[],note?,updatedAt}} + `anfangsbestand` {YYYY-MM:{value,updatedAt}} (merge je Schlüssel nach updatedAt); `buildTagesabschlussRows` 7. Param `startSaldo` (fehlend/null = unbekannt); Totals `bargeldSoll`/`kassensaldoEnde` (null ohne Anker)/`cashIst`/`letzteCashDiff`+`letzteCashDiffStatus` (letzter Tag MIT Ist)/`daysWithCashDiff`. **Adyen-Integration (nur ANZEIGE):** `buildTagesabschlussRows` nimmt optionalen 6. Param `adyenBlob` (rückwärtskompatibel, ohne Blob bleiben Adyen-Felder null) und ruft je Tag `buildDayComparison` — exakt dieselben effektiven Werte (inkl. Adyen-Overrides) wie die Adyen-Abgleich-Section; Row-Felder `hasAdyen`/`adyenTotal`/`adyenZTotal`/`adyenDiff`/`adyenDiffStatus` (Diff nur wenn BEIDE Seiten existieren). 10 Monats-KPI-Kacheln über der Tabelle (`ta-kpi-*`: Tage abgeschlossen `daysConfirmed`/`daysWithZbericht`, Tage offen rot>0, Tage mit Differenzen (Adyen ODER Cash) rot>0, Total Barausgaben, Total Einzahlung Bank, **Kassensaldo Anfang Monat** `ta-kpi-saldo-anfang` (= `resolveKassensaldoStart`, „—" ohne Anker, mit „ändern"-Button), **Kassensaldo Monatsende** `kassensaldoEnde` („—" ohne Anker), **Total Bargeld Soll**, **Differenzen begründet/unbegründet** (Tage mit nicht-grüner Cash-Diff mit/ohne Grund oder Notiz)); Anfangsbestand-Banner `ta-anfangsbestand-banner/-input/-save` erscheint, solange `resolveKassensaldoStart` null liefert (Alt-Monate zeigen dann „—" statt Saldo/Diff). Barausgaben EINZELN erfasst (Betrag/Konto/GKto/Text/MWST-Code/BelegNr/Kommentar), Übersicht zeigt NUR das Tages-Total, Export exportiert jede einzeln. Bestätigung = GEMEINSAMER Adyen-Store (`adyenAbstimmung_v1.confirmations` via `setDayConfirmation` — KEIN zweiter Bestätigungs-Store); Status fehlt/offen/bestätigt/**bestätigt_mit_differenz** — „bestätigt" verlangt Häkchen + Cash Ist erfasst + Cash-Diff grün; „bestätigt_mit_differenz" = Häkchen + Cash Ist + nicht-grüne Diff MIT Grund oder Notiz (sonst offen); `daysConfirmed` zählt beide. Overrides eigener Namespace `date:field` (Erst-Original verankert, schreibt NIE in `gn_*`). **Gemeinsamer Adyen-Blob, Frisch-Mutation Pflicht:** beide Sections auf `/tagesabschluesse` schreiben `adyenAbstimmung_v1` — Mutationen IMMER `loadAdyenAbstimmungLocal` (sync, localStorage) → reine Mutation → `saveAdyenAbstimmung` (NIE Mount-Zeit-State zurückschreiben); Save feuert `ADYEN_ABSTIMMUNG_UPDATED_EVENT`, beide Sections ziehen ihren State per Listener nach. Export rein `src/lib/tagesabschluss-export.ts`: **Brutto-Modell** — KEIN MWST-/Umsatz-Split mehr (kein 2200/3000, `steuer` immer 0/leer, `konten.umsatz`+`mwstCodes` nur LEGACY im Typ, unvalidiert): jede Zahlweg-Zeile bucht brutto Kto=Kanal/GKto=1098 (Bar 1000, KK-Sammel 1110 = MC/Visa/Maestro/TWINT, Einzelkonten via `kontoJeZahlungsart`, Debitoren 1100, Gutschein eingelöst 2003, unklassifizierte Arten je eigenes Mapping z. B. KD Tisch 5000→1104 — ohne Mapping EXPORT-FEHLER via `collectUnclassifiedZahlarten`, nie still im Barumsatz); Pflicht-Balance Σ(GKto=1098)=Tagesumsatz; keine Nullzeilen (Betrag 0 wird nicht emittiert); Gutschein verkauft Kasse→Gutscheinkonto; **Einzahlung Bank wird NICHT exportiert** (steuert nur den Kassensaldo, Bankbuchung kommt separat aus dem Bankbeleg/Bankimport — `konten.bank` LEGACY, nicht mehr validiert; Monatsprüfung zeigt sie nur als Kontrollwert); Barausgaben einzeln (Vorsteuer-`mwstCode` je Ausgabe bleibt), 20 Spalten `TABELLE2_HEADERS`; CSV quotet auch Kommas (`escapeCsvCell`); **Export nutzt durchgängig EFFEKTIVWERTE (`DayCell.value` = korrigiert > manuell > auto; Barumsatz aus Effektivwerten neu berechnet — Balance Σ(GKto=1098)=Umsatz strukturell garantiert): karten-/twint-Overrides werden NICHT auf Einzelarten verteilt, sondern als EIGENE Korrektur-Zeile auf das KK-Sammelkonto gebucht (tx `KK/TWINT Korrektur (manuell) DATUM`); enthaltene Korrekturen werden als `warnings` (`collectExportOverrideWarnings`) im Dialog nicht-blockierend als Info gelistet**. Export BLOCKIERT bis Konto-Mapping vollständig + `reviewed` (kein stiller Fallback); `defaultExportSettings` = echter „Kontoplan Oliv" (1000/1020/1100/2003/1110, amex 1114/postcard 1116/lunch_check+just_eat 1115/stripe 1118/kd_tisch_5000 1104 — MC/Visa/Maestro/TWINT bewusst OHNE Einzelkonto → KK-Sammel 1110). Persistenz `tagesabschluss-db.ts` = Settings-Infra + merge-on-save (`mergeTagesabschlussBlobs`, je Schlüssel jüngster `updatedAt` gewinnt — kein naiver Blob-Write, KEINE Migration); Section-`persist` merged das Save-Resultat FUNKTIONAL gegen den aktuellen State (Out-of-Order-Saves bei schnellen Inline-Edits verdrängen keine neueren Edits). **`upsertManualDay` patcht NUR vorhandene Keys** (`TagesabschlussManualPatch`, 5 Keys; null/leer/leeres Array löscht das einzelne Feld) — Inline-Einzel-Feld-Patches löschen keine anderen manuellen Werte; `handleSaveManual` reicht Patches 1:1 durch. Z-Bericht-Loader read-only `loadGnDayClosingsForMonth` (gn-zbericht-db.ts, NUR Tagesimporte, summiert Mehrfach-Importe, Trinkgeld = total_gross − total_excl_tip). Gäste rein lesend (readOnly blendet ALLE Edit-Flächen inkl. Gutschein-/Barausgaben-Buttons aus). **Abschluss-/Sperrmechanismus:** Blob-Felder `abschluesse` {date: DayClosure {status abgeschlossen|abgeschlossen_mit_differenz|wieder_geoeffnet, closedAt/By, fixedKassensaldo, reopenedAt/By/reopenReason, history[]}} + `monatsabschluesse` {YYYY-MM: MonthClosure inkl. `snapshot` (buildMonthClosureSnapshot: KPI-Zahlen zum Abschlusszeitpunkt) + `bereitFuerBuchhaltung`}; merge je Schlüssel nach updatedAt, Reopen = Status-Flag statt Löschung (merge-on-save würde gelöschte Keys wiederbeleben). Row-Status 5-stufig `fehlt|offen|in_bearbeitung|abgeschlossen(_mit_differenz)|wieder_geoeffnet` — Closure-Record gewinnt; „in Bearbeitung" sobald IRGENDEIN Arbeitsstand (Häkchen/Cash Ist/Einzahlung/Ausgabe/Korrektur/Begründung), Alt-Tage mit Häkchen werden NICHT auto-migriert; grüner Zeilen-Tint NUR bei `abgeschlossen` (bestätigt allein = sky „In Bearbeitung"). `canCloseDay` (Z-Bericht + Tagesbestätigung + Barbestand + Cash Ist + Saldo bekannt + Diff grün ODER begründet) → `closeDay` (wirft bei Blockern, fixiert `fixedKassensaldo`); gesperrte Zeile/Dialog komplett read-only (Table `rowLocked`, Dialog `dialogReadOnly`, zeigt fixierten Saldo), Adyen-Section prüft `isDayClosed` FRISCH aus localStorage (`loadTagesabschlussLocal`) in handleOverride/-Comment/-Confirm. `reopenDay` NUR Admin (`canReopen = isAdmin && !isGuest`) mit PFLICHT-Grund (`ta-reopen-reason/-day`), Historie wächst (`ta-dialog-closure-history`). `needsReview` = fixierter ≠ neu berechneter Saldo (Alt-Tag-Änderung) → Warn-Icon `ta-review-DATE`, gesperrte Zeile zeigt weiter den FIXIERTEN Saldo. Monatsabschluss (`ta-monatsabschluss`-Panel): `canCloseMonth` = jeder Z-Bericht-Tag definitiv abgeschlossen → `closeMonth` (Snapshot) / `reopenMonth` (Admin, ohne Grund) / `readyForBuchhaltung`-Toggle `ta-monat-bereit`; Export nur aus abgeschlossenen Tagen. KPI-Gruppen „Kasse" + „Abschluss" (`ta-kpi-in-bearbeitung/-wieder-geoeffnet/-needs-review`); Totals `daysAbgeschlossen(MitDifferenz)/daysInBearbeitung/daysWiederGeoeffnet/daysNeedsReview`. Tests: `tagesabschluss.test.ts`, `tagesabschluss-export.test.ts` (node), `TagesabschlussTable.test.tsx`, `tagesabschluss-dialogs.test.tsx` (happy-dom). **Excel-Export der Übersicht** (zusätzlich zum Buchhaltungs-CSV): Button `ta-excel-export` im Section-Header (auch für Gäste — rein lesend; disabled ohne Datentage) → `src/lib/tagesabschluss-excel-export.ts` (rein `buildTagesabschlussExcelData` + Workbook-Builder auf `xlsx`): 12 Spalten (Datum/Umsatz/Bargeld Soll/Kassensaldo Soll/KK Adyen/Barausgaben/Debitoren/VerkG/EingG/Einzahlung Bank/Kassendifferenz/Kommentar), NUR Tage mit Status ≠ `fehlt`, EFFEKTIVWERTE; „KK Adyen" mit Tabellen-Doppelsemantik (karten-Override → effektiver Z-KK inkl. TWINT, sonst Adyen-Total), Kassensaldo bei gesperrten Tagen = fixierter Saldo; Kommentar-Spalte = Bemerkung + Feld-Kommentare + Kassendifferenz-Begründung (Tombstones gefiltert); Summenzeile „Total" (Kassensaldo-Spalte = Monatsend-Saldo, null ohne Anker → leer); Datum als ECHTE Excel-Datumszelle über deterministische UTC-Serial (`excelDateSerial`, KEIN cellDates — vgl. Mirus-1904-Bug), Beträge als echte Zahlen (`#,##0.00`); Dateiname `tagesabschluesse_YYYY-MM.xlsx`. Test: `tagesabschluss-excel-export.test.ts` (node).
- **Tagesabschluss Inline-Buchungswerte + Import-Abgleich:** Alle buchungsrelevanten Zahlen direkt in der Tabellenzeile editierbar (`InlineAmountInput`, korrigiert = gelb): **KK Adyen** (`ta-input-karten-DATE` = Korrektur-Override auf `karten`, Wert==Original/leer entfernt, Pencil öffnet weiter den Override-Dialog), **Kassensaldo Soll** (`ta-input-saldo-DATE` = Tages-Anker via `setSaldoAnker`; Blob-Feld `saldoAnker` {date:{value,updatedAt}}, merge nach updatedAt; re-based die Saldo-Kette ab dem Tag — auch aus null-Kette ohne Monats-Anker; Row-Felder `saldoAnker`/`saldoBerechnet` (Wert VOR Anker), Tooltip zeigt beide; Entfernen stellt die berechnete Kette wieder her; gesperrte Tage zeigen weiter `fixedKassensaldo`), **Gutscheine VerkG/EingG** (`ta-input-gutschein-verkauft/-eingeloest-DATE` = Override, kleiner Pencil daneben öffnet weiter den VoucherDialog für Nummern/Kommentar), **Barausgaben-Total** (`ta-input-expenses-DATE` → `upsertInlineExpense`: EINE generische Ausgabe `inlineExpenseId(date)`=`inline-DATE`, Konto `INLINE_EXPENSE_DEFAULT_KONTO`=1001, Text „Barausgabe (Inline erfasst)"; nur solange `isInlineExpenseEditable` (keine oder NUR die Inline-Ausgabe) — sonst read-only-Total + Dialog; null/0 löscht; Plus-Button öffnet weiter den ExpenseDialog). **Bargeld Soll bleibt read-only** (berechnete Größe). Section-Handler blob-basiert mit Locked-/readOnly-Guards; CSV-Export nutzt automatisch die Ist-Werte (Effektivwert-Modell + KK-Korrektur-Zeile, unverändert). **Import-Abgleich beim Z-Bericht-Import:** rein `detectTagesabschlussImportConflicts(closings, blob)` (Konflikt = Tages-Import-Wert ↔ korrigierter Wert, |diff|≥0.005; `dayLocked` aus `abschluesse`-Status) + `applyImportConflictResolutions` ('uebernehmen' tombstoned den Override → Auto-Wert gilt; 'behalten' No-op; gesperrte Tage werden NIE angefasst; referenzgleicher Blob bei 0 Änderungen). **Tombstones statt Löschen (Pflicht wegen merge-on-save):** Entfernen von Overrides (`setTagesabschlussOverride` null), Saldo-Ankern (`setSaldoAnker` null), Barausgaben (`removeExpense`/`upsertInlineExpense` null), Feld-Kommentaren (`setTagesabschlussComment` leer, Typ `TagesabschlussComment extends AdyenComment`), Differenzgründen (`setCashDiffReasons` beides leer) und Anfangsbestand (`setAnfangsbestand` null) setzt `deleted: true` + frisches `updatedAt` statt den Key zu löschen — hartes Löschen würde beim Union-Merge gegen den Remote-KV sofort wiederbelebt; Löschen ohne Live-Eintrag bzw. auf Tombstone = referenzgleicher Blob (No-op); ALLE Leser (buildCell inkl. Zell-Kommentar, Rows inkl. `cashDiffBegruendet`, Kette/`resolveKassensaldoStart` inkl. Anker-Kandidaten + Section-Prefill, detect, Export, PDF-Kommentare, Section-Dialoge) filtern `deleted`; `computeMonthFingerprint` behält Tombstones BEWUSST (Löschung invalidiert den Export); erneutes Setzen reaktiviert (Flag weg), Erst-Original/letzter Stand bleibt am Tombstone verankert; Normalizer reicht `deleted` bei cashDiffReasons/anfangsbestand/saldoAnker durch. UI `TagesabschlussImportConflictDialog.tsx` (testids `ta-conflict-dialog/-all-keep/-all-take/-keep-DATE-FIELD/-take-DATE-FIELD/-cancel/-save`; Default „Manuellen Wert behalten", Massenaktionen, gesperrte Zeilen disabled mit Lock-Hinweis). Wiring in `GastronoviZBerichtPage.tsx`: nach Single- (periodFrom===periodTo) UND Batch-Import wird `checkTagesabschlussConflicts` best-effort aufgerufen (Import ist da bereits gespeichert; nur importierte Tage geprüft); Confirm lädt den Blob FRISCH, wendet an, speichert, Toast. Tests: neue describe-Blöcke in `tagesabschluss.test.ts` (setSaldoAnker/Inline-Expense/detect+apply).
- **Buchhaltungs-Export-Assistent (Monats-Export, `/tagesabschluesse`):** `src/components/umsatzabstimmung/BuchhaltungsExportSection.tsx` (testids `bx-*`), gerendert in `TagesabschlussSection.tsx` nach der Monatsübersicht (nur wenn Blob+Adyen geladen). Reine Logik `src/lib/buchhaltungs-export.ts`: Checkliste/Export-Gate (`buildExportChecklist` — alle Z-Tage abgeschlossen, Monat zu, Mapping ok, Adyen/Cash-Diffs begründet), Monatsprüfung (9 Totale), **Soll/Haben-Buchungsvorschau** (`buildSollHabenVorschau` über `buildTabelle2Rows`: jede Zeile doppelseitig Soll ODER Haben je Vorzeichen, Barausgaben einzeln mit Buchungstext + EINE aggregierte Kassen-Gegenseite je Konto; strukturell Soll≡Haben, leeres Konto → `bx-sh-differenz`; UI-Tabelle `bx-sh-*`, Zeilen `bx-sh-pos-GRUPPE-KEY`, Fusszeile „Buchung ausgeglichen — Soll X = Haben X"; Vorschau==CSV garantiert, da beide aus `buildTabelle2Rows`), **Kontrollwerte** `buildKontrollwerte` (`bx-kontrollwerte`: Einzahlung Bank/Kassensaldo & Co. NUR Anzeige, nie als Buchung), Konto-Labels `kontoLabel` (Fallback-Kette benutzerdefiniert → `DEFAULT_KONTO_BEZEICHNUNGEN` → Kontonummer). **Buchungsregeln** = umgebauter `TagesabschlussExportDialog` (Titel „Buchungsregeln", je Kontonummer editierbares Bezeichnungs-Feld `ta-exp-bez-*` mit Default-Placeholder, Save trimmt+droppt leere; Download-Button ENTFERNT — Export nur noch über die Section). `exportSettings.kontoBezeichnungen` {konto: label} in `tagesabschluss.ts`. Status `deriveExportStatus` = offen|bereit|exportiert|**veraltet** (Daten-Fingerprint über kanonisch serialisierte Monatsdaten + `computeSettingsFingerprint` über KONTEN — Konto-Änderung ⇒ veraltet, reine Bezeichnungs-Umbenennung bewusst NICHT (Abweichung Spec §9, JSDoc); Alt-Records ohne settingsFingerprint flippen nicht; Banner `bx-veraltet-banner` „Seit dem letzten Export wurden Daten geändert…"), Export-Protokoll/Versionierung `createExportRecord`/`addExportRecord` → Blob-Feld `exportProtokolle` {id: {monat, version, exportedAt/By, fingerprint, settingsFingerprint, anzahlBuchungen, **sollTotal/habenTotal** (Historie-Spalten `bx-export-vN-soll/-haben`, Alt-Records „—"), kassensaldoEnde}} (merge-on-save-kompatibel, KEINE Migration). CSV = bestehender Tabelle2-Export mit Dateiname `buchhaltungs-export-YYYY-MM-vN`; CSV-Klick lädt Blob FRISCH via `loadTagesabschlussLocal` vor der Mutation. PDF-Abschlussblatt rein `src/lib/monatsabschluss-pdf.ts` (dynamic import, jsPDF, KPI-Snapshot + Historie). Import-Cockpit: Kontrollaufgabe „Buchhaltungs-Export" in `import-center`-Checks (18 Quellen). Gäste read-only (keine Buttons, Checkliste/Prüfung/Historie sichtbar). Tests: `buchhaltungs-export.test.ts`, `monatsabschluss-pdf.test.ts` (node), `BuchhaltungsExportSection.test.tsx` (happy-dom).
- **Import-Center:** `/import` = `src/pages/ImportHub.tsx`, rein `src/lib/import-center.ts` (+ `-db.ts`). Launcher-Modell; Sichtbarkeit spiegelt echte Route-Guards (admin 9 / beaulieu_manager 3 / beaulieu_viewer 1 / Gast 0); nie Zeitstempel erfinden. **Importarten-Gruppen (UX Phase 2):** rein `src/lib/import-groups.ts` (`IMPORT_GROUPS` = 4 Gruppen Umsatz/Z-Bericht · Reservationen · Arbeitszeiten/AZB · Tagesabschluss/Kennzahlen; `buildImportGroupOverviews` nutzt `computeSourceStatus` + `startStatusFromCockpit` — KEINE eigenen Schwellen; Gruppen-Status = worst-of der Mitglieder, letzter Import = max(dataUntil ?? latestDataDate)) + UI `src/components/import-center/ImportGroupCards.tsx` (read-only via `fetchCockpitSignals`, testids `import-group-*`; „Import starten" = Route ODER `startAnchor` → `IMPORT_SECTION_OPEN_EVENT` + scroll). Inline-`Section`s auf /import sind seit Phase 2 default ZU (öffnen per Klick, URL-Hash `#id` oder Event); Header-Ghost-Link „Alle Quellen & Kontrollen" → `/import-cockpit` (showAdminSections-gated). Test: `import-groups.test.ts` (node).
- **Import-Cockpit (read-only Frische-/Fälligkeits-Übersicht):** `/import-cockpit` = `src/pages/ImportCockpitPage.tsx` (seit UX Phase 2 OHNE Nav-Eintrag — Zugang via Header-Link im Import-Center oder direkt per URL; Route+Guard unverändert), rein `src/lib/import-cockpit.ts` (Deskriptoren `COCKPIT_SOURCES`, Schwellen `INTERVAL_THRESHOLDS`, `computeSourceStatus`/`findMissingDays`/`summarizeCockpit`/`groupChecklist` + Label-/Badge-Maps), read-only Aggregator `src/lib/import-cockpit-db.ts` (`fetchCockpitSignals` via `Promise.allSettled`). Nur ANZEIGE — KEINE Migration/Schreibpfade/Änderung an Importprozessen. Admin-only, Gäste ausgeschlossen (`isAdmin && !isGuest` auf Route-Guard in `App.tsx` UND Lade-Effekt). **3-Tab-Struktur** (reine Logik `src/lib/import-cockpit-tabs.ts`, UI-Bausteine `src/components/import-cockpit/` = `cockpit-ui.tsx`/`CockpitDetailDrawer.tsx`/`DataImportsTab.tsx`/`ControlsTab.tsx`/`ImportChecklistTab.tsx`; Page orchestriert nur, `defaultValue="checklist"` — s. Bullet „Import-Checkliste"): **Datenimporte** (echte Datei-/Erfassungs-Importe, `section:'import'`), **Kontrollen** (wiederkehrende organisatorische Checks, `section:'control'`), **Aufgaben** (Tab + `TasksTab.tsx` im Juli 2026 durch den Checkliste-Tab ERSETZT/gelöscht; die reine `buildTasks`-Logik in `import-cockpit-tabs.ts` bleibt test-covered erhalten). Jede Quelle trägt `section` + `tabCategory` (`CockpitTabCategory`, 13 Werte; Reihenfolge `IMPORT_CATEGORY_ORDER`/`CONTROL_CATEGORY_ORDER`, Labels `TAB_CATEGORY_LABEL`); Kontrollen zusätzlich `responsible`/`importance`/`procedure` (Drawer). 17 Quellen = **10 Datenimporte + 7 Kontrollen** (11 prüfbar mit ableitbarem Signal, 6 reine Kontrollaufgaben `checkable:false`; neu `adyen` = monatlicher Datei-Upload, Signal read-only aus dem `adyenAbstimmung_v1`-localStorage-Blob via `adyenDaysFromBlob`, Frische = letzter importierter Adyen-Tag ≤ heute, `detectGaps:false`, Route `/tagesabschluesse`, actionLabel „Zu den Tagesabschlüssen"). **Umsatzabstimmung als monatlicher manueller Datenimport** (`manual_entry`, Label „Umsatzabstimmung", Route `/umsatzabstimmung`, keine Format-Badges): Signal read-only aus dem `reporting_v1`-localStorage-Blob via reiner `umsatzabstimmungMonthsFromBlob` (Monat gepflegt, sobald `grossRevenueManual` ODER `takeAwayGrossManual` > 0; `latestDataDate` = letzter gepflegter Monat 'yyyy-MM' — passt zu `normalizeDataDate`/Monatsübersicht), NIE via Reporting-Store-Funktionen (kein Seed/Write); Drawer zeigt Pflege-Hinweis (`COMPLETENESS_NOTE.umsatzabstimmung`) + Button „Zur Umsatzabstimmung". Frische je Intervall (täglich/wöchentlich/monatlich/jährlich); Datenlücken (`findMissingDays`, innere Löcher) NUR für tägliche `detectGaps`-Quellen (aktuell nur Tagesumsatz KV — Mirus ist periodenbasiert, `detectGaps:false`) und senken den Status höchstens auf gelb (Ruhetage sind legitim). Tenant-Filter folgt bestehenden Mustern (actual_hours/schedule_entries = `employee_id`-Präfix `b-` — `employees` hat KEINE `restaurant_id`, Reservations/CRM = `restaurant_id`, KV via `tenantKey`); `product_sales` ist mandantenübergreifend (`tenantNeutral`-Flag in der UI). **Budget/Reporting-Signal NIE via `loadBudgetYear`** (seedet+schreibt) — direkt aus dem localStorage-Blob (`budget_v1`/`reporting_v1`) lesen. Jede Quelle trägt zusätzlich `category` (7 Werte `CockpitCategory`), `importType` (`CockpitImportType`: file_upload/manual_entry/control/system/not_configured — `system` derzeit ungenutzt, nur `inventur` = not_configured), `uploadLabel` (was hochladen), optional `sourceHint`/`exampleFormat` (Datei-Upload-Quellen) und `actionLabel`. **Legacy Single-View-Layer** (bleibt im Modul für Rückwärtskompat + Tests, von der 3-Tab-UI abgelöst und NICHT mehr gerendert): `CockpitFilterState`/`rowMatchesFilters`/`matchesCockpitSearch`, `groupChecklist`/`visibleIds`, Maps `CATEGORY_LABEL`/`CATEGORY_ORDER`, `IMPORT_TYPE_LABEL`/`IMPORT_TYPE_ORDER`/`IMPORT_TYPE_BADGE_CLASS`, `STATUS_HINT`; das `category`-Feld (7 Werte `CockpitCategory`) ist `@deprecated` zugunsten `section`+`tabCategory`. UI je Tab: **Datenimporte** — KPI-Kacheln (Aktuell/Bald fällig/Überfällig/Nie+Nicht prüfbar/Datenlücken via `summarizeCockpit`) sind **Toggle-Filter** (Klick filtert die Tabelle, aktive Kachel markiert/aria-pressed, erneuter Klick hebt auf; reine Logik `ImportKpiFilter` + `importRowMatchesKpi`/`toggleImportKpiFilter`, Feld `kpi` in `ImportTabFilterState`; „Datenlücken" = `missingDays>0` unabhängig vom Status, „Nie/Nicht prüfbar" gebündelt); nach Bereich gruppierte Tabelle (Spalten Datenquelle/„Import-Art"/„Letzter Import"/„Ist-Daten bis"/Intervall/Status-Badge/Nächste Fälligkeit — KEINE „Was hochladen?"- und KEINE „Aktion"/Details-Spalte mehr, die GANZE Zeile ist klickbar → Drawer, cursor-pointer + Hover). „Import-Art" zeigt bei Datei-Uploads **Dateiformat-Badges** (CSV/Excel/PDF) statt „Datei-Upload": reine Ableitung `importFileFormats(def)` aus den `exampleFormat`-Endungen (csv/xls/xlsx/pdf, Reihenfolge CSV→Excel→PDF, NUR `importType:'file_upload'`, nie erfinden — sonst leere Liste und Fallback auf `ImportTypeBadge`, z. B. „Manuelle Eingabe"); Anzeige `FileFormatBadges` (cockpit-ui). Filter Bereich/Import-Art/Intervall/Status + Suche (KPI-Kachel wird UND-verknüpft, „Filter zurücksetzen" löscht auch sie). **Monatsübersicht** (Card zwischen KPI-Kacheln und Filterleiste): Jahresauswahl (Chevrons, Default = laufendes Jahr; Jahreswechsel überträgt einen aktiven Monatsfilter auf denselben Monat des neuen Jahres) + „Ganzes Jahr"-Button (aktiv = kein Monatsfilter) + Jahres-Totale (`summarizeImportYear`: aktuell/überfällig aus Quellen mit „Ist-Daten bis" im Jahr, nie = Nie-Quellen [0 für reine Zukunftsjahre], Lücken-Tage im Jahr — direkt aus Rows, NIE Monats-Summen [Nie würde 12× zählen]) + 12 Monatskarten (`buildMonthOverview`: aktuell/überfällig via Monat von `result.latestDataDate`, Lücken-Tage via `missingDays`-Monat, „nie" in jedem NICHT-zukünftigen Monat; Problem-Monate rot [überfällig/nie] bzw. amber [nur Lücken], aria-pressed). Monatsklick = Toggle-Filter (`month: 'YYYY-MM'|null` in `ImportTabFilterState`, reine `importRowMatchesMonth(row,month,today)`: Match via Ist-Daten-bis-Monat ODER Lücken-Tag im Monat ODER status never in nicht-zukünftigem Monat; UND-verknüpft mit allen anderen Achsen inkl. KPI-Kachel, z. B. Überfällig+Juli). `importRowMatchesFilters` hat optionalen 3. Param `today` (Default `localTodayIso()`), `DataImportsTab` optionale `today`-Prop (nur Tests). `due_soon`-Zeilen matchen den Monatsfilter, tauchen aber bewusst in keiner Zell-Zählung auf (Zellen zählen nur die 4 Spec-Kennzahlen); jahres-granulare `latestDataDate` ('yyyy') matcht keinen Monat, zählt aber im Jahres-Total. Komponententest `src/components/import-cockpit/__tests__/DataImportsTab.test.tsx` (happy-dom). **Kontrollen** — abgeleiteter `ControlStatus` (`done`/`due_today`/`due_soon`/`overdue`/`not_configured` via reiner `computeControlStatus`: `!checkable`|`never`|`uncheckable`→`not_configured`; `overdue`→`overdue`; `current`/`due_soon`→`due_today` wenn `nextDue===today` sonst `done`/`due_soon`), KPI-Kacheln (Erledigt/Heute fällig/Bald fällig/Überfällig/Nicht eingerichtet via `summarizeControls`), Tabelle mit Verantwortlich/Rhythmus/Nächste Fälligkeit, Filter Bereich/Intervall/Status + Suche. **Aufgaben** — reine `buildTasks(rows,today)` leitet aus Import-/Kontroll-Status OFFENE Punkte + Priorität (`critical`/`medium`) + `timeframe` (Intervall→today/week/month/year) ab (kritisch-zuerst sortiert), `groupTasksByTimeframe` in vier Karten (Heute/Woche/Monat/Jahr), KPI via `summarizeTasks`, Filter Bereich/Priorität/Zeithorizont + Suche. Prioritäten: Import `overdue`→critical, `never`/`due_soon`→medium; Kontrolle `overdue`/`due_today`→critical, `due_soon`→medium. **No-Fabrication:** neutrale Zustände (Import current/uncheckable, Kontrolle done/not_configured) erzeugen BEWUSST KEINE Aufgabe und werden nicht als offen gezählt (bewusste Abweichung von den gelben Spec-Beispielen für `uncheckable`/`not_configured`). Geteilter `CockpitDetailDrawer` über alle drei Tabs. Reine Filter-Layer je Tab: `importRowMatchesFilters`/`controlRowMatchesFilters`/`taskMatchesFilters` (+ `EMPTY_*_TAB_FILTER`). Weiterhin nur ANZEIGE — keine Upload-/Importlogik, keine Migration. Tests: `import-cockpit.test.ts` (582 Z., unverändert) + `import-cockpit-tabs.test.ts`. Nav: eigener Eintrag in der Import-Gruppe (`AppNav.tsx`). **„Ist-Daten bis" (früher „Stand / Daten bis"):** die Spalte/der Drawer zeigen den letzten ECHTEN Ist-Tag, nie ein Zukunftsdatum. Bei täglichen `detectGaps`-Quellen wird ein zukünftiges MAX-Datum auf heute gekappt und als `ignoredFutureDate` (Signalfeld `futureDataDate`) im Drawer als amber Hinweis ausgewiesen; `computeSourceStatus` nutzt durchgängig das gekappte `refRaw` für `latestDataDate`/Reason. **Vollständigkeit statt MAX:** reine `lastGaplessDay(covered, today)` liefert den letzten lückenlos verfügbaren Tag (bricht beim ersten Loch ab, ignoriert Tage > heute) → `completeUntil` im Drawer als „Vollständig importiert bis" (nur `detectGaps`). Lücken senken Status weiterhin höchstens auf gelb. **Mirus PERIODENBASIERT (nur im Aggregator, read-only):** „Ist-Daten bis" ist NICHT MAX(date) aus `actual_hours`, sondern das Monatsende der zuletzt erfolgreich importierten Mirus-Periode aus der **Import-Historie `timesheet_import_history`** (Quelle 'mirus', via `getImportHistoryAll`) — so heben verirrte Einzel-Tageszeilen (z. B. ein 04.07-Datensatz obwohl nur bis 30.06 importiert) den Stand nicht fälschlich an. Reine Ableitung: `deriveMirusPeriodFromHistory(entries)` (max year·12+month unter verwertbaren [source null|'mirus', importedCount null|>0], Tie-Break jüngster `importedAt`; → `periodFrom`=Monatsanfang, `periodTo`=Monatsende) + `deriveMirusPeriodEndFromDays(days,today)` (Fallback ohne Historie: Monatsende des letzten Monats VOR dem laufenden mit echten Ist-Tagen). **Kappen im Aggregator:** ein `periodTo` im laufenden Monat (Teil-Import) wird in `mirusSignal` auf heute gekappt (`min(periodTo,today)`) und das Monatsende als `futureDataDate`-Hinweis ausgewiesen — „Ist-Daten bis" bleibt nie ein Zukunftsdatum. Ohne ableitbare Periode → Status `never` (nicht `uncheckable`; Drawer behält Hinweise). `actual_hours` liefert nur noch **Drawer-Hinweise**, die NIE in Status/„Ist-Daten bis" einfliessen: `latestRecordDate` (spätester echter Ist-Tag ≤ heute, `.is('absence_type',null)` + `.or(hours>0,start_time≠null,end_time≠null)`) und `futureDataDate`. Interval bleibt `daily` → 30.06 vs. heute = mehrere Tage hinter = überfällig (nie „aktuell"). Drawer-Bausteine: „Datei" (`fileName`), „Letzter gefundener Tagesdatensatz" (`latestRecordDate`, nur wenn ≠ „Ist-Daten bis"), amber Hinweis bei späteren Tagesdatensätzen > Perioden-Ende, `COMPLETENESS_NOTE.mirus`. Tagesumsatz/Z-Bericht/Produktverkäufe weiterhin auf ≤ heute gekappt; Reservationen/Gäste-CRM/Dienstplan bleiben bewusst zukunfts-fähig. **Deviation:** Spec nannte `import_runs`, doch das ist Foratable-only — die echte Mirus-Historie liegt in `timesheet_import_history`. Keine Migration/Schreibpfade. **Manuelle Kontroll-Erledigung (EINZIGE Ausnahme vom read-only):** Kontrollen (`section:'control'`) + Kontroll-Aufgaben können per Mehrfachauswahl (Zeilen-/Gruppen-/Master-Checkbox) über einen bestätigten Sammel-Button „Als erledigt markieren" manuell abgeschlossen werden. Echte **Datenimporte** (`section:'import'`) NIE — in den Aufgaben werden sie beim Erledigen mit `partitionTasksForCompletion` abgetrennt und per Toast „Datenimporte können nur durch den passenden Import abgeschlossen werden." blockiert. Reine Logik `src/lib/import-cockpit-checks.ts` (`ManualCompletion{completedAt,nextDue}`/`ManualCompletionMap`, `nextDueAfterCompletion` spiegelt `computeNextDue`: täglich+1/wöchentlich+7T/monatlich+1M/jährlich+1J, `makeCompletion`/`markControlsDone`/`isCompletionActive` = `today<nextDue`/`pruneCompletions`/`partitionTasksForCompletion`); Persistenz `src/lib/import-cockpit-checks-db.ts` = **bestehende** Settings-Infra (localStorage primär via `tlsGetJson`/`tlsSetJson` + best-effort Supabase-KV-Backup `app_settings` via `kvGet`/`kvSet`, tenant-scoped `tenantKey(tenantId,'importCockpitControlChecks')`, wirft nie) — KEINE neue Tabelle, KEINE Migration, KEIN SQL. `buildControlRows`/`buildTasks` nehmen optionalen 3. Param `completions` (rückwärtskompatibel): aktive Erledigung → `controlStatus:'done'` + überschriebene `nextDue` + `manuallyCompleted`/`completedAt`; Import-Zeilen bleiben unberührt. Page hält `manualChecks`-State (Load bei `tenantId`-Wechsel), Handler filtert defensiv auf `section:'control'`. Drawer zeigt bei aktiver Erledigung `done` + „Zuletzt erledigt (manuell)" + neue Fälligkeit. Tests: `import-cockpit-checks.test.ts` (26 Z.).
- **Import-Checkliste (Juli 2026, Umbau `/import-cockpit`):** Seite heisst jetzt „Import-Checkliste" (`ImportCockpitPage.tsx`, PageShell/PageHeader, 3 Tabs: **Checkliste** [default] / **Status** [= bisheriger Datenimporte-Tab] / **Kontrollen**; `TasksTab.tsx` GELÖSCHT). Engine rein `src/lib/import-tasks-engine.ts`: 9 Importtypen (`TASK_TYPE_DEFS` — zbericht/reservationen/umsatz/verkaufsdaten/mirus/marketing tages-/zeitraumbasiert, erfolgsrechnung/istkosten monatlich, budget jährlich), `buildImportTasks(year,month,coverage,today)` erzeugt pro Monat Aufgaben mit stabilen IDs (`type:day`/`type:from:to`/`type:YYYY-MM`/`type:YYYY`/`type:error`); erwarteter Zeitraum IMMER auf min(Monatsende, gestern) gedeckelt; **Teilzeitraum-Restberechnung** (`missingRanges`: teilweise importierte Zeiträume → Restaufgaben nur für Lücken); laufender Monat/Jahr bei monthly/yearly = neutral „läuft noch" (keine falsche Fälligkeit); Coverage-Fehler → sichtbare error-Aufgabe, NIE still. `buildImportTarget` → Ziel-Route + Prefill-Query (zbericht→/gastronovi-import?from&to&scope, umsatz→/import?target=tagesumsatz, mirus/marketing→/import?target=…, erfolgsrechnung/istkosten→/reporting?target&year&month, budget→/budget?year, reservationen→/foratable-import, verkaufsdaten→/sales-upload). Coverage read-only `src/lib/import-tasks-db.ts` (`fetchMonthCoverage` via Promise.allSettled; zbericht=gn_imports-Perioden, reservationen=reservation_records, umsatz=`dailyBudgets`-KV mit actualRevenue>0, verkaufsdaten=product_sales [mandantenübergreifend], mirus=timesheet_import_history→Monats-Voll-Coverage + actual_hours, marketing=Maison-KV, erfolgsrechnung/istkosten/budget=localStorage/KV-Blobs read-only — KEINE Store-Loader [seeden/schreiben]); KEINE neue Tabelle/Migration. UI `src/components/import-cockpit/ImportChecklistTab.tsx` (Monats-Toolbar prev/next/Heute, „Erledigte anzeigen"-Toggle [Collapsible pro Gruppe], Frequenz-Gruppen, KPI-Zeile offen/erledigt, testids `checklist-*`; **Konflikt-Dialog** `checklist-conflict-dialog` bei „Ganzer Monat" über schon abgedeckten Zeitraum: Behalten/Ersetzen/Abbrechen — „Ersetzen" NAVIGIERT NUR zum Import, geschrieben wird hier NIE, der Ziel-Flow entscheidet). **Prefill advisory (nie harte Einschränkung):** rein `src/lib/import-prefill.ts` (`parseImportPrefill` validiert from/to/scope/target/year/month, `prefillRangeLabel`) + `src/components/ImportTaskPrefillHint.tsx` (HintBox, optional `forTarget`), eingebaut in GastronoviZBerichtPage, ForatableImportPage, SalesUpload, ImportHub (3 Hints tagesumsatz/mirus/maison + Scroll auf Sektions-Anker), Reporting (erfolgsrechnung/istkosten), Budget (`?year` = initiale Jahresauswahl). Ruhetage bleiben bewusst offene Z-Bericht-Tagesaufgaben (InfoTip-Hinweis statt Auto-Erledigung). Tests: `import-tasks-engine.test.ts` (20, node), `import-prefill.test.ts` (5, node), `ImportChecklistTab.test.tsx` (9, happy-dom — Coverage als Fixture-Prop, TooltipProvider-Wrapper nötig).
- **Foratable (Sammelseite):** `src/pages/ForatableImportPage.tsx` (`?tab=reservationen|gaeste|verlauf`); Alt-Routen `/reservationen-import`, `/gaeste-import` redirecten.
- **Reservations-Import:** `src/pages/ReservationenImportPage.tsx`, rein `src/lib/reservation-import-parser.ts`, `-db.ts`, Migration `20260621_reservations.sql` (`reservation_imports`/`guest_profiles`/`reservation_records`). Idempotent via UNIQUE `(restaurant_id, external_reservation_id)`. Gast-Match nur Telefon → E-Mail (nie Name); Name-only-Bookings de-duplizieren über `(restaurant_id, match_key)`.
- **Gästeexport-Import (CRM-Anreicherung):** `src/pages/GaesteImportPage.tsx`, rein `foratable-guest-import(-parser).ts`, `-db.ts`. Füllt NUR leere manuelle CRM-Felder — überschreibt nie, fasst Reservationen/Segmente/Besuchszähler nicht an. Match E-Mail → Telefon → Name (Ambiguität = „nicht zugeordnet").
- **Import-Historie:** Tabelle `import_runs` (`20260623_import_runs.sql`), rein `import-runs.ts` + `-db.ts`. `logImportRun` best-effort/wirft nie, additiv, keine PII, `finished_at` = DB `now()`.

### Gäste-CRM & Reservationen
- **CRM-Liste + Kern:** `src/pages/GaesteCrmPage.tsx`, rein `src/lib/reservation-crm.ts` (Single Source für Segment/Metrik — rein besuchsbasiert, `reservation_records` hat keine Geldspalte), `reservation-crm-db.ts` (read-only, tenant-isoliert). Manuelle CRM-Badges/Filter über `guest-list-filters.ts` (fehlendes CRM → alle Flags false). Optionale read-only View `guest_statistics` (`20260622_*`) spiegelt die TS-Semantik.
- **Manuelles CRM-Profil:** Tabelle `guest_crm_profiles` (`20260622_*`; kein `restaurant_id`, Tenant über Eltern-Gast), rein `guest-crm-profile.ts`, `-db.ts` (verifiziert Tenant vor Read/Write, wirft bei Schreibfehler). Strikt getrennt von berechnetem Segment/Score/Kampagnen.
- **Smart-Segmente:** rein `src/lib/guest-smart-segments.ts` (7 Live-Segmente, Mehrfachzugehörigkeit, strikt additiv, keine neue Query/Migration).
- **Detailseite (Kundenakte):** `src/pages/GaesteDetailPage.tsx` (admin `/gaeste/:guestId`), rein `reservation-guest-profile.ts`. Tabs Übersicht/CRM/Aktivität; gewichteter CRM-Score 0–100.
- **CRM Auswertung:** `src/pages/CrmAuswertungPage.tsx` (admin `/gaeste/auswertung`), rein `reservation-dashboard.ts`. Tabs Übersicht/Rückkehrpotenzial/Kampagnen; Kachel-Zählung und Drilldown-Liste stammen aus DENSELBEN Rows. View-State open-redirect-sicher in URL (`crm-auswertung-url.ts`).
- **Kampagnen:** rein `src/lib/reservation-campaigns.ts` (18 Listen; manuelle CRM-Kampagnen lesen nur `m.crm`, nie das berechnete Segment). CSV UTF-8 mit BOM (in der Seite), `;`-getrennt, CRLF, dt. Header.
- **Rückkehrpotenzial:** rein in `reservation-dashboard.ts` (Tab-Faktor `OVERDUE_INTERVAL_FACTOR=1.5`) + Gästeliste-Metrik (`RETURN_RISK_FACTOR=2.0`, `reservation-crm.ts`) — beide koexistieren bewusst.
- **Duplikate:** `src/pages/GaesteDuplikatePage.tsx` (admin `/gaeste/duplikate`), rein `guest-duplicates.ts`, `-db.ts`. Merge ohne DB-Transaktion: Preflight (Cross-Tenant-Abort) → Reservationen zuerst umhängen (FK SET NULL) → CRM-Merge → Aggregate NEU berechnen (nie summieren) → Postcondition-Check → Duplikate löschen → PII-freies Audit (`20260623_guest_merge_log.sql`).
- **Segment-Aktionen (intern):** `SegmentActionBar.tsx`, rein `crm-activities.ts`, `-db.ts`, Migration `20260624_crm_activities.sql` (manuell ausführen). `verifyGuestsBelongToTenant` vor JEDEM Write; keine E-Mails/externen Integrationen.
- **Reservations Analyse (konsolidiert):** `src/pages/ReservationAnalysePage.tsx` (admin `/gaeste/analyse`, Nav-Gruppe Foratable). Ersetzt die Nav-Einträge `/gaeste/wochentag` + `/gaeste/vorjahr` (Routen bleiben per URL erreichbar, wie `/reporting`). Globaler Kennzahl-Umschalter **Personen (Default)/Reservationen** steuert KPI-Kacheln, alle Tabellen und beide Detail-Popups. Filter: Jahr + Von/Bis-Monat + Status (booked Default) + Schnellwahl-Presets. 5 Tabs: **Monate** (Ist vs. Vorjahr, Zeilen-Klick → Tages-Popup), **Wochentage**, **Saison**, **Matrix** (Wochentag × Monat: Wochentage Mo–So als Zeilen, Ist-Monate als Spalten, hinten Total + Ø/Monat = Total ÷ Anzahl Monate; transponiert die Heatmap-Basis via `buildWeekdayMonthMatrix` — GLEICHE Daten wie Heatmap, kein zweiter Scan; stärkster/schwächster Wochentag nach Zeitraum-Total markiert via `weekdayMatrixExtremes`: nur Totals > 0, bei nur einem NUR stärkster, bei komplettem Gleichstand ≥2 NICHTS), **Heatmap** (Monat × Wochentag, Zell-Klick → Tages-Aufschlüsselung). Monats-Popup: 8 KPI-Kacheln (Res./Pers. je Ist+VJ, Diff/Diff% der aktiven Kennzahl, Ø Pers./Res. Ist+VJ via `buildMonthDetailKpis`), recharts-Balkendiagramm Mo–So Ist vs. VJ (`buildWeekdayIstVorjahrChartData`; recharts-`Tooltip` als `RechartsTooltip` aliasen — Kollision mit ui/tooltip), Wochentag-Zusammensetzung mit „Anteil Monat" (`buildMonthWeekdayComposition`, `WeekdayYoyTable` optional `shareByWeekday`), Tagesliste mit Wochentag (Ist-Datum, „—" bei nur-VJ) + Res.+Pers. + VJ/Diff der aktiven Kennzahl + Badge stärkster/schwächster Tag (`monthDetailDayFlags`, bei nur 1 aktivem Tag NUR stärkster). Reine Logik `src/lib/reservation-analyse-utils.ts` (reuse `reservation-yoy-utils.ts` + `reservation-weekday-analytics.ts`); keine Migration/Schreibzugriffe. Tests: `reservation-analyse-utils.test.ts`.
- **Abgelöste Reservations-Seiten (nav-versteckt, nur per URL):** `ReservationWochentagPage.tsx` (`/gaeste/wochentag`, rein `reservation-weekday-analytics.ts` — 3 Modi Zeitraum/Schulferien/Saisonvergleich, Saison-Defs in KV via `season-definitions-db.ts`), `ReservationVorjahrPage.tsx` (`/gaeste/vorjahr`, rein `reservation-yoy-utils.ts`). Aktiv verlinkt bleibt der **Foratable Report** `ForatableReportPage.tsx` (`/foratable-report`, rein `foratable-report.ts` + `reservation-time-analysis.ts`).

## Wichtige Invarianten (Guardrails)
Detail-Lehren + Historie in `.agents/memory/`. Kernregeln:
- Nie `restaurant_id` zu `employeeToDb`/`employees` hinzufügen — Tenant = ID-Präfix.
- Nur das Personalstamm-Formular schreibt `employees` nach Supabase; Auto-Sync/SchedulePlanner/Seed schreiben NICHT. Import-Match muss die VOLLE Mitarbeiterliste sehen (sonst Dubletten).
- Reporting-Monate nur via `safeUpsertReportingMonth`/`safeDeleteReportingMonth` (naives kvSet löscht andere Monate).
- Rollen-Gates nie „verbreitern" — ein weiter gefasstes Gate leakt alle Flächen, die es mit-gated. Stattdessen ein engeres Alt-Gate anlegen.
- Bulk-Upserts vor `.upsert()` nach dem `onConflict`-Key deduplizieren.
- Detail-`/:id`-Seiten: Fetched-State am Anfang von `load()` UND im `catch` zurücksetzen (sonst Cross-Guest-PII).
- Positions-/Staffing-Zuordnungen speichern **Keys**, nie Anzeigenamen.
- Overtime/Analytics: Absenz-Einträge (`absenceType`) sind nicht produktiv.

## Architecture decisions
- **Multi-tenancy:** ID-Präfixe (`b-` für Beaulieu) statt dedizierter `restaurant_id`-Spalte (Einfachheit + Rückwärtskompatibilität).
- **Data Persistence:** LocalStorage als schneller Primärspeicher, Supabase `app_settings` als persistentes Backup (v. a. Finanz-/Budgetdaten); Sync via `useSyncStore`.
- **Authorization:** zentraler `usePermissions`-Hook für rollenbasierten Zugriff.
- **Onboarding:** bestehende MA vervollständigen Daten per tokenisiertem Link; Selbstregistrierung erzeugt `pending_review`-Datensätze zur Admin-Freigabe.
- **Time-based Reporting:** globaler „Stichtag"-Filter (`StichtagContext`) für historische Auswertungen/Periodenabschluss.

## Product
- **Dashboard:** KPIs, Umsatz, Kosten, anteilige Personalkosten.
- **Dienstplanung:** Plan- und Ist-Schichten; Muster-Warnungen (aufeinanderfolgende Tage, kurze Ruhezeiten).
- **Soll/Ist-Analyse:** Plan- vs. Ist-Leistung.
- **Personalstamm:** Mitarbeiterverwaltung (admin), rollenbasierte Sichtbarkeit.
- **Reporting/Finanzen:** P&L, Budget, Vorjahresvergleiche.
- **Fixe Personalkosten:** Monatslöhne inkl. 13. Monatslohn je Abteilung.
- **Multi-Tenant + Rollen:** Oliv/Beaulieu isoliert; admin/service_manager/kueche_manager mit unterschiedlichem Zugriff.
- **Onboarding & Gäste-Links:** Selbstregistrierung; temporäre, passwortgeschützte read-only Freigabelinks.
- **Produktstammdaten & WES:** Food/Beverage-Artikel, WES (Warenkostensatz) je Produkt, Vergleich Rezept/Lieferant/Buchhaltung.
- **Gäste-CRM:** Gästeliste mit Suche, Besuchszahlen, Auto-Segmentierung, manuellem CRM-Profil, Kundenakte, Auswertungs-Dashboard, Kampagnen-Listen (CSV-only), Duplikat-Zusammenführung und Reservations-Analyse.

## User preferences

### Datenintegrität — verbindliche Entwicklervorgabe
- **Umsatzdaten dürfen niemals per localStorage-Overwrite oder naivem Blob-Write gespeichert werden.**
- Jeder Schreibpfad für `dailyBudgets` / `beaulieu:dailyBudgets` muss `safeUpsertDailyBudgets()` verwenden.
- `safeUpsertDailyBudgets` liest immer zuerst den aktuellen Supabase-KV-Stand, mergt, schreibt zurück.
- Bei KV-Schreibfehler erscheint eine sichtbare Fehlermeldung — kein stilles Fallback auf localStorage.
- Die `daily_revenues`-Tabelle (Roadmap in diesem File) wird erst aktiviert, wenn die aktuelle Lösung fachlich fertig getestet ist. Bis dahin keine Migration ausführen.

## Technische Roadmap — daily_revenues Migration

### Kontext
Tagesumsätze werden aktuell als grosser KV-Blob in `app_settings` gespeichert
(Schlüssel `dailyBudgets` / `beaulieu:dailyBudgets`). Als Sofortschutz gegen
Datenverlust wurde `safeUpsertDailyBudgets` eingeführt (liest KV → mergt →
schreibt zurück). Diese Lösung ist stabil, hat aber strukturelle Grenzen:
kein Audit-Log, kein atomarer UPSERT pro Tag, Merge-Konflikte bei Parallelzugriff.

### Zielarchitektur
Normalisierte Tabelle `daily_revenues` in Supabase Postgres:
```sql
-- Vorbereitet in supabase/migrations/20260507_daily_revenues.sql
CREATE TABLE daily_revenues (
  restaurant_id TEXT    NOT NULL,  -- 'oliv' | 'beaulieu'
  date          DATE    NOT NULL,
  actual_revenue        NUMERIC,
  planned_revenue       NUMERIC,
  actual_food           NUMERIC,
  actual_beverage       NUMERIC,
  actual_labor_cost     NUMERIC,
  previous_year_revenue NUMERIC,
  created_at    TIMESTAMPTZ DEFAULT now(),
  updated_at    TIMESTAMPTZ DEFAULT now(),
  updated_by    TEXT,              -- user email für Audit-Trail
  PRIMARY KEY (restaurant_id, date)
);
```
Jeder Tag hat einen eindeutigen Datensatz — kein Blob, kein Merge nötig.

### Migrationsschritte (in dieser Reihenfolge)

**Schritt 1 — SQL-Migration ausführen**
- Script `supabase/migrations/20260507_daily_revenues.sql` im Supabase SQL-Editor
  ausführen (noch nicht aktiv).
- Zählt bestehende KV-Daten vor und nach der Migration (Abweichung = 0 prüfen).

**Schritt 2 — Datenmigration KV → Tabelle**
- Einmalige Server-Side-Migration: alle Einträge aus `app_settings` wo
  `key IN ('dailyBudgets', 'beaulieu:dailyBudgets')` als normalisierte Zeilen
  in `daily_revenues` einfügen.
- Validierung: `SELECT count(*) FROM daily_revenues` muss ≥ bisherige Blob-Tage.

**Schritt 3 — Neue Datenbankschicht**
- Neue Datei `src/lib/daily-revenues-db.ts` mit:
  - `upsertDailyRevenue(restaurantId, date, fields)` → atomarer UPSERT inkl.
    `updated_by` und `updated_at`
  - `getDailyRevenues(restaurantId, fromDate, toDate)` → Rückgabe als Map
  - `getDailyRevenueMonth(restaurantId, year, month)` → 31-Tage-Array
- Kein localStorage mehr für Tagesumsätze — direkt aus Supabase Postgres.

**Schritt 4 — Lese- und Schreibpfade umstellen**
Betroffene Komponenten (alle nutzen aktuell `safeUpsertDailyBudgets`):
1. `GastronoviImportSection` (handleSave, commitImport, handleImportClick)
2. `TagesansichtPage` (saveManualIst)
3. `TagesControllingPage` (commitUmsatzEdit)
4. `Dashboard` (saveRevenue)
5. `useVj2025Import` (Vorjahres-Seed)
6. `usePersonnelData` (Lohnkosten-Schreibpfad)
7. `SchedulePlanner` (Lohnkosten-Tagesblock)

**Schritt 5 — Audit-Log**
- Tabelle `daily_revenues` enthält `updated_by` (Email des einloggten Benutzers)
  und `updated_at` (Timestamp).
- Optional: separate `daily_revenues_audit`-Tabelle mit Trigger für vollständigen
  Change-History.

**Schritt 6 — Fallback entfernen**
- `safeUpsertDailyBudgets` auf Read-Only-Fallback reduzieren (nur noch lesen,
  nicht mehr schreiben).
- `useSyncStore` `SYNC_KEYS`-Liste: `dailyBudgets` entfernen.
- localStorage-Einträge `dailyBudgets` / `beaulieu:dailyBudgets` nach Migration
  löschen (Cleanup-Script).

**Schritt 7 — Integrationstests**
Alle vier Views müssen für jeden Testtag identische Werte zeigen:
- Tagesansicht (`TagesansichtPage`)
- Dashboard (`Index.tsx`)
- Tages-Controlling (`TagesControllingPage`)
- Erfolgsrechnung / P&L (`PLView.tsx`)

Testscript: `src/lib/__tests__/daily-revenues-integration.test.ts` (noch zu erstellen).

### Status
- [x] SQL-Migration-Script vorbereitet (`supabase/migrations/20260507_daily_revenues.sql`)
- [ ] SQL-Script im Supabase SQL-Editor ausführen
- [ ] Datenmigration KV → Tabelle
- [ ] `daily-revenues-db.ts` implementieren
- [ ] Schreibpfade umstellen
- [ ] Audit-Log aktivieren
- [ ] Fallback entfernen
- [ ] Integrationstests

## Gotchas
- **Supabase Migrations:** Alle SQL-Scripts in `supabase/migrations/` müssen sequenziell im Supabase SQL Editor ausgeführt werden.
- **Tenant Filtering:** Beaulieu = `id LIKE 'b-%'`, Oliv = `id NOT LIKE 'b-%'`; keine explizite `restaurant_id`-Spalte (außer Reservations-/CRM-Tabellen).
- **Mirus Name Mapping:** ggf. manuelles Mapping nötig, um MA-Namen korrekt zu verknüpfen; Excel-1904-Datumsflag nie mit `cellDates:true` lesen (Datumsverschiebung).
- **Default Budget Seed:** 2026-Budget wird beim ersten Laden geseedet, falls keine `plLineItems` existieren.

## Pointers
- **Supabase:** https://supabase.com/docs
- **React:** https://react.dev/
- **Tailwind CSS:** https://tailwindcss.com/docs
- **Shadcn UI:** https://ui.shadcn.com/docs
