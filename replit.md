# Personalkostentracker
An internal reporting tool for revenue, personnel costs, schedules, and KPIs, supporting multiple restaurant tenants.

## Run & Operate
- **Run:** `npm run dev`
- **Build:** `npm run build`
- **Required Env Vars:** Supabase URL and Anon Key (see `src/integrations/supabase/client.ts`)

## Stack
- **Frameworks:** React, TypeScript, Shadcn UI, Tailwind CSS
- **Runtime:** Node.js (via Vite)
- **Database/ORM:** Supabase (Auth, Postgres DB, KV Store)
- **Date Handling:** `date-fns`
- **Charting:** `recharts`

## Where things live
- **Core Application:** `src/App.tsx`
- **Tenant Management:** `src/contexts/TenantContext.tsx`, `src/components/TenantSwitcher.tsx`, `src/lib/tenant-utils.ts`
- **Supabase Integration:** `src/integrations/supabase/client.ts`, `src/lib/supabase-db.ts`, `src/lib/supabase-kv.ts`
- **Database Schema:** `supabase/setup_new_project.sql` (and migration scripts in `supabase/migrations/`)
- **Authentication & Permissions:** `src/contexts/AuthContext.tsx`, `src/hooks/usePermissions.ts`
- **Küchen-Manager Personalstamm-Zugriff (kueche_manager, eingeschränkt):** `usePermissions.ts` lässt `kueche_manager` `canAccessModule('personalstamm')` + `canEditEmployees` (Mitarbeiter anlegen/bearbeiten), aber die Lohn-Gates (`canSeeHourlyWages`/`canToggleCostView`/`canAccessSettings`) bleiben UNVERÄNDERT → Küchen-Manager sieht NIE Löhne. In `Personalstamm.tsx` ist die Liste über `allowedDepartment` bereits auf Küche gefiltert; `handleNew()` setzt die Abteilung neuer Mitarbeiter auf `allowedDepartment` und der Abteilungs-`Select` ist gesperrt, wenn `allowedDepartment !== 'all'` (Küchen-Manager kann nur Küchen-Mitarbeiter anlegen). **Wichtig (PII/Lohn-Leak vermieden):** `canEditEmployees` war historisch = Admin+GF Beaulieu und gated noch immer vertrauliche Flächen; daher gibt es das lokale Gate `canManageAllEmployees = isAdmin || isBeaulieuManager` (= alte canEditEmployees-Semantik), das ALLE Admin-/Vertraulich-Flächen abschirmt und NICHT für den Küchen-Manager gilt: Duplikat-Warnung (zeigt Löhne), „Ausstehende Anmeldungen"/pending_review (Onboarding-PII), Abschnitt „Persönliche Daten" (inkl. AHV/IBAN), „Vertragliche Grundlagen", „Arbeitsvertrag". Lohnfelder selbst bleiben unter `canEditWages`. Küchen-Manager bearbeitet nur Stammdaten (Name/Abteilung[Küche]/Anstellungsart/Wochenstunden/aktiv/Zeiterfassungs-Flag) + Notizen.
- **Routing:** Defined in `src/App.tsx`
- **Navigation (Menüstruktur):** `src/components/AppNav.tsx` — `DASHBOARD_ITEM` + `NAV_GROUPS` (7 groups: Verkauf, Umsatz, Personal, Warenkosten, Foratable, Admin, Import) drive desktop sidebar, mobile bottom bar AND mobile sheet. Active highlighting uses the longest-path matcher `activeNavPath(pathname)`. `/reporting` is intentionally unlinked (route kept in `App.tsx`, reachable only by URL).
- **UI Components:** `src/components/`
- **Pages:** `src/pages/`
- **Reporting & Financials:** `src/types/reporting.ts`, `src/lib/reporting-store.ts`, `src/types/pl.ts`, `src/lib/pl-engine.ts`, `src/pages/PLView.tsx`
- **P&L Vorjahres-Diagnose (nur Sichtbarkeit, KEINE Berechnung):** Pure `src/lib/pl-prior-year-diagnostics.ts` (`computePriorYearDiagnostics(priorYear, prevYearRecords, vjDailyData)` → `PriorYearDiagnostics`; NO supabase/DOM, only `import type` from `reporting`/`vj-daily-supabase`). Prüft die in `PLView.tsx` BEREITS geladenen Vorjahresdaten (`prevYearRecords = loadYear(year-1)` aus `reporting_v1` + `vjDailyData = loadVjDailyYear(year-1)`, beide via authentifizierte Supabase-Session) auf: `hasRevenue` (`revenueActual>0`), `hasExpenses` (`expenseCategories[].amount≠0`), `hasPersonnel` (`personnelCostActual>0`), `hasDailyRevenue` (vj_daily keys `${priorYear}-` ODER `rec.year===priorYear`, `actualRevenue>0`). Abgeleitet: `hasAnyRevenue`/`hasAnyCosts`/`hasAnyData`; `status` `'none'|'revenue_only'|'ok'` + dt. `message` (Jahr nur in `'none'`). `none`→amber Banner „Keine Vorjahresdaten für {Jahr} vorhanden. Bitte Vorjahres-Kontoblätter oder Vorjahreswerte importieren."; `revenue_only`→blaues Banner „Vorjahresumsatz vorhanden, aber keine Vorjahreskosten vorhanden."; `ok`→kein Banner. Wiring in `PLView.tsx`: `priorYearDiag` useMemo nach `prevYearRecords`, Banner ganz oben im Content (nach `<StichtagBanner/>`, in allen Modi sichtbar). Verändert KEINE Berechnungen/Spalten. Tests: `src/lib/__tests__/pl-prior-year-diagnostics.test.ts` (node env, 10 synthetische — none/revenue-only/voll, daily-only, gemischte Kosten, Fremdjahr/0-Werte ignoriert).
- **Budgeting:** `src/types/budget.ts`, `src/lib/budget-store.ts`, `src/pages/Budget.tsx`
- **Product & Inventory:** `src/pages/Artikel.tsx`, `src/lib/artikel-store.ts`, `src/pages/ProduktStamm.tsx`, `src/lib/produkt-stamm-db.ts`, `src/pages/WesAnalyse.tsx`, `src/pages/ArtikelTracking.tsx`, `src/lib/artikel-tracking-store.ts`
- **Produkt-Analyse (Rangliste + Drill-down):** `src/pages/ProduktAnalyse.tsx` (`/produkt-analyse`, ranking) → clickable rows open `src/pages/ProduktDetail.tsx` (`/produkt-analyse/produkt`, single product). Pure logic in `src/lib/product-analytics.ts` (NO supabase/DOM, only `import type` from `sales-db`): `PeriodSelection` discriminated union **Day/Week/Month/Year** (replaces the old "Mehrere Monate"/"Kumuliert" modes; room for range/comparison later), ISO-week helpers (`isoWeekInfo`/`isoWeekStart`/`isoWeeksInYear` — handles 52/53-week cross-year, `shiftIsoWeek(year,week,delta)` for prev/next week jumps that roll across year/53-week boundaries), `periodBounds`/`periodLabel`/`weekRangeLabel(year,week)` (compact nav label "KW 26 · 22.06.–28.06.2026"; shows the start-year only on cross-year weeks), `categoryOf`/`matchesCategory` (`SOURCE_CATEGORY` food/beverage), `filterRows`, `aggregateProducts` (ranking), `aggregateProductsByWeekday(rows,year,week)` (per-product Mo–So buckets + totals for the weekly ranking; only rows whose `sale_date` falls in the 7 ISO-week days; emits `ProductWeekAggregate{days:WeekdayBucket[7],total_revenue,total_qty}`), `buildBreakdown` (single product → month=row/day, week=Mon–So, year=Jan–Dez, day=per-source entries [product_sales has no hourly data]; returns rows w/ `share`+`avgPrice`, `totalRevenue`/`totalQty`/`salesCount`/`avgRevenuePerSale`), and `filtersToParams`/`filtersFromParams` for URL round-trip of `{period,category,metric}`. Ranking mirrors filters to the URL (`useSearchParams`) so the detail page is refresh-safe and the back button restores context (incl. metric); keeps sort/Top10-20-All/Flop20/category/hide + **inline product search & column sort only (min/max Umsatz/Anzahl inputs removed)**. **Week selection is clamped via `Math.min(week, isoWeeksInYear(year))`** so an invalid KW53 never reaches the filter/URL. **Weekly view UX:** the KW dropdown is replaced by prev/next arrows + the exact `weekRangeLabel` range + "Heute"/"Diese Woche" quick buttons (both jump to the current ISO week; year dropdown kept). In week mode the ranking renders `WeekRankTable` (weekday columns Mo–So with date sub-labels + Total; each `WeekCell` shows revenue on top and qty ("… Stk.") smaller underneath, emphasizing qty when sorting by Anzahl; tfoot column totals + "–" for empty days); other periods keep the original `RankTable`. The page's `weekAgg`/`weekDayDates` memos derive from the SAME `filterRows(...)` scope as the ranking so per-product totals match the weekday-bucket sums and header dates align with `agg.days`. Detail shows KPIs (Gesamtumsatz/Gesamtanzahl/Ø Umsatz pro Verkauf) + breakdown table (Datum/Periode · Anzahl · Umsatz · % vom Produkt-Total · Ø Preis) with a Umsatz/Anzahl toggle (drives the %-column). Loads via existing `loadProductSalesRows` (`product_sales` has no `restaurant_id` — tenant posture is boundary-faithful pure functions only; no schema/migration change). Tests: `src/lib/__tests__/product-analytics.test.ts` (node env, 37 synthetic tests — monthly/weekly/yearly/day, cross-year ISO weeks, empty data, URL round-trip, scope isolation, `weekRangeLabel`/`shiftIsoWeek` rollovers, `aggregateProductsByWeekday` bucket placement/totals/out-of-week exclusion).
- **Schedule Planner:** `src/pages/SchedulePlanner.tsx`, `src/lib/pattern-warnings.ts`
- **Positionsverwaltung / Personalbedarf (Phase 1, Grundlage):** Konfigurierbare Positions-/Stationsstammdaten je Mandant. DB-Tabelle `positions` (Migration `supabase/migrations/20260625_positions.sql`: `restaurant_id`, `key`, `name`, `department` ['service'|'kueche'], `department_group`, `color`, `icon`, `sort_order`, `active`; UNIQUE(restaurant_id,key); RLS authenticated + REVOKE anon) + `employees.primary_station text` / `secondary_stations text[]`. **Stabile KEYS** (Slugs), nicht Anzeigenamen, werden in `employee.primaryStation`/`secondaryStations` gespeichert (Umbenennen bricht Zuordnungen nicht). Modell `src/types/positions.ts` (Position, PositionDraft=Omit id/restaurantId/timestamps). Reine Logik `src/lib/position-utils.ts` (NO supabase/DOM): `slugifyKey` (Umlaut-fest), `defaultPositions()` (= die 5 echten Planungsbereiche aus dem Personalbedarf-PDF in Reihenfolge: `bar_buffet_springer` BAR Buffet/Springer [Bereich `bar_buffet`], `service` Service [Bereich `restaurant`] · `piazzolo_take_away` Piazzolo Take Away [Bereich `take_away`], `abwasch` Abwasch [Bereich `abwasch`], `kueche` Küche [Bereich `kueche_produktion`] — stabile Slugs + Hex-Farben + kuratierte Icons + `departmentGroup`=Bereichs-Key; KEIN `STATIONS_BY_DEPT` mehr), `positionByKey`/`resolvePositionKey` (toleriert Alt-Anzeigenamen→Key)/`positionDisplayName`, `employeeCoverableKeys`/`employeeCanCover`, `positionsForDepartment(positions,dept,{activeOnly})`/`activePositions`/`sortPositions`, `DEPARTMENTS`, `DEPT_DEFAULT_COLOR`/`DEPT_DEFAULT_ICON`, `POSITION_COLORS[16]`/`POSITION_ICONS[22]`. **Bereich-Hierarchie (Abteilung → Bereich → Positionen, nur Anzeige/Architektur-Vorbereitung, KEINE Berechnung):** `PositionArea{key,name}`, `AREAS_BY_DEPARTMENT` (service→[`restaurant` Restaurant, `bar_buffet` Bar/Buffet]; küche→[`kueche_produktion` Küche Produktion, `take_away` Take Away, `abwasch` Abwasch]), `areasForDepartment`/`areaLabel`, `resolvePositionArea(p)` (1. gültiger `departmentGroup`-Key der Abteilung → 2. Fallback: Default-Bereich der gleichnamigen Standard-Position aus `defaultPositions()` [damit bereits angewendete Positionen OHNE gesetzten `departmentGroup` korrekt einsortieren] → 3. `''`), `groupPositionsByArea(positions,dept,{includeInactive})` → `PositionAreaGroup[]{area:PositionArea|null,positions}` (rendert ALLE definierten Bereiche der Abteilung in Reihenfolge — auch leere; Positionen ohne gültigen Bereich landen in abschließender Sammelgruppe `area:null`, die nur bei Inhalt erscheint; intern `sortWithinArea`=aktive vor inaktiven, dann sortOrder, dann Name; `includeInactive=false` blendet inaktive komplett aus). **Mehrere Positionen je Bereich sind möglich OHNE neue Migration** (Spalte `department_group` existiert bereits in DB + `Position`-Typ + `positions-db`-Roundtrip). DB-Layer `src/lib/positions-db.ts` (load best-effort→[] wenn Tabelle fehlt; upsert/delete/seed WERFEN; DB-dept 'kueche'; seed idempotent via onConflict restaurant_id,key ignoreDuplicates; **`applyDefaultPositions(restaurantId)` (Reconcile, mandantengeprüft):** upsert der 5 Standard-Positionen per onConflict restaurant_id,key — vorhandene `id` je key wird WIEDERVERWENDET → echtes Update (kein PK-Wechsel, keine Doppelanlage), `active=true`; danach werden ALLE übrigen noch aktiven Positionen des Mandanten auf `active=false` gesetzt (**DEAKTIVIERT, NICHT gelöscht** → Mitarbeiter-Keys `primaryStation`/`secondaryStations` bleiben gültig, keine Scheduling-/Kosten-/Overtime-Änderung)). Mapper `supabase-db.ts`: `dbToEmployee` liest primary_station/secondary_stations; `upsertEmployee` schreibt sie best-effort über `saveEmployeeStationsBestEffort` (NICHT über `employeeToDb`, um die ali-reactivation-Semantik nicht zu berühren). Hook `src/hooks/usePositions.ts` (tenantId/positions/loading/error/reload/save/remove/seed/applyDefaults). Icon-Render `src/components/PositionIcon.tsx` (kuratierte Lucide-Map, Fallback Circle — kein `import *`). UI: Seite `src/pages/Positionen.tsx` (`/positionen`, nur Admin via `usePermissions` AppModule 'positionen' → `canAccessModule`=isAdmin; Gast read-only; CRUD **hierarchisch Abteilung → Bereich → Positionen** (`grouped[dept]=groupPositionsByArea(...,{includeInactive:showInactive})`: je Abteilungs-Card eine Sub-Überschrift pro Bereich + bereichsgebundener „+ Position"-Button [setzt `departmentGroup` des neuen Eintrags auf den Bereichs-Key], leere Bereiche zeigen „Keine Position in diesem Bereich."), Add/Edit-Dialog Name/Abteilung/**Bereich** (Select „Ohne Bereich" + `areasForDepartment`; Abteilungswechsel setzt Bereich zurück)/Farbe/Icon/aktiv, Up/Down-Reorder (innerhalb der gerenderten Bereichs-Liste, swap `sortOrder`), **Toggle „Inaktive Positionen anzeigen" (Default AUS)** — AUS blendet inaktive komplett aus; AN zeigt sie ausgegraut (`opacity-60`+`line-through`+Badge „inaktiv") unter den aktiven (alte Demo-Positionen bleiben so erhalten, NICHT gelöscht), Seed-Defaults (Leerzustand) + Header-Button **„Standard-Positionen anwenden"** (Admin, wenn Positionen existieren; AlertDialog-Bestätigung → `applyDefaults` = upsert der 5 Standards per Slug + übrige deaktivieren; RLS authenticated-only → Schreibvorgang läuft eingeloggt je Mandant in der App); **Key ist nach Anlegen fix**, Abteilung bei Bearbeitung gesperrt) — verlinkt in `AppNav.tsx` Gruppe „Personal" (Tags-Icon, adminOnly). **Personalstamm — Sektion „Positionen / Qualifikationen"** (gated `canEditEmployees`, also auch Küchen-Manager; vor „Vertragliche Grundlagen"). Zeigt jetzt **ALLE aktiven Positionen beider Abteilungen hierarchisch Abteilung → Bereich → Position** (NICHT mehr auf die MA-Abteilung gefiltert — Cross-Training möglich; ändert NICHT die `employee.department`-Logik). Reine, testbare Helfer in `position-utils.ts` (Mitarbeiter-Qualifikationen): `activeQualificationGroups(positions)` → `DepartmentQualificationGroup[]{department,areas:PositionAreaGroup[]}` (nur nicht-leere Bereiche/Abteilungen, kanonische Reihenfolge via `groupPositionsByArea({includeInactive:false})`), `isActivePositionKey`, `setPrimaryStation(secondary,newPrimary)` (entfernt newPrimary aus secondary, dedupe/trim), `toggleSecondaryStation(secondary,key,primary)` (nie = primary, dedupe), `normalizeStationsForSave(primary,secondary)` → `{primaryStation?,secondaryStations?}` (primary nie in secondary, dedupe, leer→undefined, **inaktive/alte Keys BLEIBEN erhalten — kein stilles Löschen**), `inactiveStoredStationKeys(emp,positions)` (gespeicherte nicht-aktive Keys → nur Hinweis). UI (Edit): **Hauptposition**-Select (`SelectGroup`/`SelectLabel` je Abteilung + Bereich-Sub-Label, Sentinel `__none__`; bei inaktiver gespeicherter Hauptposition zeigt der Select `__none__` + amber Hinweis, Wert bleibt erhalten bis Ersatzwahl), **„Kann zusätzlich abdecken"**-Checkboxen (gleiche Hierarchie; Hauptposition als „(Hauptposition)" disabled), **Alt-/Inaktiv-Block** (line-through Badges „nicht mehr auswählbar"). Validierung erzwungen über die Helfer + finale `normalizeStationsForSave` in `handleSave` direkt vor `upsertEmployee`. Read: Badges via `positionDisplayName` (inaktive mit „(inaktiv)"-Suffix). Speichert stabile **Keys** (Slugs) in `primaryStation`/`secondaryStations`; Persistenz via `saveEmployeeStationsBestEffort` → nach Reload erhalten. Lohnfelder bleiben separat gegated (`canEditWages`/`canSeeHourlyWages` UNVERÄNDERT → Küchen-Manager sieht nie Löhne); vertrauliche Karten bleiben unter `canManageAllEmployees`. `StationMatrixDialog.tsx` (SchedulePlanner) aus `usePositions` gespeist, speichert Keys, zeigt Alt-Keys, Link zu `/positionen`. Ändert KEINE Scheduling-/Kosten-/Overtime-/Report-Berechnung (`hour-balance-utils` vergleicht weiterhin peer-to-peer Keys). Tests: `src/lib/__tests__/position-utils.test.ts` (node env, 50 synthetische — inkl. Bereiche/`resolvePositionArea` Fallback/`groupPositionsByArea` leere Bereiche, inaktiv-Toggle, aktive-vor-inaktiven, Abteilungs-Scope + die 6 Qualifikations-Helfer: Hierarchie/Reihenfolge, primary-aus-secondary, dedupe/trim, Legacy-Erhalt, Inaktiv-Hinweis).
- **Personalbedarf / SOLL-Besetzung (Phase 1, nur Erfassung — KEINE Prüfung):** Pro (Saison × Wochentag) je Position eine oder MEHRERE Schichten (Schichtbeginn/-ende `HH:MM` + Anzahl benötigte MA + Reihenfolge). **Vollständig getrennt von der Dienstplanung** (eigene Tabelle, kein FK auf schedule_entries). DB-Tabelle `staffing_requirements` (Migration `supabase/migrations/20260625_staffing_requirements.sql`, spiegelt `positions`: RLS authenticated-only USING/CHECK true + REVOKE anon, self-test+verify): `id` uuid pk, `restaurant_id` text, `scope_type` text default 'weekly', `season` text default 'standard' NOT NULL, `weekday` smallint NULL CHECK(null OR 1..7 ISO Mo..So), `scope_ref` text NULL, `position_key` text (Positions-Slug, **bewusst kein FK** → Umbenennen/Deaktivieren bricht Vorlage nicht), `shift_start`/`shift_end` text 'HH:MM', `required_count` smallint>=0, `sort_order` int, `meta` jsonb default '{}', `created_at`/`updated_at`. **EINE Zeile = EINE Schicht** (kein natürlicher Unique-Key — mehrere Schichten je Scope/Position erlaubt). **Schemafreie Erweiterbarkeit (KEINE künftige Migration):** Feiertage/Events/Sonderöffnung kommen als neue `scope_type`-Werte (z.B. 'holiday' mit `scope_ref`=Datum) + `meta` jsonb-Auffangraum; `weekday` ist für datumsbasierte Bereiche null. Typen `src/types/staffing.ts` (StaffingRequirement, StaffingRequirementDraft=Omit id/restaurantId/timestamps, StaffingScope, StaffingScopeType/StaffingSeason als breite String-Typen). Reine Logik `src/lib/staffing-requirements-utils.ts` (NO supabase/DOM, nur `import type`): `SEASONS` (standard/sommer/winter verfügbar, custom `available:false`), `DEFAULT_SEASON`, `SCOPE_WEEKLY`, `WEEKDAYS` (ISO 1..7), `seasonLabel`/`weekdayLabel`/`weekdayShort`/`isSeasonAvailable`, Zeit-Helfer (`isValidTime`/`timeToMinutes`/`minutesToTime`/`shiftDuration` [Minuten; NaN wenn Ende≤Beginn — KEINE Übernacht-Unterstützung]), `ShiftDraft{id?,shiftStart,shiftEnd,requiredCount}`, `defaultShiftDraft()`, `validateShiftDraft`/`isShiftDraftValid` (gültiges HH:MM, Ende MUSS nach Beginn liegen, requiredCount Integer ≥0), `sortShifts`, `shiftsForScope(reqs,season,weekday)` (filtert scope_type='weekly'+season+weekday), `totalRequired`, `buildRequirementMatrix(positions,reqs,season,weekday)` → `DepartmentRequirementGroup[]{department,areas:AreaRequirementGroup[]{area,positions:PositionRequirement[]{position,shifts}}}` (**reuse `groupPositionsByArea` active-only**, leere Bereiche/Abteilungen verworfen). DB-Layer `src/lib/staffing-requirements-db.ts`: `loadStaffingRequirements` (best-effort → [] wenn Tabelle fehlt), **`saveStaffingScope(restaurantId,scope,drafts)` (RLS-sicher, WIRFT):** kein Bulk-Upsert-auf-PK (authenticated-weite RLS schützt NICHT pro Mandant) — stattdessen 1) ids des EXAKTEN Mandanten+Scope laden, 2) nur diese ids UPDATEN (`.eq('id').eq('restaurant_id')`+Scope-Prädikate, NULL via `.is(...,null)` sonst `.eq`), 3) übrige frisch INSERTEN (neue id), 4) entfernte (im Scope, nicht mehr gewünscht) DELETEN (mandanten-+scope-gefiltert). **`sortOrder` wird vom Aufrufer je Position vergeben und im Helfer NICHT überschrieben** (sonst würden erhaltene Orphan-Zeilen mutiert). Hook `src/hooks/useStaffingRequirements.ts` (tenantId/requirements/loading/error/reload/saveScope). Seite `src/pages/Personalbedarf.tsx` (`/personalbedarf`, nur Admin via AppModule 'personalbedarf' → `canAccessModule`=isAdmin; Gast read-only): Saison-Buttons (custom disabled „(bald)"), Wochentag-Buttons (Default = heutiger ISO-Tag), hierarchische Render via `buildRequirementMatrix`, je Position Schicht-Zeilen (time/time/number-Inputs) + „+ Schicht hinzufügen"/Entfernen, Dirty-Tracking (serialize edits vs baseline), Verwerfen/Speichern, Edits-Rebuild bei Scope-/Datenwechsel. **Orphan-Schutz:** Schichten von NICHT angezeigten (inaktiven/gelöschten) Positionen dieses Scopes werden beim Speichern UNVERÄNDERT mitgegeben → kein stilles Löschen. Tagessumme/„Benötigt gesamt" ist rein informativ (KEINE Besetzungs-Prüfung/Warnung/Vorschlag/Budget — späterer Schritt). Verlinkt in `AppNav.tsx` Gruppe „Personal" (ClipboardList-Icon, adminOnly), Route in `App.tsx`. Tests: `src/lib/__tests__/staffing-requirements-utils.test.ts` (node env, 21 synthetische — Zeit-Helfer, validateShiftDraft, shiftsForScope-Filter, totalRequired, buildRequirementMatrix Hierarchie/active-only/leere-Bereiche).
- **Personalbedarf-Abgleich / SOLL-Ist-Vergleich (Phase 2, nur ANZEIGE — KEINE Änderung):** Im Dienstplan (`SchedulePlanner`) vergleicht ein read-only Panel den gespeicherten Personalbedarf (`staffing_requirements`) mit den FÜR EINEN TAG eingeplanten Mitarbeitenden. Je Bedarfs-Schicht: Position · Zeitraum · Benötigt · Geplant · Differenz · Status (Ampel). **Ändert KEINE Dienstplan-/Budget-/Kosten-/Overtime-Daten, gibt KEINE Vorschläge, keine neue Tabelle/Migration.** Reine Logik `src/lib/staffing-comparison-utils.ts` (NO supabase/DOM, nur `import type` aus `personnel`/`positions`/`staffing` + reuse von `position-utils`/`staffing-requirements-utils`): `comparisonStatus(required,planned)` (**3-STUFIG, ab ±1**: `'red'` 🔴 Unterbesetzung [planned<required], `'orange'` 🟠 Überbesetzung [planned>required], `'green'` 🟢 NUR exakte Erfüllung [diff===0]; `counts:{green,orange,red}`), `formatStaffingDiff(diff)` (immer exakte Differenz mit Richtung „+1 Person zu viel"/„−2 Personen zu wenig", **U+2212 Minus**, 0 → „±0"), `slotOverlapsShift(slot,shiftStart,shiftEnd)` (**strikte** Überlappung in Minuten — Berührung an den Rändern zählt NICHT; ungültige/0-Längen/Übernacht-Slots ausgeschlossen), `countPlanned`/`buildPlannedEmployees` und `computeStaffingComparison(positions,requirements,employees,scheduleData,date,season,{departments?})`. **Matching-Regel (im Util dokumentiert, bewusste Näherung):** ein Mitarbeiter zählt zu einer Bedarfs-Schicht NUR wenn aktiv (`isActive !== false`) UND `resolvePositionKey(primaryStation) === shift.positionKey` (**NUR Hauptposition** — Sekundär-Qualifikationen zählen NICHT, da der Dienstplan KEINE Position je Schicht speichert; Cross-Training an fremder Station ist nicht abbildbar) UND ein produktiver (Nicht-Absenz: `früh`/`spät` ohne `frühAbsence`/`spätAbsence`) Slot die Schicht überlappt; jeder MA max. 1× je Schicht. `computeStaffingComparison` iteriert über die BEDARFS-Schichten (nur Positionen MIT Bedarf erscheinen), gruppiert via `groupPositionsByArea` (active-only) Abteilung→Bereich→Position; Bedarfe auf inaktiver/unbekannter Position → `orphanPositions` (kein stilles Verwerfen); Summen/Counts NUR aus gerenderten Zeilen. **Rollen-Scope:** optionaler `departments`-Filter blendet Fremd-Abteilungen UND Orphans aus; `hasRequirements` spiegelt ALLE Scope-Schichten (vor Filter) → neutrale Meldung „Für diesen Tag ist noch kein Personalbedarf definiert." bezieht sich auf den TAG, nicht die Rolle. UI `src/components/schedule-planner/StaffingComparisonPanel.tsx` (self-contained: `usePositions` + `useStaffingRequirements`, eigener Datum-Selektor [unabhängig vom Planer-Datum — `initialDate` nur Startwert]; Saison optional KONTROLLIERT via `season`/`onSeasonChange`-Props [geteilt mit den Tages-Badges im Grid, s. Phase 3], sonst interner State; hierarchische Ampel-Tabellen mit Summary-Chips erfüllt/zu viel/zu wenig, Orphan-Abschnitt, neutrale Meldung, Link zu `/personalbedarf`). Eingebunden in `SchedulePlanner.tsx` vor den Tages-Dialogen mit `employees={roleScopedEmployees}`, `scheduleData`, `initialDate`, `season={staffingSeason}`/`onSeasonChange`, **`departments={comparisonDepartments}`** (= `effectiveEmployeeDepartmentScope(role, allowedDepartment)`; `'all'`→undefined/kein Filter; gilt für ALLE eingeschränkten Rollen `service_manager` UND `kueche_manager` — memoisiert, damit die Panel-`useMemo` nicht je Render neu rechnet → KEIN Cross-Department-Leak). Tests: `src/lib/__tests__/staffing-comparison-utils.test.ts` (node env, 31 synthetische — Status 3-stufig (rot zu wenig / orange zu viel / grün nur exakt, ab ±1), formatStaffingDiff mit Richtung „zu viel"/„zu wenig" + U+2212, strikte Überlappung, Absenz-Ausschluss, Hauptpositions-Matching/kein Doppelzählen, Hierarchie/Orphans, departments-Scope, leere Bedarfe + die Phase-3-Tageszusammenfassung).
- **Personalbedarf↔Dienstplan-Sync (Phase 3, nur ANZEIGE — KEINE Änderung an Plan-/Bedarfs-Daten):** Kompakte SOLL/IST-Badges im Dienstplan-Tageskopf + Ist-vs-Soll-Card auf der Personalbedarf-Seite. Reine Logik in `staffing-comparison-utils.ts`: `PlannedEmployeeDay` hat jetzt Pflichtfeld `department` (aus `emp.department` in `buildPlannedEmployees`); neue Exports `plannedIdsForShift` (countPlanned delegiert), `formatShortStaffingDiff` („+1"/„−2"/„±0", U+2212), `computeDayStaffingSummary({positions,requirements,plannedEmployees,season,weekday,departments?})` → `DayStaffingSummaryResult{hasRequirements,departments:DepartmentDaySummary[]{department,required,planned,diff,status,unmatchedPlanned}}`. **Einheiten = Personen-SCHICHTEN** (Σ über Bedarfs-Schichten; 1 MA mit Früh+Spät erfüllt 2 Schichten — konsistent mit dem Detail-Panel); `unmatchedPlanned` = produktiv geplante MA der Abteilung, die KEINE Bedarfs-Schicht des Tages matchen (matchedIds über ALLE Zeilen inkl. Orphans) — reiner Hinweis, zählt NICHT in `planned`; Orphan-Bedarfe fließen NICHT in Abteilungs-Summen; nur Abteilungen MIT Bedarf werden geliefert. **Dienstplan-Badges:** `SchedulePlanner.tsx` hält `staffingSeason` (geteilt mit dem Panel via kontrollierte Props) + lädt `usePositions`/`useStaffingRequirements`; `dayStaffingSummaries` useMemo rechnet je `displayDays`-Tag (`getISODay` → ISO 1..7) mit `roleScopedEmployees` + `departments={comparisonDepartments}` (Rollen-Scope wie Panel — kein Cross-Department-Leak) und überspringt Tage ohne Bedarf (kein Badge). `ModernScheduleGrid.tsx` Prop `dayStaffingSummaries?: Record<yyyy-MM-dd, DayStaffingSummaryResult>` rendert `src/components/schedule-planner/DayStaffingBadge.tsx` im Tageskopf: je Abteilung Mini-Pill „S 5/6 +1" (3-stufige Farbe), Klick → Popover mit „Soll x · Ist y", Richtungs-Diff, unmatchedPlanned-Hinweis + Link `/personalbedarf` (`stopPropagation`, nur Anzeige). Legacy `ScheduleGrid`/`MobileDayView` bewusst OHNE Badge. Tage mit `isAdditionalCostPlan` zählen als geplant (dokumentierte Näherung). **Personalbedarf-Seite:** `src/components/schedule-planner/StaffingScheduleCheckCard.tsx` (Card „Dienstplan-Abgleich", eingebunden in `Personalbedarf.tsx` über der Bedarfs-Hierarchie): Datums-Input (Default `nextDateForIsoWeekday(weekday)` = nächstes Vorkommen des oben gewählten Wochentags, springt bei Wochentagswechsel mit), lädt read-only `loadEmployees(tenantId)` + `loadScheduleForMonth(date,tenantId)` (Monat via monthKey gecacht, cancelled-Guard), vergleicht IMMER mit dem ISO-Wochentag des GEWÄHLTEN Datums (Amber-Hinweis wenn ≠ Seiten-Wochentag), zeigt Abteilungs-Pills „Service: Soll x / Ist y / Diff" + Schicht-Tabelle (Position·Zeit·Soll·Ist·Differenz, 3-stufig) + Link `/dienstplan`.
- **Überstundenkosten Festangestellte (PersonalFix `/personal-fix`):** Pure `src/lib/overtime-analysis.ts` (KEIN React/supabase/DOM; nur `import type` von `personnel` + `getEffectiveHourlyRate` aus `employee-rate`). Überstunden NUR für echte Festangestellte: `isFixedSalaryEmployee` = `employmentType` `vollzeit|teilzeit` **UND `(monthlySalary ?? 0) > 0`** (zweite Sicherheitsschicht, deckt sich mit `hasFixedSalary` in PersonalFix); flexible/stündliche bzw. fälschlich als vollzeit/teilzeit erfasste MA OHNE fixen Monatslohn werden sowohl im Wiring (Kandidaten-Filter `hasFixedSalary`) als auch in der puren Lib ausgeschlossen. **MONATSLOGIK (NEU, ersetzt die frühere Wochenlogik):** Überstunden werden AUSSCHLIESSLICH monatlich berechnet — je MA `overtimeHours = max(0, produktive Monats-Ist − Monatssoll)`. Das **Monatssoll ist pensum- UND monatslängenabhängig**: `monthlyTargetHours(emp, daysInMonth)` = `42h × (daysInMonth ÷ 7) × Pensum`. Da `42/7 = 6` ergibt sich bei 100 % `Soll = 6 × daysInMonth` (30→180, 31→186, 28→168). Pensum = vertragliche `weeklyHours` (bereits `42×Pensum`): `weeklyTargetHours(emp)` = `weeklyHours>0 ? round2(weeklyHours) : 42` (100 %→42, 80 %→33.6, 50 %→21; fehlt/≤0 → Vollzeit 42); `workloadPercent(emp)` = `weeklyTargetHours/42×100`. Konstanten: `WEEKLY_FULLTIME_TARGET=42` (+ deprecated Alias `WEEKLY_OVERTIME_THRESHOLD`), `DAILY_OVERTIME_THRESHOLD=8.4` ist **NUR INFO** (Tage über 8.4h erzeugen KEINE Überstunden; `daysOver84Count` wird je MA nur gezählt). `computeOvertimeAnalysis({employees, entries, daysInMonth, departmentFilter?, disabledEmployeeIds?})`: aggregiert produktive Ist-Stunden je (MA, Tag) → `productiveHours`, `difference = round2(productiveHours − monthlyTargetHours)` (kann negativ sein, bleibt sichtbar), `overtimeHours = max(0, difference)`, `overtimeCost = overtimeHours × rate` (null wenn Satz fehlt). **Pro-Mitarbeiter-Deaktivierung:** IDs in `disabledEmployeeIds` (Set/Iterable) → `overtimeDisabled=true`, `overtimeHours=0`, `overtimeCost=0` (Soll/Ist/Differenz bleiben zur Transparenz erhalten). **Abwesenheiten ausgeschlossen:** Einträge mit `OvertimeHoursEntry.absenceType` (FE/K/U/…) werden übersprungen (produktiv = `hours>0 && !absenceType`). **Manuelle Zusatzkosten (`OvertimeHoursEntry.isAdditionalCost`) NICHT als Überstunden:** diese Tage zählen NICHT in `productiveHours` (sie sind über `zusatzIstCHF` bereits im regulären `pfix.month.istTotal` enthalten — eine erneute Zählung wäre Doppelverrechnung), werden aber SEPARAT pro MA als `additionalCostHours`/`additionalCostDays`/`additionalCost` (pro Tag `round2(h×rate)` summiert; null ohne Satz) ausgewiesen — **reine Anzeige**. Ein MA erscheint, wenn `productiveHours>0` ODER `additionalCostHours>0`. `EmployeeOvertimeResult` hat: `workloadPercent, monthlyTargetHours, productiveHours, difference, overtimeHours, overtimeCost, overtimeDisabled, daysOver84Count, additionalCost(Hours/Days), hourlyRate, rateAvailable`. `OvertimeAnalysis`: `employees[], totalOvertimeHours, totalOvertimeCost, affectedEmployeeCount (overtimeHours>0), hasUnavailableRates`. Sortierung: ÜS desc → Differenz desc → Name. `round2` überall; Stündliche (minijob/aushilfe) ignoriert; Abteilungsfilter respektiert. `computeOvertimeTotals(regularCost, overtimeCost, netRevenue, includeOvertime)` → `regularCost/overtimeCost/totalExclOvertime/totalInclOvertime/pkqExclOvertime/pkqInclOvertime/effectiveTotal/effectivePkq` (`safeQuote` null bei Umsatz≤0; Drop der alten `OvertimeWeek`/`OvertimeDay`/`weeks[]`). **WOCHENANSICHT (additiv, Monatslogik bleibt unverändert):** `computeWeeklyOvertimeAnalysis({employees, entries, year, month, departmentFilter?, disabledEmployeeIds?})` → `WeeklyOvertimeAnalysis{employees:EmployeeWeeklyOvertimeResult[], totalOvertimeHours, totalOvertimeCost, affectedEmployeeCount, hasUnavailableRates}`. Je MA `EmployeeWeeklyOvertimeResult{employeeId,name,department,workloadPercent,weeks:WeekOvertimeRow[],totalOvertimeHours,totalOvertimeCost,overtimeDisabled}`; je Woche `WeekOvertimeRow{isoYear,isoWeek,weekLabel("KW 23"),rangeLabel("01.06.–07.06.2026"),daysInMonth,weeklyTargetHours,productiveHours,difference,overtimeHours,overtimeCost,additionalCost(Hours/Days),daysOver84Count}`. ISO-Wochen via `weeksInMonth(year,month)→MonthWeekDescriptor[]` (UTC-Donnerstag-Algorithmus, nur die Tage der Woche, die IM Monat liegen → `daysInMonth` 1..7; Randwochen am Monatsanfang/-ende sind partiell). **Wochensoll ist anteilig:** `proratedWeeklyTargetHours(emp, daysInWeekInMonth)` = `weeklyTargetHours(emp) × daysInWeekInMonth ÷ 7` (volle 7-Tage-Woche 100 %→42, Randwoche mit 2 Tagen 100 %→12; Pensum skaliert: 80 %/7d→33.6). `overtimeHours = max(0, productiveHours − weeklyTargetHours)` je Woche; gleiche Ausschlüsse wie Monat (Abwesenheiten, `isAdditionalCost` separat als Zusatzkosten, stündliche MA raus); `disabledEmployeeIds` → `overtimeDisabled`, Stunden/Differenz bleiben sichtbar, Überstunden+Kosten=0. **Nutzen:** fängt Wochen über dem Wochensoll, die in der Monatssumme untergehen (Monat unter Soll, einzelne Woche darüber). **TAGESDETAIL (additiv, REINE ANZEIGE — KEINE Berechnung):** `buildWeekDayRows(entries:DayDetailEntry[], employeeId, isoYear, isoWeek)→DayDetailRow[]` liefert die Tageszeilen EINER ISO-Woche eines MA (chronologisch; mehrere Einträge desselben Tages werden zusammengefasst — Stunden summiert, Zeitbereiche verbunden). `DayDetailEntry{employeeId,date,hours,absenceType?,isAdditionalCost?,start/end/start2/end2?}` enthält im Gegensatz zu `OvertimeHoursEntry` AUCH Abwesenheitstage + Schichtzeiten; `DayDetailRow{date,dayLabel("09.06."),weekday(dt. `WEEKDAYS_DE`),timeRange("09:00–15:00 / 17:00–23:00", '' wenn keine),productiveHours,absenceType,isAdditionalCost,over84}`. Produktiv-Definition konsistent (`hours>0 && !absenceType && !isAdditionalCost`); `over84 = productiveHours > DAILY_OVERTIME_THRESHOLD`. Reine Helper `formatTimeRange`/`WEEKDAYS_DE`; nutzt interne `parseYMD`/`isoWeekParts`/`pad2`/`round2`. **Persistenz (KEINE Migration):** `loadOvertimeDisabledIds(tenantId)`/`saveOvertimeDisabledIds(tenantId, ids)` in `src/lib/supabase-kv.ts` (mandanten-prefixed Key `overtime-disabled`, Wert `string[]`; load cache-first nur wenn non-empty; save = `kvSetStrict` + dynamischer sonner-Toast bei Fehler). **Rate-Extraktion:** `getEffectiveHourlyRate`+`getWageLabel` in pure `src/lib/employee-rate.ts` (socialCostFactor 1.13); `ActualHoursGrid.tsx` re-exportiert sie. **UI (3-Ebenen-Drill-down-Accordion, ersetzt den früheren Monat/Woche-Umschalter):** eigenständige `src/components/personal-fix/OvertimeCostCard.tsx` (Card/Switch/Table/Checkbox/Badge; lokaler `useState` `Set` für aufgeklappte MA + Wochen → bleibt erhalten solange die Seite offen ist). Toggle „Überstundenkosten einbeziehen" steuert Total+PKQ. **Top-Kacheln (nur `canSeeTotals`, 5 Stück):** Fixlohnkosten (= `regularCost − totalZusatz`), Überstundenkosten (`analysis.totalOvertimeCost`), Zusatzkosten (Σ `additionalCost`), Total Personalkosten (`computeOvertimeTotals(...).effectiveTotal`, Label inkl./exkl. ÜS), PKQ (`effectivePkq`) — Fixlohn = regulär − Zusatz ist reine Aufgliederung, KEINE geänderte Berechnung. **Ebene 1 (KOMPAKT/manager-fokussiert, ersetzt die frühere breite Tabelle — `analysis.employees`, abteilungsgefiltert + nach höchsten ÜS-Kosten sortiert):** über der Tabelle drei Filter-Buttons **Alle/Küche/Service** (`DEPARTMENT_FILTER_OPTIONS`, sofort wirksam, in `localStorage` Key `pf-overtime-dept-filter` gemerkt; `sortEmployeesByOvertimeCost(filterEmployeesByDepartment(analysis.employees, deptFilter))` → `displayedEmployees`; leere Auswahl zeigt Hinweistext). **Standard-Sortierung der Tabelle: höchste ÜS-Kosten zuerst** (`sortEmployeesByOvertimeCost`, REINE ANZEIGE — `overtimeCost===null` zählt als 0/landet hinten, Tie-Break Überstunden-Stunden desc → Name; KEINE Berechnungsänderung, mutiert die Eingabe nicht). Die lib-interne `analysis.employees`-Sortierung (ÜS-Stunden desc → Differenz desc → Name) bleibt unverändert; die Tabelle re-sortiert nur die angezeigte Teilmenge. Tabelle hat NUR noch **7 Spalten** (`OVERVIEW_TABLE`, noch kompakter als `COMPACT_TABLE`): Chevron · **Mitarbeiter** · **Soll Monat** · **Ist Monat** · **ÜS Monat** (`OvertimeBadge`, „deaktiviert" wenn `overtimeDisabled`) · **ÜS Kosten** (gestapelt, s.u.) · **Zusatzkosten** (`ZusatzkostenCell`, gestapelt: Zeile 1 „Monat: CHF…" [= `additionalCost`, unverändert] + Zeile 2 klein „Saldo bis KW{n}: CHF…" [kumulierte Zusatzkosten bis einschl. der AUSGEWÄHLTEN Woche, gleicher Cutoff wie der ÜS-Saldo; „CHF 0.00" wenn bis dahin keine]; „—" wenn `additionalCostHours<=0`; ohne `canSeeIndividualRates` Tage-Fallback ohne Saldo). **Entfernt:** Abteilung, Pensum, Status-Spalte, separate Total-Zusatzkosten-Spalte, separate Woche/Saldo-Spalte. **Klick auf die ZEILE** klappt Ebene 2 auf (Chevron); **Klick auf den NAMEN** (`stopPropagation`) öffnet das Mitarbeiter-Modal `EmployeeOvertimeModal` (Dialog): Titel = Name, Checkbox „Überstundenkosten für diesen Mitarbeiter berechnen" (angehakt = NICHT `overtimeDisabled`; nur bei `showStatusToggle`=`canManageDisable && onToggleEmployeeDisabled`, sonst nur Info + „Schließen"), Info-`dl` (Aktuelle Einstellung/Pensum/Soll Monat/Ist Monat/Überstunden Monat), Speichern (disabled wenn unverändert) ruft `onToggleEmployeeDisabled(id, !calc)` → schließt; Abbrechen/Schließen. **Visuelle Hervorhebung:** AKTIVIERTE MA = blauer Punkt (`bg-blue-500`) + fetter Name; deaktivierte MA = neutraler/grauer Name + `opacity-60`-Zeile. tfoot „Total" aus `summarizeOvertimeEmployees(displayedEmployees)` (gefilterte Summe ÜS-Stunden/-Kosten/Zusatz; CHF-Spalten „—" ohne `canSeeIndividualRates`). Die Top-Kacheln + Header-Satz bleiben monatsweit (volles `analysis`), NUR die Tabelle/tfoot sind abteilungsgefiltert. **Ebene 2 (lazy, beim Aufklappen eines MA — `EmployeeDetail`):** Wochentabelle aus `weeklyByEmployee.get(id)` (KW · Zeitraum [Randwoche „· N/7 Tage"] · Soll · Ist · ÜS [Badge] · „Kosten / Saldo" — eine kombinierte, immer sichtbare Spalte: oben die Wochen-ÜS-Kosten [nur `canSeeIndividualRates`], darunter kumulierter Saldo h [+ CHF nur `canSeeIndividualRates`]; die frühere separate „Saldo h bis KW"-Spalte entfiel), jede Wochenzeile klickbar; darunter separater Zusatzkosten-Block (Liste Tag · Stunden + Total). **Ebene 3 (lazy, beim Aufklappen einer Woche — `DayDetailTable`):** ruft `buildWeekDayRows(dayDetailEntries, employeeId, w.isoYear, w.isoWeek)` und mappt je Zeile `computeDailyOvertimeInfo(productiveHours, rate, disabled)` → **kompakte** Tabelle (Konstante `COMPACT_TABLE`: `text-sm [&_th]:h-9 [&_th]:px-2 [&_td]:px-2 [&_td]:py-1.5`, gilt für ALLE 3 Ebenen-Tabellen) mit den Spalten Datum · Tag (Kurz-Wochentag) · Arbeitszeit · Std. (produktiv) · **+Std Info** · **+CHF Info** (nur `showCost`=`canSeeIndividualRates`) · Abw. (Badge FE/K/… bzw. „Zusatzkosten"). **`computeDailyOvertimeInfo(productiveHours, rate, disabled)→DailyOvertimeInfo{overtimeHours,overtimeCost}`** (pure, in `overtime-analysis.ts` nach `buildWeekDayRows`; REINE ANZEIGE, fliesst NICHT in die offizielle Monats-/Wochen-ÜS-Berechnung): `overtimeHours = round2(max(0, round2(productiveHours − DAILY_OVERTIME_THRESHOLD)))` (8.4h), `overtimeCost = disabled ? 0 : rate==null ? null : round2(overtimeHours × rate)`. **Satz = derselbe `getEffectiveHourlyRate` wie die offiziellen Kosten (enthält BEREITS den Sozialkostenfaktor 1.13 — KEIN zusätzliches ×1.13).** Anzeige je Tag: `hasDayOvertime = !absenceType && !isAdditionalCost && info.overtimeHours>0`; **+Std Info** amber `+X.XX h` (sonst „—", immer sichtbar — Stunden sind keine Lohndaten); **+CHF Info** (gated) „—" wenn kein ÜS, `CHF 0.00` wenn deaktiviert, „kein Satz" bei `rate==null`, sonst amber `chf(cost)`. Spalten „Wochentag/Produktive Stunden/Bemerkung" der früheren Tabelle entfielen. `DayDetailRow.over84` bleibt im Typ (von `buildWeekDayRows` gesetzt), wird in der neuen Tabelle aber nicht mehr separat angezeigt. Fussnote: ÜS über Monatssoll (Wochenansicht anteilig), Abwesenheiten/Zusatzkosten zählen nicht, Tage über 8.4h nur Info, Zusatzkosten bereits im regulären Total. **Berechtigungen (defense-in-depth, obwohl PersonalFix nur admin/beaulieu_manager):** individuelle Kosten/Sätze + Deaktivieren-Checkbox nur bei `canSeeHourlyWages`/`canSeePersonnelCostTotals`, aggregierte Kosten/PKQ nur bei `canSeePersonnelCostTotals`; Überstunden-STUNDEN sind keine Lohndaten → immer sichtbar. **Wiring in `PersonalFix.tsx`:** `overtimeAnalysis` useMemo baut `entries` aus `supabaseActualHours` des Monats (überspringt `absenceType`+`hours≤0`, **flaggt** `isAdditionalCost` statt zu überspringen) UND — additiv, separater Pfad, KEINE Berechnungsänderung — `dayDetailEntries: DayDetailEntry[]` (alle Tage MIT Inhalt inkl. Abwesenheiten + Schichtzeiten start/end/start2/end2, an `OvertimeCostCard` als `dayDetailEntries`-Prop für Ebene 3 durchgereicht), `daysInMonth = new Date(year, month, 0).getDate()`, Kandidaten = `hasFixedSalary(e) && isEmployeeActiveInMonth(e,…)`, `departmentFilter:'all'`, `disabledEmployeeIds: overtimeDisabledIds` (State, geladen via `loadOvertimeDisabledIds` im tenant-Effekt; Toggle-Handler optimistisch + `saveOvertimeDisabledIds`). Karte nach dem KPI-Grid; **`regularCost={pfix.month.istTotal}`** (VOLLER Monat, nicht `pfix.active`/cutoff — ÜS-Soll ist Vollmonat), `netRevenue={effectiveRevenue}`. **SELBSTSTÄNDIGE Karte — fädelt ÜS NICHT in die bestehenden Seiten-Totale ein** (lokaler PKQ-Recompute). **Bekannte, dokumentierte Grenzen:** (1) am Monatsrand nur geladene Tage; (2) Zusatzkosten sind über `zusatzIstCHF` bereits in `pfix.month.istTotal` enthalten → ÜS sind eine separate kalkulatorische Auswertung, Zusatzkosten-Unterzeile ist reine Anzeige. **VERRECHNETER SALDO / AKTUELLE WOCHE (additiv, REINE AGGREGATION — KEINE neue Überstundenlogik):** `computeWeekCumulativeBalances(weeks:WeekOvertimeRow[])→WeekCumulativeBalance[]` bildet je Woche laufende Summen (chronologisch nach `isoYear`,`isoWeek`) der bereits berechneten Wochen-`overtimeHours`/`overtimeCost` UND `additionalCostHours`/`additionalCost`: `WeekCumulativeBalance{isoYear,isoWeek,overtimeHours,overtimeCost,cumulativeOvertimeHours,cumulativeOvertimeCost,additionalCostHours,additionalCost,cumulativeAdditionalCostHours,cumulativeAdditionalCost,combinedCost,cumulativeCombinedCost}` (combinedCost/cumulativeCombinedCost = GESAMTE Zusatz-Personalkosten = ÜS + Zusatzkosten je Woche bzw. kumuliert, gebildet via exportiertem Helper `sumCosts(a,b)` = `round2(a+b)`, `null` wenn ein Bestandteil keinen Satz hat). Kosten-Saldo wird `null`, sobald eine Woche im Präfix keinen Satz hat (gilt für ÜS- UND Zusatzkosten-Saldo getrennt; bei vorhandenem Satz haben Wochen ohne Zusatztage `additionalCost=0`, nicht null → Saldo bleibt Zahl); deaktivierte MA haben je Woche 0 → Saldo 0; mutiert die Eingabe nicht; `round2` überall. `defaultSelectedWeekKey(weeks, today)→{isoYear,isoWeek}|null` wählt die „aktuelle Woche" = ISO-Woche, die `today` enthält (falls in `weeks`), sonst erste Woche (today vor Monat) bzw. letzte Woche (today danach); `null` bei leer. **UI (OvertimeCostCard) — GESAMTE Zusatz-Personalkosten (ÜS + manuelle Zusatzkosten):** Die „ÜS Kosten"-Spalte zeigt GESTAPELT die kombinierten Kosten + den kumulierten kombinierten Saldo (`UeberstundenKostenCell`, ersetzt die frühere separate „Woche / Saldo"-Spalte): 3 Zeilen — Zeile 1 (fett) = „Monat: CHF Y" (= `sumCosts(monthOvertimeCost, additionalCost)`; deaktiviert → ÜS-Anteil 0, also nur Zusatz; keine Zusatzkosten → reine ÜS; beides 0 → CHF 0.00), Zeile 2 (klein/grau) = „KW{n}: CHF Y" der ausgewählten Woche (`bal.combinedCost`), Zeile 3 (klein/grau) = „Saldo bis KW{n}: CHF Y" (`bal.cumulativeCombinedCost`, kumuliert für Wochen ≤ gewählte). Ohne `canSeeIndividualRates` Stunden-Fallback (ÜS-Stunden, keine Lohndaten). tfoot-Summe der Spalte = `sumCosts(totalOvertimeCost, totalAdditionalCost)`. REINE ANZEIGE — die Monats-Kacheln + ÜS-/Zusatzkosten-Berechnung bleiben unverändert. Default-Woche je MA via `defaultSelectedWeekKey`; Klick auf eine Wochenzeile in Ebene 2 setzt die ausgewählte Woche des MA (State `selectedWeekByEmployee`) UND klappt sie auf — die Ebene-1-Zelle aktualisiert sich, die gewählte Wochenzeile wird hervorgehoben (`bg-primary/10`). Die Ebene-2-Wochenspalte „Kosten / Saldo" trägt die kombinierten Wochen-Kosten (`bal.combinedCost`) + den kumulierten kombinierten Saldo „Saldo bis KW{n}" (`bal.cumulativeCombinedCost`; CHF nur `canSeeIndividualRates`, sonst Stunden-Fallback). Kosten/Saldo-CHF nur bei `canSeeIndividualRates`; Stunden-Salden immer sichtbar (keine Lohndaten). KEINE Berechnungsänderung — PersonalFix-Wiring unverändert (Karte filtert/aggregiert clientseitig, berechnet Salden/Default-Woche intern aus den bereits gelieferten `analysis`/`weeklyAnalysis`; der Abteilungsfilter ändert keine Berechnung). KEINE Migration/DB-/Import-Änderung. **Pure Helfer für die kompakte Übersicht (`overtime-analysis.ts`, REINE ANZEIGE):** `DepartmentFilter` (`Department|'all'`), `DEPARTMENT_FILTER_OPTIONS` ([Alle/Küche/Service]), `filterEmployeesByDepartment<T extends {department}>(rows, filter)`, `summarizeOvertimeEmployees(employees)→OvertimeSummary{totalOvertimeHours, totalOvertimeCost (overtimeCost??0), totalAdditionalCost (additionalCost??0), affectedEmployeeCount (overtimeHours>0)}` (round2). Tests: `src/lib/__tests__/overtime-analysis.test.ts` (node env, 92 — `sumCosts` [Summe/round2/null-Propagation], kombinierter Saldo `combinedCost`/`cumulativeCombinedCost` [je Woche + kumuliert, Saldo==ÜS+Zusatz, Wochenwechsel, nur-ÜS, keine-Zusatz→CHF 0.00, null-Propagation]; `filterEmployeesByDepartment` [all/küche/service, leer, keine Mutation] + `DEPARTMENT_FILTER_OPTIONS`-Reihenfolge; `sortEmployeesByOvertimeCost` [höchste ÜS-Kosten zuerst, null→0/hinten, Tie-Break Stunden desc → Name, keine Mutation, leer]; `summarizeOvertimeEmployees` [Summen+affectedCount, null→0, round2, leer→0, deaktiviert zählt nicht]; Modal-Speichern-Semantik [calc(true)=berechnen → neuer Deaktiviert-Status=!calc; „unverändert" wenn neuer Status==aktueller → Speichern gesperrt, für aktivierten+deaktivierten MA je Initial-/Toggle-Zustand]; `computeDailyOvertimeInfo`: Mehrstunden+Mehrkosten über 8.4h [10h@50→1.6h/80], Schwelle 8.4→0, unter Soll→0, deaktiviert→Kosten 0 [Stunden bleiben], `rate==null`→Kosten null, Satz enthält bereits Sozialkosten [kein ×1.13], round2, konsistent mit `buildWeekDayRows.over84`. **Hinweis:** für `OvertimeCostCard` (Ebene-3-Tabelle) gibt es bewusst KEINEN Komponenten-Render-Test — jsdom lädt im Replit-Container die native `canvas`-Bindung (`libuuid` fehlt) und stürzt ab; alle Tests laufen daher im `node`-Env, die neue Logik ist über `computeDailyOvertimeInfo` rein abgedeckt. `computeWeekCumulativeBalances`: laufende Summen Stunden+Kosten, unsortierte Eingabe chronologisch, Jahreswechsel KW53/2026 vor KW1/2027, null-Kosten-Propagation [Stunden laufen weiter], deaktiviert→Saldo 0, Wochenauswahl-Lookup, keine Mutation, leer→leer; Zusatzkosten-Saldo (kumuliert): laufender CHF+Std-Saldo, spätere Wochen nicht im früheren Saldo, Satz vorhanden+keine Zusatztage→Saldo 0 (nicht null), null-Propagation ohne Satz [Std laufen weiter], letzte Woche == Monatssumme, round2; `defaultSelectedWeekKey`: today im Monat→enthaltende KW, vor Monat→erste, nach Monat→letzte, leer→null; `buildWeekDayRows`: nur Tage der ISO-Woche/des MA chronologisch, dt. Wochentage+Tageslabel, geteilte Schichten als Zeitbereich, Abwesenheitstage 0 produktiv + Code, Zusatzkosten nicht produktiv + markiert, über 8.4h informativ [8.4 NICHT darüber], Mehrfacheinträge desselben Tags zusammengefasst, leere Eingabe; Monat: Konstanten, `weeklyTargetHours`/`workloadPercent`/`monthlyTargetHours` (Pensum × Monatslänge), Prädikat; unter Soll 160<180→0, über Soll 190→10, 80 % (Soll 144) 150→6, stündlich/vollzeit-ohne-Monatslohn ausgeschlossen, Abwesenheiten ignoriert, deaktiviert→0 Kosten (Differenz bleibt), `isAdditionalCost` raus aus ÜS + separat ausgewiesen, MA mit NUR Zusatzkosten erscheint, Tage über 8.4h nur Info, Abteilungsfilter, `computeOvertimeTotals` Toggle inkl./exkl. + null-PKQ; Woche: `weeksInMonth(2026,6)`→KW23–27 [7/7/7/7/2 Tage], `proratedWeeklyTargetHours` voll/anteilig/80 %, 50h/Woche→8h ÜS, 30h→0, 80 % 40h→6.4h, Randwoche KW27 14h@Soll12→2h, deaktiviert→0 (Differenz bleibt), stündlich/Abwesenheit/Zusatzkosten ausgeschlossen, Woche fängt was Monat verfehlt [Monat unter Soll, KW23 8h ÜS]).
- **Küchen-Manager Tages-Header-Totale (lohn-sicher):** Pure `src/lib/schedule-daily-totals.ts` (KEIN React/supabase/DOM) liefert die Per-Tag-Spaltenkopf-Totale für den `kueche_manager` im Wochen-Dienstplan (`ModernScheduleGrid`-Header). `dailyTotalsVisibility('full'|'manager')` → `{showHours,showRevenue,showRatio,showTargetIndicator,showCostChf,showIndividualWages}`; **'full' (admin/service/beaulieu) = nur `showHours`** → Verhalten UNVERÄNDERT; 'manager' = Stunden+Umsatz+Quote+Zielindikator, `showCostChf`/`showIndividualWages` IMMER false. `computeDailyLaborRatio(cost,revenue,thresholdPercent)` → `{ratio: cost/revenue*100 (null bei revenue≤0), isOverTarget: ratio>threshold}` (gleiche Einheiten wie die Wochen-/Monats-PKQ-Karten). `computeDailyKitchenTotals(emps:EmployeeDayInput[],revenue,thresholdPercent,daysInMonth)` → `DailyKitchenTotals{plannedHours,personnelCost(CHF),revenue,laborCostRatio,isOverTarget}`; Kostenmodell spiegelt `weeklyPlannedLaborCost`: Festangestellte (vollzeit/teilzeit mit `monthlySalary`>0) → `monthlySalary/daysInMonth`, Stündliche → `(dayHours+dayAbsenceHours)*hourlyWage`. **`toDailyTotalsDisplay()` strippt `personnelCost`** → grid-seitige `DailyTotalsDisplay{plannedHours,revenue,laborCostRatio,isOverTarget}`, d.h. **kein CHF-Lohnbetrag erreicht je die Grid-Props/DOM**. Verdrahtung: `SchedulePlanner.tsx` `dailyManagerTotals` useMemo (gibt `{}` zurück außer `isKuecheManager`; iteriert `displayDays`×`visibleEmployees` — gleiche kitchen-scoped Basis wie die PKQ-Karten, NICHT das row-gefilterte `displayEmployees`; `revenue=dailyBudgets[date].plannedRevenue`, `threshold=laborCostThreshold`) → Props `managerSafeTotals={isKuecheManager}`+`dailyManagerTotals` an `ModernScheduleGrid`. `ModernScheduleGrid.tsx` Header-Block: `managerSafeTotals` → Manager-Block (Netto-Stunden, „CHF {revenue}", Quote% emerald/rot + `AlertTriangle` bei over-target, „–" wenn `ratio==null`), sonst die UNVERÄNDERTE Brutto-`dayTotals`-Zeile. Nur `kueche_manager` betroffen (admin+service unverändert), keine Migration/DB-Änderung. **Bekannte Grenze (nicht im Scope):** Mitarbeiter-Objekte mit `hourlyWage`/`monthlySalary` erreichen den Grid bereits für alle Rollen (Pre-existing Gap, kein NEUER Leak hier; echte Lohn-Vertraulichkeit braucht später eine Data-Layer/Projektion). Tests: `src/lib/__tests__/schedule-daily-totals.test.ts` (node env, 8 — Stunden/Umsatz sichtbar, keine Löhne/CHF [Display ohne `personnelCost`], nur Küchen-Mitarbeiter via `getVisibleEmployeesForRole`, admin 'full' unverändert, Quote-Edgecases revenue=0/over-target/Festangestellten-Proration).
- **Dienstplan-Export Abteilungsfilter (abteilungstreu):** Pure `src/lib/schedule-export-department.ts` (`selectEmployeesForDepartment(employees, dept)`, KEIN supabase/DOM; re-exportiert von `schedule-export-import.ts`, alle Export-Pfade routen darüber) filtert Mitarbeiter strikt nach Abteilung (`'all'`=alle, sonst exakte Abteilung) — behebt den Bug, dass der Küchen-Export auch Service enthielt. `ExportOptionsDialog` nimmt `activeDepartment` (setzt die Abteilung beim Öffnen auf die aktive) + `lockDepartment`; `SchedulePlanner` übergibt `lockDepartment={!canSwitchDepartment}`, sodass eingeschränkte Manager (Küche/Service) die Abteilung im Export nicht wechseln können. Tests: `src/lib/__tests__/schedule-export-department.test.ts` (node env, 5 Tests — Küche/Service/all + Leerfälle).
- **Manager-Mitarbeiter-Sichtbarkeit (zentrale Quelle der Wahrheit):** Pure `src/lib/employee-visibility.ts` (KEIN supabase/DOM; delegiert an `selectEmployeesForDepartment`) ist die EINZIGE Stelle, die entscheidet, welche Mitarbeiter eine Rolle sehen/auswählen/exportieren/zählen darf. Exporte: `departmentForRole(role)` (`kueche_manager`→`'küche'`, `service_manager`→`'service'`, sonst `null`), `effectiveEmployeeDepartmentScope(role, allowedDepartment)` (eingeschränkte Rolle GEWINNT über `allowedDepartment` = defense-in-depth; admin/beaulieu folgen `allowedDepartment`='all'), `getVisibleEmployeesForRole(role, allowedDepartment, employees)`. **Invariante:** admin/`beaulieu_manager` (`allowedDepartment`='all') → unveränderte Liste (No-op). Verdrahtung in `SchedulePlanner.tsx`: `roleScopedEmployees = useMemo(getVisibleEmployeesForRole(...))` direkt nach dem `employees`-State; `activeEmployees`-Basis + ALLE Anzeige-/Auswahl-/Zähl-/Export-Pfade (paFixCost, varHoursExceeded, detectPatternWarnings, PDF/Template-Export, publish `targetEmps`+`publishDiffAll`, StaffingStatusBar, ActualHoursImportButton, EmployeeHoursSummary, kosten-gegatete „Mitarbeiter (N)"-Tabelle, „Alle (N)"-Zähler, Publish-Personenauswahl, sämtliche Dialoge) lesen `roleScopedEmployees`; `activeDepartment`-Init + Rollen-Coercion-Effekt nutzen `effectiveEmployeeDepartmentScope`. In `Personalstamm.tsx` routet `visibleBase` durch `getVisibleEmployeesForRole` (pending_review bleibt separat ausgeblendet); Neuanlage-Default-Abteilung + gesperrter Abteilungs-Select unverändert. **Bewusst RAW (`employees`) gelassen:** localStorage Laden/Speichern/Merge, Mitarbeiter-CRUD, und der Import-MATCH (`importScheduleFromExcelV2`/`matchEmployeeByName`) — Matching MUSS die volle Liste sehen, sonst werden Dubletten angelegt. **Bekannte Grenzen (nicht im Scope dieses Fixes):** (1) `AuthContext` startet `role='admin'` und stuft asynchron auf `kueche_manager` herab → kurzer Render-Frame, in dem die volle Liste sichtbar ist (dokumentierte Auth-Bootstrap-Entscheidung); (2) der Import-PREVIEW/-Write-Pfad ist abteilungsübergreifend (Match läuft bewusst gegen die volle Liste). Tests: `src/lib/__tests__/employee-visibility.test.ts` (node env, 12 synthetische — küche/service/admin/beaulieu, Rolle gewinnt bei `allowedDepartment`='all', Export-Scope nur Küche, keine Mutation).
- **Schicht-Schnellauswahl Farbe ändern (ShiftLegend):** In `src/components/schedule-planner/ShiftLegend.tsx` hat jede Schichtzeile (Arbeits- UND Abwesenheits-Schichten) im Hover-Overlay einen Paletten-Button, der ein `SIDEBAR_COLORS`-Swatch-Panel umschaltet; Auswahl ruft `updateShifts` mit neuer `color` und geleerten `excelColor`/`textColor` auf, die via `getExcelColorsFromTailwind(color)` in `useShiftConfig.ts` neu abgeleitet werden.
- **CSV/PDF Import:** `src/lib/csv-import-engine.ts`, `src/lib/pdf-import-engine.ts`, `src/pages/CSVImport.tsx`
- **Gastronovi Produkt-CSV-Import (Anzahl+Umsatz → `product_sales`):** `src/pages/SalesUpload.tsx` + pure `src/lib/gastronovi-csv-parser.ts`. `matchAnzahlUmsatz` aggregates BOTH the Anzahl and Umsatz files **per normalized product name, summing day values per date** (`aggregateRowsByName`), emitting exactly ONE record per `(product, sale_date)` — fixes the old `Map.set` last-write-wins (dropped revenue) + per-line duplicate emission (inflated qty / double-counted revenue). `MatchResult.duplicateProducts` lists merged products (blue info banner in the preview). Same-name-different-price articles get summed (flat CSV has no article number — inherent limit). Tests: `src/lib/__tests__/gastronovi-csv-parser.test.ts` (node env, synthetic).
- **Supplier Documents:** `src/types/supplier-documents.ts`, `src/lib/supplier-documents-store.ts`, `src/pages/SupplierDocuments.tsx`, `src/pages/SupplierComparison.tsx`
- **Onboarding Flow:** `src/pages/OnboardingPage.tsx`, `src/components/onboarding/`
- **Reservations Import (Foratable):** `src/pages/ReservationenImportPage.tsx` (embeddable Tab des Foratable Imports), `src/lib/reservation-import-parser.ts`, `src/lib/reservation-import-db.ts`, Migration `supabase/migrations/20260621_reservations.sql` (Tabellen `reservation_imports`, `guest_profiles`, `reservation_records`) — getrennt von den `gn_*`-Importen. **Idempotent** über UNIQUE `(restaurant_id, external_reservation_id)` + `.upsert(onConflict)` (Re-Import aktualisiert statt dupliziert; CSV-interne Duplikat-Res.Nr. last-row-wins). `saveReservationImport` liefert `inserted`/`updated` (read-before-write), `duplicateKeyMerged`, `skippedRows` (Zeilen ohne Res.Nr.). Gemeinsame Summary-UI `src/components/reservations/ReservationSummary.tsx` (Import-Preview + Foratable Report). Tests: `reservation-import-db.test.ts` (In-Memory-Supabase-Mock).
- **Foratable Report:** `src/pages/ForatableReportPage.tsx` (admin-only `/foratable-report`, `isAdmin && !isGuest`, Fetch vor `Navigate` geguardet). Read-only KPI-Report über importierte `reservation_records` (Von/Bis + Schnellbereiche); pure `src/lib/foratable-report.ts` + read-only `foratable-report-db.ts` (paginiert, tenant-gefiltert, wirft bei Fehler). Neu/Wiederkehrend = Gast hatte irgendeine Reservation strikt VOR `from`. PDF-Export (jsPDF) + Print. **„Beste Reservationszeiten“:** pure `src/lib/reservation-time-analysis.ts` gruppiert nach exakter Startzeit; schliesst BEWUSST `cancelled`/`noshow` aus (anders als `computeReservationStats.topTimes`, das alle Status zählt — realisierte Nachfrage); Zeilen ohne Zeit → `ignoredNoTime`; `ReservationSummary` hat additives `hideTopTimes`. Tests: `foratable-report.test.ts`, `reservation-time-analysis.test.ts`.
- **Gästeexport Import (Foratable → CRM-Anreicherung):** `src/pages/GaesteImportPage.tsx` (admin-only Tab `/foratable-import?tab=gaeste`), pure Parser/Matching in `foratable-guest-import-parser.ts` + `foratable-guest-import.ts` (Match email→phone→name mit Ambiguitäts-Flag, `mergeCrmProfile` fill-empty-only), Orchestrierung `foratable-guest-import-db.ts` (Commit re-plant frisch; tenant-gefilterter Bulk-Upsert NUR in `guest_crm_profiles`, wirft bei Schreibfehler). **Scope-Guard:** reichert NUR LEERE manuelle CRM-Felder bestehender Gäste an — überschreibt nie, fasst `reservation_records`/`guest_profiles`/`guest_statistics`/Besuchszähler/Segmente NICHT an. Keine Migration. Tests: `foratable-guest-import-parser.test.ts` + `foratable-guest-import.test.ts`.
- **Import-Center (zentrale Karten-Übersicht):** `/import` = `src/pages/ImportHub.tsx`; Zugriff admin + `beaulieu_manager` + `beaulieu_viewer` über die EINE Quelle `visibleCategories(...)` (gleiches Gate für Nav/Seite/Karten; Gäste = 0 Karten); Inline-Sektionen darunter admin-only. **Launcher-Modell, kein Embedding:** Karten navigieren zu bestehenden Routen (`kind:'route'`) oder scrollen zu Anchors (`kind:'inline'`, admin-only). Pure `src/lib/import-center.ts` (9 Kategorien in fixer Reihenfolge; `canAccess` spiegelt die ECHTEN Route-Guards → Sichtbarkeit admin=9 / beaulieu_manager=3 / beaulieu_viewer=1 / Gast=0; Status-State-Machine none/imported/attention/unknown; **Invariante: niemals Zeitstempel erfinden** — ohne echtes Signal `lastImportedAt:null`); `import-center-db.ts` liest NUR die Status-Quellen der SICHTBAREN Karten (Least-Privilege; stabiler `sourcesKey` gegen Refetch-Loop; Fehler degradiert pro Kategorie isoliert zu `unknown`). Nav konsolidiert auf EIN Item „Import“. Tests: `import-center.test.ts` (18) + `import-center-db.test.ts` (6).
- **Foratable Import (Sammelseite):** `src/pages/ForatableImportPage.tsx` (admin-only `/foratable-import`); Tabs via `?tab=` (`reservationen` Default | `gaeste` | `verlauf`), eingebettete Seiten über `embedded`-Prop, `forceMount`+`hidden` erhält den Wizard-State bei Tab-Wechsel; Alt-Routen `/reservationen-import` + `/gaeste-import` redirecten; zwei „Letzter Import“-Karten (DB-Zeitstempel) + Import-Verlauf-Tab (Filter Alle/Reservationen/Gästeexport).
- **Import-Historie (gemeinsam):** beide Foratable-Importe loggen in EINE Tabelle `import_runs` (Migration `20260623_import_runs.sql`, RLS authenticated-only/anon-revoked). `logImportRun` (`import-runs-db.ts`) ist **best-effort und wirft NIE**, verdrahtet NUR an den Seiten-Callsites — `saveReservationImport`/`commitForatableGuestImport` UNVERÄNDERT; `created_by` = Operator-UUID (keine E-Mail/PII), `finished_at` = **DB `DEFAULT now()`** (nie Browser-Zeit); nur Aggregat-Zahlen + Dateiname, nie Gast-PII. Pure `import-runs.ts`. Tests: `import-runs.test.ts`.
- **Gäste-CRM (Liste + Kern):** `src/pages/GaesteCrmPage.tsx` (Liste/Suche/Segmente), `src/lib/reservation-crm.ts` (Single Source of Truth für Segment-/Metrik-Logik; `simpleGuestStatus` Neu=1 / Wiederkehrend=2–3 / Regelmässig=4–9 / VIP≥10 ist REIN besuchsbasiert und fliesst NIE in `classifySegment`/`computeCrmScore` — Umsatz-Tiers unmöglich, `reservation_records` hat keine Geldspalte), `src/lib/reservation-crm-db.ts` (read-only, paginiert; Tenant-Isolation getestet in `reservation-crm-db.test.ts`). Metriken/Segmente live in TS über `guest_profiles`/`reservation_records` (keine eigenen Tabellen); optionale read-only SQL-View `guest_statistics` (`20260622_guest_statistics_view.sql`) spiegelt exakt die TS-Semantik.
- **Manuelle CRM-Daten in der Gäste-Liste:** `GuestListMetrics` trägt optionales `crm` (additiv — wird NIE von `classifySegment`/`computeCrmScore` gelesen); Bulk-Read `fetchGuestCrmProfilesByIds` (`guest-crm-profile-db.ts`, chunked `.in`; tenant-sicher NUR weil Aufrufer tenant-gescope-te IDs aus `fetchGuestProfiles` übergeben); Ladefehler → amber Banner + leere Map (manuelle Filter/Badges/Kampagnen bleiben leer). Bool-Filter/Sort in `src/lib/guest-list-filters.ts` (fehlendes CRM → alle Flags false).
- **Gäste-CRM Smart-Segmente:** pure `src/lib/guest-smart-segments.ts` — 7 LIVE-Segmente mit Mehrfach-Zugehörigkeit aus den geladenen `GuestListMetrics` (+`m.crm`), **strikt additiv** (nie in Segment/Score, keine neue Query/Migration): `vip` ≥10 Besuche, `regular` ≥4, `new` ≤30d, `upcoming_birthday` (30d-Fenster, jahres-agnostisch mit Wraparound, 29.2.→1.3.), `never_returned` =1 Besuch, `at_risk` ≥90d, `lost` ≥180d (überlappt `at_risk` bewusst). Badges `src/components/crm/SmartSegmentBadges.tsx`; Filter + CSV-Export je Segment (reused Kampagnen-CSV-Helfer). Tests: `guest-smart-segments.test.ts` (Grenzwerte, Geburtstags-Wrap, Multi-Membership).
- **Gäste Detailseite (Kundenakte):** `src/pages/GaesteDetailPage.tsx` (admin-only `/gaeste/:guestId`), pure `src/lib/reservation-guest-profile.ts`. Tabs: **Übersicht** (CRM-Score 0–100 — Gewichte Besuche 40 % [voll ≥20] / Aktualität 25 % [voll ≤30d] / Regelmässigkeit 20 % / Gruppengrösse 15 %; Tiers 0-39 niedrig / 40-69 mittel / 70-89 hoch / 90-100 VIP-Potenzial; Besuchsverhalten inkl. Gast-Status-Kachel, Präferenzen inkl. Lieblingsmonat [completed-only], Risiko [reused `reservation-dashboard.ts`], 90d-Besuchstrend [nicht-überlappende Fenster], filterbare Historie), **CRM** (manuelles Profil), **Aktivität**. Tests: `reservation-guest-profile.test.ts`.
- **Gäste CRM-Profil (manuell, editierbar):** Tabelle `guest_crm_profiles` (Migration `20260622_guest_crm_profiles.sql`; 1:1 via `guest_id` UUID PK + FK `ON DELETE CASCADE`, **kein** `restaurant_id` — Tenant über den Eltern-Gast; RLS wie `20260621_reservations.sql`). Pure Mapping/Validierung `guest-crm-profile.ts` (`normalizeCrmProfile`, `manualCrmBadges`); DB-Layer `guest-crm-profile-db.ts` verifiziert VOR Read/Write die Tenant-Zugehörigkeit via `fetchGuestById`, Write wirft (kein silent fallback). Manuelle Felder (VIP/Stammgast/Firmenkunde/Newsletter/Sperrliste, Geburtstag, Firma, Allergien, Notizen …) sind **strikt getrennt** vom berechneten Segment/Score/Kampagnen-Prädikaten. CRM-Tab mit Dirty-Tracking (Speichern + Änderungen verwerfen); manuelle Badges im Header + read-only Summary-Card. Tests: `guest-crm-profile.test.ts` + `guest-crm-profile-db.test.ts`.
- **CRM Auswertung (Dashboard):** `src/pages/CrmAuswertungPage.tsx` (admin-only `/gaeste/auswertung`), pure `src/lib/reservation-dashboard.ts`. **Status-Semantik:** aktiv=`confirmed|completed`, offen=`pending|unknown`, ausgeschlossen=`cancelled|noshow`. Tabs Übersicht/Rückkehrpotenzial/Kampagnen; klickbare KPI-Kacheln (zukünftige Reservationen, individueller Zeitraum) → Drilldown `src/components/crm/ReservationDetailList.tsx`; **Zählung und Liste kommen aus DENSELBEN `ReservationDetailRow[]`** (Listenlänge === Kachelwert, Personensumme === Kachel-Personen). Gastnamen klickbar via shared `GuestProfileLink.tsx` (null-safes `guestId`, `stopPropagation`). View-State komplett in der URL via pure `crm-auswertung-url.ts` — inkl. **open-redirect-sicherem `parseFromParam`** (synthetische Origin + EXAKTE `/gaeste/auswertung`-Allowlist) und String-Gleichheits-Guard gegen Sync-Loops; `GaesteDetailPage`-Back-Button ist kontextbewusst (`from`-Param). Tests: `reservation-detail-list.test.ts`, `crm-auswertung-url.test.ts` (30).
- **Reservationen nach Wochentag (3 Modi: Zeitraum / Schulferien BE / Saisonvergleich) — NAV-VERSTECKT, nur per URL erreichbar (abgelöst durch `/gaeste/analyse`, Route bewusst behalten wie `/reporting`):** `src/pages/ReservationWochentagPage.tsx` (admin-only `/gaeste/wochentag`, gated auf Route-Guard UND Lade-Effekt, tenant-gefiltert via `fetchReservationsInRange`); gesamte Logik pure in `src/lib/reservation-weekday-analytics.ts` (NO supabase/DOM). Kern: `aggregateByWeekday` (je ISO-Wochentag Mo=1..So=7: Reservationen/Personen/**Vorkommen im Zeitraum** [`countWeekdayOccurrences`]/Ø pro Vorkommen/Anteil), `StatusScope` `booked` (Default, ohne Storno/No-Show)/`active`/`all`, Kennzahl-Toggle Reservationen/Personen/Ø Personen (`metricValue`; Reservationszahl bleibt in JEDEM Modus sichtbar), 4 KPI-Karten + Balkendiagramm + Haupttabelle, Monatsvergleichs-Heatmap `buildMonthComparison` (Zellen-Rang vs. Spalten-Ø; **Top/Tief markiert ALLE Zellen am Extremwert**, chronologischer Tie-Break; Default eingeklappt) mit Wochentag-Fokus-Selektor, deterministische dt. Headlines/Insights/Interpretation (**leere Buckets fliessen NIE in Ø/Extreme; Gleichstand → früherer Wochentag/Monat**), Detail-Popup je gefüllte Zelle (`src/components/crm/MonthWeekdayDetailDialog.tsx`, `buildMonthWeekdayDetail`/`buildRangeWeekdayDetail`, saison-bewusste Labels + optionales `rangeLabel`). Presets: aktueller/letzter Monat, letzte 3 Monate (rollierend über Jahreswechsel), Okt–Dez, Winter-/Sommersaison (manuell anpassbar, pro Mandant in localStorage `pkt:reservation-weekday-seasons:<tenantId>`), Individuell (Startmonat/Endmonat `type="month"`). View-State (f/t/p/sc/y/m) URL-gespiegelt (einmaliger `initRef` + String-Vergleich gegen Endlosschleife). Ferien- und Saison-Modus teilen den Holiday-Pfad (`aggregateHolidayWeekday`/`buildHolidayMonthComparison`, EIN Superset-Fetch über `holidayBounds`). KEINE Migration/Schreibzugriffe. Tests: `reservation-weekday-analytics.test.ts` (node env, 195 synthetische).
- **Saisonvergleich (frei definierte, fixe Datumsbereiche):** dritter Modus derselben Wochentag-Seite (`Layers`-Button). Nutzer definieren beliebig viele FIXE Saisons `{id,name,from,to,color?,active}` (CRUD `SeasonManagerDialog.tsx`; pro Mandant in KV `pkt:reservation-weekday-seasons:defs:<tenantId>` via `season-definitions-db.ts`/`useSeasonDefinitions` — **keine Migration**). `seasonsToComparisonPeriods` (**key = Saison-ID**, ungültige verworfen), Auswahl via Farbchips, Jahr-Stepper ausgeblendet (Saisons jahresfix). Ab 2 Saisons zusätzlich `SeasonComparisonSection.tsx` (Recharts-Linien Mo→So, Rangliste je Wochentag mit Stärkste/Schwächste-Badges, deterministische Empfehlungen — `buildSeasonRecommendations` liefert `[]` bei <2); Diagrammpunkte + Ranglisten-Einträge öffnen das Detail-Popup (zeigt Saison-Zeitraum via `rangeLabel`). Leere gewählte Saisons bleiben als 0-Zeile sichtbar (+ amber Hinweis); GAR keine Saisons definiert → Seed-Button `exampleSeasonDefinitions()` (5 Saisons 2026, stabile Slug-IDs). URL `mode=saison&seasons=<ids>` (Alt-Param `sel` toleriert). Tests im Wochentag-Test-File.
- **Reservationen: Monat Ist vs. Vorjahr (YoY, REINE ANZEIGE — keine Tabelle/Migration/Schreibzugriffe) — NAV-VERSTECKT, nur per URL erreichbar (abgelöst durch `/gaeste/analyse`):** `src/pages/ReservationVorjahrPage.tsx` (admin-only `/gaeste/vorjahr`, gated `isAdmin && !isGuest` auf Route-Guard UND Lade-Effekten; Route VOR `/gaeste/:guestId`). Zeitraum = EIN Jahr + Von/Bis-Monat-Selects (Quick-Buttons „Bis heute“/„Ganzes Jahr“/„Okt–Dez“), `StatusScope`-Toggle; lädt Ist + Vorjahr via `fetchReservationsInRange` (`Promise.all`, tenant-gefiltert). Pure `src/lib/reservation-yoy-utils.ts` (reuse `monthKeyOf`/`shiftMonthKey`/`statusPredicate` aus `reservation-weekday-analytics`): `makeYoyMetric` (**kein Vorjahr bekannt → prior/diff/diffPct null + Trend neutral** [kein falsches „Wachstum“ gegen leeren Monat]; **Vorjahr 0 → diffPct null**, kein Div/0), `buildYoyComparison` (**`hasPriorData` = Vorjahresmonat hat Zeilen VOR dem Status-Filter**; **Totals summieren das Vorjahr NUR über Monate MIT Vorjahresdaten** — UI zeigt blauen „x von y Monaten“- bzw. amber „keine Vorjahresdaten“-Hinweis), `buildYoyDayComparison` (Tages-Paarung 1..31, 29. Feb. erscheint einseitig; stärkste/schwächste Tage = Top/Bottom 3 nach IST-Wert, **nur Tage mit Ist>0**), `buildYoyWeekdayComparison` (**Wochentagsverteilung Mo–So Ist vs. gleiche Vorjahresmonate** — im Monats-Drilldown UND als „Wochentagsverteilung im Zeitraum“-Card; dokumentierte Näherung: verglichen werden absolute Summen, Wochentags-Vorkommen können zwischen Jahren abweichen). UI: 6 KPI-Kacheln (Res/Pers je Ist/VJ/Wachstum, Trend-Farben), Monats-Tabelle (Diff-Zellen grün/rot, „kein Vorjahr“-Badge, Total-Fusszeile), Zeilen-Klick → Drilldown-Dialog (6 Monats-KPIs mit expliziten VJ-Werten, stärkste/schwächste Tage mit Kennzahl-Toggle, Wochentags-Tabelle, Tages-Tabelle mit Diff-Färbung). Formatierung `+`/`−` (U+2212)/`±0`, „—“ für unbekannt. Tests: `reservation-yoy-utils.test.ts` (node env, 33 synthetische).
- **Reservations Analyse (konsolidierte Seite, ersetzt die zwei obigen Nav-Einträge — Foratable Report UNBERÜHRT):** `src/pages/ReservationAnalysePage.tsx` (admin-only `/gaeste/analyse`, gated `isAdmin && !isGuest` auf Route-Guard UND Lade-Effekten; Route VOR `/gaeste/:guestId`; Nav „Reservations Analyse" Gruppe Foratable, TrendingUp-Icon — die alten Einträge `/gaeste/wochentag`+`/gaeste/vorjahr` sind aus der Nav ENTFERNT, Routen bleiben per URL erreichbar). **Globaler Kennzahl-Umschalter Personen (DEFAULT)/Reservationen** steuert KPI-Kacheln, ALLE Tabellen UND beide Detail-Popups (Einfach-Kennzahl-Spalten Ist/VJ/Diff/Diff %). Pure `src/lib/reservation-analyse-utils.ts` (NO supabase/DOM; reuse `reservation-yoy-utils` + `reservation-weekday-analytics`): `AnalyseMetric`/`DEFAULT_ANALYSE_METRIC='persons'`/`pickMetric(pair,metric)` (wählt aus YoyMonthRow/YoyWeekdayRow/Totals die aktive Kennzahl), `avgPersonsPerReservation` (null bei 0 — nur klein als KPI-Sub angezeigt), Zeitraum-Presets `presetMonthRange(preset,today)` (aktueller/letzter Monat [Jahreswechsel-fest Jan→Dez VJ], aktuelles/letztes Jahr, Wintersaison Okt–Dez des heutigen Jahres) + `matchesPreset` (aktive Button-Optik), `buildWeekdayMonthBreakdown({currentRows,priorRows,monthKeys,weekday,scope})` (Wochentag-Popup „Zusammensetzung": je Ist-Monat NUR dieser Wochentag vs. Vorjahresmonat; **`hasPriorData` auf MONATS-Ebene** [alle Wochentage, vor Status-Filter — konsistent mit `buildYoyComparison`]: VJ-Monat mit Daten aber ohne diesen Wochentag = echtes Vorjahr 0, kein „kein Vorjahr"). Seite: Zeitraum = EIN Jahr + Von/Bis-Monat (Default Jan–heute) + 5 Preset-Buttons + `StatusScope`-Toggle (booked Default); lädt Ist+VJ via `fetchReservationsInRange` (`Promise.all` MIT `.catch`); 4 KPI-Kacheln der aktiven Kennzahl; shadcn-Tabs **„Monat Ist vs. Vorjahr"** (Monats-Tabelle, Zeilen-Klick → Tagesvergleich-Popup mit stärksten/schwächsten Tagen nach aktiver Kennzahl + Wochentagsverteilung + Tages-Tabelle), **„Wochentag-Analyse"** (Mo–So-Tabelle, Zeilen-Klick → Zusammensetzungs-Popup) und **„Saison / Zeitraum"** (Monats- + Wochentagsaufschlüsselung kombiniert); amber/blaue Hinweise bei fehlendem/teilweisem Vorjahr wie YoY-Seite. KEINE Migration/Schreibzugriffe/Import-Änderung. Tests: `reservation-analyse-utils.test.ts` (node env, 19 synthetische — Default persons, pickMetric wirkt auf Monat+Wochentag, VJ-0→diffPct null, leere Monate=0-Zeilen, Presets inkl. Jahreswechsel, Breakdown-Scope/Status/Robustheit).
- **Rückkehrpotenzial & gefährdete Stammgäste:** pure in `src/lib/reservation-dashboard.ts` (`OVERDUE_INTERVAL_FACTOR=1.5`). Überfällig = `daysSinceLastVisit > avgDaysBetweenVisits × 1.5` (braucht ≥2 Besuche + positives Intervall); Gefährdet = Besuchszahl-Tier (`baseSegmentByVisits` in `reservation-crm.ts` — **nicht** das Live-Segment, das nach 90d auf `inaktiv` kippt) ist `stammgast`/`vip` UND letzter Besuch >90d. Shared Badge `src/components/crm/SegmentBadge.tsx`. Tests: `reservation-return-potential.test.ts`.
- **CRM Kampagnen (Listen & Export):** pure `src/lib/reservation-campaigns.ts` — 18 `CAMPAIGNS` (Segment-basierte nutzen das **live** `m.segment`; Inaktiv >90/>180 tagesbasiert; Überfällig/Gefährdet/Neukunden-30/No-Show reusen `reservation-dashboard.ts`; **7 manuelle CRM-Kampagnen lesen NUR `m.crm`-Flags**, nie das berechnete Segment). Dritter Tab in `CrmAuswertungPage`. CSV: UTF-8 **mit BOM (in der Seite beim Download, NICHT in `buildCampaignCsv`)**, `;`-getrennt, CRLF, dt. Header, `dd.MM.yyyy`; 8 manuelle CRM-Spalten werden JEDER Zeile angehängt (Indizes 9–16). Tests: `reservation-campaigns.test.ts`.
- **Gäste-Duplikate (Erkennung & Zusammenführung):** `src/pages/GaesteDuplikatePage.tsx` (admin-only `/gaeste/duplikate`, Fetch vor `Navigate` geguardet, Route VOR `/gaeste/:guestId`); Gruppierung nach **normalisiertem Telefon** (`guest-duplicates.ts`: `normalizePhoneKey`, `mergeCrmProfiles` [bool OR, fill-empty, idempotente Zeilen-Union], `fillEmptyIdentity` [NIE `match_key`]). `mergeGuests` (`guest-duplicates-db.ts`) — **keine DB-Transaktion**: Preflight (IDs unique, alle Profile tenant-gefiltert geladen, count==erwartet → Abbruch VOR jedem Write) → Reservationen ZUERST auf Master umhängen (FK `ON DELETE SET NULL`) → CRM-Merge → Identität auffüllen → **Master-Aggregate aus `reservation_records` NEU berechnen (nie summieren)** → Postcondition-Check → Duplikate löschen (CASCADE) → best-effort **PII-freies** Audit-Log (`20260623_guest_merge_log.sql`). Jeder Read/Write `.eq('restaurant_id')`. Tests: `guest-duplicates.test.ts` + `guest-duplicates-db.test.ts` (in-memory Mock inkl. Cross-Tenant-Abort).
- **CRM Segment-Aktionen (intern, mandantengesichert):** 5 manuelle, bestätigungsbasierte Aktionen je Smart-Segment-Ansicht in `GaesteCrmPage` (CSV-Export, Kontaktliste, Als kontaktiert markieren, Notiz, Folgeaufgabe — KEINE E-Mails/externen Integrationen); UI `SegmentActionBar.tsx` (Migrations-Hinweis wenn Tabellen fehlen, CSV bleibt aktiv). Tabellen via `20260624_crm_activities.sql` (RLS authenticated-only, manuell ausführen): `guest_crm_activities` (Typ `contacted`|`note`|`follow_up` mit **per-Typ CHECK-Constraints**, `batch_id` für Sammelaktionen) + `guest_crm_contact_lists` (JSONB Snapshot); `created_by` = Operator-UUID, keine PII. Pure `crm-activities.ts` (Fan-out `buildActivityInsertRows`, `splitActivitiesByType`); DB `crm-activities-db.ts`: **`verifyGuestsBelongToTenant` vor JEDEM Write** (ein fremder Gast bricht VOR jedem Write ab), jeder Read/Write tenant-gescoped, best-effort `[]` NUR bei fehlender Tabelle (echte Fehler werfen). Aktionsziel = explizite Checkbox-Auswahl sonst die angezeigten Zeilen. `GaesteDetailPage`-Tab „Aktivität“ (Folgeaufgaben-Switch → `setFollowUpStatus`). Tests: `crm-activities.test.ts` + `crm-activities-db.test.ts`.

## Architecture decisions
- **Multi-tenancy:** Implemented via ID prefixes (`b-` for Beaulieu) in Supabase `employees` and `app_settings` keys, avoiding a dedicated `restaurant_id` column for simplicity and backwards compatibility.
- **Data Persistence Strategy:** LocalStorage is used as the primary, fast data store, with Supabase `app_settings` serving as a secondary, persistent backup, especially for financial and budget data. A sync hook (`useSyncStore`) handles this.
- **Authorization:** A centralized `usePermissions` hook enforces role-based access control, defined in `src/hooks/usePermissions.ts`, for granular control over features and data visibility.
- **Onboarding Flow:** Supports both existing employees completing details via a tokenized link and new self-registrations creating `pending_review` employee records for admin approval.
- **Time-based Reporting:** A global "Stichtag" (cut-off date) filter (`StichtagContext`) allows historical reporting and period closing, impacting various financial views.

## Product
- **Dashboard:** Overview of KPIs, revenue, costs, pro-rata personnel costs.
- **Schedule Planning:** Plan and actual shift schedules.
- **Target/Actual Analysis:** Comparison of planned vs. actual performance.
- **Personnel Master Data:** Employee data management (admin only).
- **Reporting/Financial Module:** Comprehensive financial data including prior year comparisons (P&L, budget).
- **Fixed Personnel Costs:** Overview of monthly salaries including 13th month salary by department.
- **Multi-Tenant Support:** Manage data for multiple restaurants (Oliv, Beaulieu) with tenant-specific data isolation.
- **Role-Based Access:** Different user roles (admin, service_manager, kueche_manager) have varying data access and permissions.
- **Onboarding System:** Facilitates employee data collection and self-registration.
- **Guest Link System:** Allows admins to generate temporary, password-protected read-only links for sharing sessions.
- **Pattern Warnings:** Identifies operational risks in schedules (e.g., consecutive days, short recovery times).
- **Product Master Data:** Centralized database for food/beverage articles, WES (cost of goods sold) per product, and tracking.
- **WES Analysis:** Compares WES from recipes, suppliers, and accounting for cost control.
- **Reservations Import:** Imports Foratable reservation-tool CSV exports (admin only) with upload→preview→save wizard and per-file history; recognizes returning guests for guest/behavior analytics. Existing-guest reuse before creating a new profile matches **only by normalized phone → email (case-insensitive), tenant-scoped** — name is NOT used for matching (avoids merging different people who share a name). Name-only bookings (no phone/email) still de-duplicate via the `(restaurant_id, match_key)` upsert.
- **Gästeexport Import (CRM-Anreicherung):** Admin-only import (`/foratable-import?tab=gaeste`, also linked in the Import-Zentrale) that enriches existing guests' manual CRM profiles from a Foratable *guest-list* export. Matches each CSV row to an existing guest by normalized email→phone→name (ambiguous matches are reported as "nicht zugeordnet"), then fills ONLY empty CRM fields (VIP, Newsletter, Sperrliste, Geburtstag, Firma, Lieblingsplatz, Notizen) — already-maintained values are kept and divergences counted as conflicts. Preview shows matched/created/updated/conflicts/unassignable before committing. Reservations, visit counts and segments are never touched; CSV-only, no email/WhatsApp.
- **Gäste-CRM:** Admin-only guest list with search (name/email/phone), visit count, first/last visit, average visit interval, and auto-segmentation (VIP ≥20 / Stammgast 8–19 / Wiederkehrend 3–7 / Neukunde 1–2 / Inaktiv >90d / Ohne Besuch). The list also surfaces the **manual CRM profile**: VIP/Stammgast-manuell, Firmenkunde, Newsletter and Sperrliste show as badges next to each guest; eight Ja/Nein/Alle filters (VIP manuell, Stammgast manuell, Firmenkunde, Newsletter, Sperrliste, Allergien vorhanden, Geburtstag hinterlegt, CRM-Notiz vorhanden) plus sortable Firma- and Geburtstag-columns let admins slice by manual data. Manual data is shown read-only here and never changes the auto-computed segment. The per-guest detail page is a full CRM customer record (Kundenakte) with six sections: a weighted CRM-Score 0–100 (niedrig/mittel/hoch/VIP-Potenzial), Besuchsverhalten (incl. a simple visit-count-based Gast-Status: Neu/Wiederkehrend/Regelmässig/VIP — separate from the segment), Präferenzen (favorite area/weekday/month/time/party-size), Risiko (no-shows, cancellations, overdue, at-risk), Besuchstrend (last 90d vs prior 90d → steigend/stabil/rückläufig), and a filterable reservation history (Alle/Nur Besuche/Nur Storniert/Nur No-Show). A visit = a completed reservation. Optional `guest_statistics` SQL view exposes the same KPIs directly in Supabase. The detail page now has two tabs: **Übersicht** (the read-only analytics above) and **CRM** (editable manual profile, see below).
- **Gäste CRM-Profil (Bearbeitung):** Admin-only editable manual CRM record per guest on the detail page's **CRM** tab — status flags (VIP / Stammgast manuell, Firmenkunde, Newsletter, Sperrliste), Stammdaten (Geburtstag, Firma, Sprache), Präferenzen (Lieblingsplatz/-bereich/-wein/-gericht), Allergien & Unverträglichkeiten, and internal notes. Two buttons: **Speichern** saves all fields at once and **Änderungen verwerfen** resets the form to the last-saved state (no DB write); both are disabled while there are no unsaved edits. If no record exists yet the form starts empty and the first save creates the row (upsert). Manual VIP/Stammgast flags additionally show as badges in the guest header and in a read-only **CRM-Profil** summary on the Übersicht tab (VIP, Stammgast, Geburtstag, Firma, Allergien, Notizen), shown independently of the auto-computed segment. Manual data is kept strictly separate and never changes the auto-computed segment/score/campaigns. Read-only guest links (guest sessions) cannot edit. Stored in the dedicated `guest_crm_profiles` table; computed live metrics remain untouched.
- **CRM Auswertung:** Admin-only dashboard (`/gaeste/auswertung`) with three tabs. **Übersicht:** guest-overview KPI cards (total/active/inactive/new/repeat-rate/VIP/Stammgäste/ohne Besuch/No-Show-Risiko/Rückkehrpotenzial), future-reservation analysis (current & next month, next 30/60/90 days, plus open/unanswered shown separately), and a custom date-range filter (active vs. open reservations + persons). All future-reservation tiles and the custom-range result tiles are clickable and open a detailed list of the underlying reservations (date, time, guest, persons, status, room/area, contact). **Rückkehrpotenzial:** KPI cards for overdue guests, at-risk Stammgäste and at-risk VIP, plus a list of overdue guests (most-overdue first) linking to each guest's detail page. **Kampagnen:** 18 predefined campaign lists — the 11 behavioural ones (überfällige/gefährdete Stammgäste & VIP, inaktiv >90/>180 Tage, 2+ No-Shows, Wiederkehrende, Stammgäste, VIP, Neukunden letzte 30 Tage, ohne Besuch) plus 7 manual-CRM ones (VIP manuell, Stammgäste manuell, Firmenkunden, Newsletter, mit Allergien, mit Geburtstag, Sperrliste). Each shows count + description with "Anzeigen" (detail table: Name, E-Mail, Telefon, Segment, Besuche, letzter Besuch, Tage seit letztem, Ø Intervall, No Shows) and "CSV exportieren" (only the filtered list, UTF-8 with BOM, semicolon-separated, German headers, `crm-kampagne-<slug>.csv`; the CSV always includes 8 manual-CRM columns). Admin-only, CSV download only — no email/WhatsApp. Computed live in TS from existing reservation tables; no new migration.
- **Rückkehrpotenzial (gefährdete Stammgäste) — Gästeliste:** Flags regulars slipping away — `returnRiskRatio` = daysSinceLastVisit ÷ personal avgDaysBetweenVisits; `isAtReturnRisk` = ratio ≥ `RETURN_RISK_FACTOR` (2) AND visits ≥ `RETURN_RISK_MIN_VISITS` (3). Logic in `src/lib/reservation-crm.ts`; filter (`returnRisk`) + sort key (`returnRisk`) in `src/lib/guest-list-filters.ts`; KPI count `returnRiskGuests` in `src/lib/reservation-dashboard.ts`. Surfaced on `/gaeste` (filter dropdown, quick-filter chip, "Rückkehr-Risiko" column showing the overdue ratio) and as the `Rückkehrpotenzial` KPI card in the `/gaeste/auswertung` Übersicht tab. This 2.0×-factor metric is distinct from the Rückkehrpotenzial **tab's** 1.5×-factor `OVERDUE_INTERVAL_FACTOR` logic (`reservation-dashboard.ts`); both coexist. Independent of the static "Inaktiv (>90d)" segment so it catches guests early. Admin-only, computed live in TS, no migration.

## User preferences

### Datenintegrität — verbindliche Entwicklervorgabe
- **Umsatzdaten dürfen niemals per localStorage-Overwrite oder naivem Blob-Write gespeichert werden.**
- Jeder Schreibpfad für `dailyBudgets` / `beaulieu:dailyBudgets` muss `safeUpsertDailyBudgets()` verwenden.
- `safeUpsertDailyBudgets` liest immer zuerst den aktuellen Supabase-KV-Stand, mergt, schreibt zurück.
- Bei KV-Schreibfehler erscheint eine sichtbare Fehlermeldung — kein stilles Fallback auf localStorage.
- Die `daily_revenues`-Tabelle (Roadmap in diesem File) wird erst aktiviert, wenn die aktuelle Lösung fachlich fertig getestet ist. Bis dahin keine Migration ausführen.

## Technische Roadmap — daily_revenues Migration

### Kontext
Tagesumsätze werden aktuell als grosser KV-Blob in `app_settings` gespeichert
(Schlüssel `dailyBudgets` / `beaulieu:dailyBudgets`). Als Sofortschutz gegen
Datenverlust wurde `safeUpsertDailyBudgets` eingeführt (liest KV → mergt →
schreibt zurück). Diese Lösung ist stabil, hat aber strukturelle Grenzen:
kein Audit-Log, kein atomarer UPSERT pro Tag, Merge-Konflikte bei Parallelzugriff.

### Zielarchitektur
Normalisierte Tabelle `daily_revenues` in Supabase Postgres:
```sql
-- Vorbereitet in supabase/migrations/20260507_daily_revenues.sql
CREATE TABLE daily_revenues (
  restaurant_id TEXT    NOT NULL,  -- 'oliv' | 'beaulieu'
  date          DATE    NOT NULL,
  actual_revenue        NUMERIC,
  planned_revenue       NUMERIC,
  actual_food           NUMERIC,
  actual_beverage       NUMERIC,
  actual_labor_cost     NUMERIC,
  previous_year_revenue NUMERIC,
  created_at    TIMESTAMPTZ DEFAULT now(),
  updated_at    TIMESTAMPTZ DEFAULT now(),
  updated_by    TEXT,              -- user email für Audit-Trail
  PRIMARY KEY (restaurant_id, date)
);
```
Jeder Tag hat einen eindeutigen Datensatz — kein Blob, kein Merge nötig.

### Migrationsschritte (in dieser Reihenfolge)

**Schritt 1 — SQL-Migration ausführen**
- Script `supabase/migrations/20260507_daily_revenues.sql` im Supabase SQL-Editor
  ausführen (noch nicht aktiv).
- Zählt bestehende KV-Daten vor und nach der Migration (Abweichung = 0 prüfen).

**Schritt 2 — Datenmigration KV → Tabelle**
- Einmalige Server-Side-Migration: alle Einträge aus `app_settings` wo
  `key IN ('dailyBudgets', 'beaulieu:dailyBudgets')` als normalisierte Zeilen
  in `daily_revenues` einfügen.
- Validierung: `SELECT count(*) FROM daily_revenues` muss ≥ bisherige Blob-Tage.

**Schritt 3 — Neue Datenbankschicht**
- Neue Datei `src/lib/daily-revenues-db.ts` mit:
  - `upsertDailyRevenue(restaurantId, date, fields)` → atomarer UPSERT inkl.
    `updated_by` und `updated_at`
  - `getDailyRevenues(restaurantId, fromDate, toDate)` → Rückgabe als Map
  - `getDailyRevenueMonth(restaurantId, year, month)` → 31-Tage-Array
- Kein localStorage mehr für Tagesumsätze — direkt aus Supabase Postgres.

**Schritt 4 — Lese- und Schreibpfade umstellen**
Betroffene Komponenten (alle nutzen aktuell `safeUpsertDailyBudgets`):
1. `GastronoviImportSection` (handleSave, commitImport, handleImportClick)
2. `TagesansichtPage` (saveManualIst)
3. `TagesControllingPage` (commitUmsatzEdit)
4. `Dashboard` (saveRevenue)
5. `useVj2025Import` (Vorjahres-Seed)
6. `usePersonnelData` (Lohnkosten-Schreibpfad)
7. `SchedulePlanner` (Lohnkosten-Tagesblock)

**Schritt 5 — Audit-Log**
- Tabelle `daily_revenues` enthält `updated_by` (Email des einloggten Benutzers)
  und `updated_at` (Timestamp).
- Optional: separate `daily_revenues_audit`-Tabelle mit Trigger für vollständigen
  Change-History.

**Schritt 6 — Fallback entfernen**
- `safeUpsertDailyBudgets` auf Read-Only-Fallback reduzieren (nur noch lesen,
  nicht mehr schreiben).
- `useSyncStore` `SYNC_KEYS`-Liste: `dailyBudgets` entfernen.
- localStorage-Einträge `dailyBudgets` / `beaulieu:dailyBudgets` nach Migration
  löschen (Cleanup-Script).

**Schritt 7 — Integrationstests**
Alle vier Views müssen für jeden Testtag identische Werte zeigen:
- Tagesansicht (`TagesansichtPage`)
- Dashboard (`Index.tsx`)
- Tages-Controlling (`TagesControllingPage`)
- Erfolgsrechnung / P&L (`PLView.tsx`)

Testscript: `src/lib/__tests__/daily-revenues-integration.test.ts` (noch zu erstellen).

### Status
- [x] SQL-Migration-Script vorbereitet (`supabase/migrations/20260507_daily_revenues.sql`)
- [ ] SQL-Script im Supabase SQL-Editor ausführen
- [ ] Datenmigration KV → Tabelle
- [ ] `daily-revenues-db.ts` implementieren
- [ ] Schreibpfade umstellen
- [ ] Audit-Log aktivieren
- [ ] Fallback entfernen
- [ ] Integrationstests

## Gotchas
- **Supabase Migrations:** All SQL migration scripts in `supabase/migrations/` **must** be executed sequentially in the Supabase SQL Editor.
- **Tenant Filtering:** Data for Beaulieu is filtered by `id LIKE 'b-%'` and for Oliv by `id NOT LIKE 'b-%'`; there is no explicit `restaurant_id` column.
- **Mirus Name Mapping:** Manual mapping might be required for Mirus imports to correctly link employee names.
- **Default Budget Seed:** The 2026 budget is seeded automatically upon first load if no `plLineItems` exist for that year.

## Pointers
- **Supabase Documentation:** [https://supabase.com/docs](https://supabase.com/docs)
- **React Documentation:** [https://react.dev/](https://react.dev/)
- **Tailwind CSS Documentation:** [https://tailwindcss.com/docs](https://tailwindcss.com/docs)
- **Shadcn UI Documentation:** [https://ui.shadcn.com/docs](https://ui.shadcn.com/docs)
- **Vite Documentation:** [https://vitejs.dev/guide/](https://vitejs.dev/guide/)